# Bounded ECMAScript 2026 codec

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

- Feature id: `feature/sim-codecs/javascript-source-frontend`
- Subject: `crate/sim-codec-javascript`
- Canonical key: `crate/sim-codec-javascript/feature-sim-codecs-javascript-source-frontend`

Tokenize and structurally parse frozen ECMAScript 2026 Script and Module goals, lower every accepted form to stable javascript/* expressions, and round-trip through plain, located, and tree codec lanes.

## Anchors

- `anchor/crate/sim-codec-javascript`
- `anchor/runtime-lib/sim-codec-javascript/javascript-codec-lib`

## Surfaces

- `syntax/javascript`

## Specimens

- `spec-test/sim-codecs/crates/sim-codec-javascript/tests/conformance`

## Worked Example

Specimen `spec-test/sim-codecs/crates/sim-codec-javascript/tests/conformance` is checked by `cargo test`.

Source `crates/sim-codec-javascript/tests/conformance.rs`:

```rust
use sim_codec_javascript::{Goal, LexicalGoal, NodeKind, TokenKind, parse_module, parse_script};

// conformance: frozen ECMAScript source is bounded, located, and byte-preserving.
#[test]
fn lossless_script_and_module_frontend() {
    let script = include_str!("corpus.js");
    let tree = parse_script(script).expect("curated Script corpus");
    assert_eq!(tree.preserve_source(), script);
    assert_eq!(tree.goal, Goal::Script);
    assert!(
        tree.tokens
            .iter()
            .any(|token| token.kind == TokenKind::RegExp)
    );
    assert!(
        tree.tokens
            .iter()
            .any(|token| token.goal == LexicalGoal::Div)
    );

    let module = parse_module("import x from 'x'; export default class C {}").unwrap();
    assert_eq!(module.root.kind, NodeKind::Module);
}
```
