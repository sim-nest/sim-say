# Output contract repair command

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

- Feature id: `feature/sim-agent-net/output-contract-repair-cli`
- Subject: `crate/sim-lib-agent-runner-core-recipe-output-contract-repair`
- Canonical key: `crate/sim-lib-agent-runner-core-recipe-output-contract-repair/feature-sim-agent-net-output-contract-repair-cli`

Repair model output contracts with the checked runner-core fixture command.

## Anchors

- `anchor/cli/sim-lib-agent-runner-core-recipe-output-contract-repair`
- `anchor/crate/sim-lib-agent-runner-core-recipe-output-contract-repair`

## Surfaces

- `cli/sim-lib-agent-runner-core-recipe-output-contract-repair`

## Specimens

- `spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/agent_recipe_result`

## Worked Example

Specimen `spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/agent_recipe_result` is checked by `cargo test`.

Source `crates/sim-lib-agent/src/tests/agent_recipe_result.rs`:

```rust
use super::support::{eval_cx, install_agent_lib, install_roundtrip_codecs};
use sim_kernel::{Args, Expr, Symbol};

// conformance: output contract repair recipes return checked agent results.

#[test]
fn agent_recipe_result_decodes_embedded_recipe_expectation() {
    let mut cx = eval_cx();
    install_roundtrip_codecs(&mut cx);
    install_agent_lib(&mut cx).unwrap();

    let recipe_id = cx
        .factory()
        .string("a30-001-autonomous-decision".to_owned())
        .unwrap();
    let value = cx
        .call_function(
            &Symbol::qualified("agent", "recipe-result"),
            Args::new(vec![recipe_id]),
        )
        .unwrap();
    let expr = value.object().as_expr(&mut cx).unwrap();

    let Expr::List(items) = expr else {
        panic!("expected recipe result list");
    };
    assert_eq!(
        items.first(),
        Some(&Expr::Symbol(Symbol::new("agent-pattern")))
    );
}
```
