# Raised exception SDK composition

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

- Feature id: `feature/sim-sdk/raised-exception-composition`
- Subject: `crate/sim-nest`
- Canonical key: `crate/sim-nest/feature-sim-sdk-raised-exception-composition`

Expose the one non-recursive envelope, bounded class matcher, and managed relation adapter for every guest runtime.

## Anchors

- `anchor/rustdoc/sim-nest/exceptions`

## Specimens

- `spec-test/sim-sdk/tests/exceptions_facade`

## Worked Example

Specimen `spec-test/sim-sdk/tests/exceptions_facade` is checked by `cargo test`.

Source `tests/exceptions_facade.rs`:

```rust
#![cfg(feature = "control")]

// conformance: the SDK exports the canonical raised envelope, matcher, and managed relation adapter.

#[allow(unused_imports)]
use sim::exceptions::match_raised_class;
use sim::exceptions::{ManagedException, Raised};

#[test]
fn exports_envelope_matcher_and_managed_adapter() {
    let _ = std::any::type_name::<Raised>();
    let _ = std::any::type_name::<ManagedException<(), ()>>();
}
```
