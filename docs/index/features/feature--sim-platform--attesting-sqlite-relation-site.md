# Attesting SQLite relation capsule

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

- Feature id: `feature/sim-platform/attesting-sqlite-relation-site`
- Subject: `crate/sim-platform-sqlite`
- Canonical key: `crate/sim-platform-sqlite/feature-sim-platform-attesting-sqlite-relation-site`

Realize checked relation plans against bounded memory or preopened SQLite stores with typed values, atomic migration attestations, normalized introspection, safe attach, and stable failures.

## Anchors

- `anchor/crate/sim-platform-sqlite`

## Specimens

- `spec-test/sim-platform/crates/sim-platform-sqlite/src/lib`

## Worked Example

Specimen `spec-test/sim-platform/crates/sim-platform-sqlite/src/lib` is checked by `cargo test`.

Source `crates/sim-platform-sqlite/src/lib.rs`:

```rust
// conformance: SQLite realization remains bounded, attested, and locator-safe.

//! Attesting `SQLite` realization behind the provider-neutral relation site.
#![forbid(unsafe_code)]
#![deny(missing_docs)]

mod driver;

pub use driver::*;
```
