# SIM Index Routes

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

## User routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `route/add-automotive-workflow` | Add an automotive diagnostic or service workflow | code, framework, user | `feature/sim-auto/automotive-runtime` - The automotive runtime row owns the shared contracts, bounded lanes, codec, and view integration.<br>`spec-test/sim-auto/crates/sim-lib-auto-order/tests/conformance` - The conformance specimen checks the order boundary through the public runtime contract. |
| `route/add-http-search-site` | Add a provider-neutral HTTP search site | user, code, framework | `feature/sim-agent-net/provider-neutral-http-search` - Configure the codec behind the bounded shared HTTP skill transport.<br>`recipe/sim-agent-net/crates/sim-lib-search-http/01-basics/fixture-site` - The fixture proves live and cassette execution without a provider-specific transport. |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/adopt-and-subtract-stewardship-composition` | Review, adopt, rebuild, and make a stewardship composition smaller | user, framework | `feature/sim-sdk/stewardship-adoption-subtraction` - Use the generic transaction and subtraction discipline without embedding household choices in public fixtures.<br>`recipe/sim-sdk/stewardship/adopt-subtract-composition` - Inspect the data-only composition, complete preview, rebuild inputs, tombstones, and declined mutual-porch outcome.<br>`spec-test/sim-sdk/tests/validate_adopt_subtract_composition` - Prove ephemeral-state-independent rebuild and absence of retired parts from every surface and replay. |
| `route/analyze-counterpoint` | Analyze counterpoint and bounded stretto relations | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Counterpoint and identity-bearing Staff conversion plus audited transform operations.<br>`feature/sim-music/exact-score-consonance` - The consonance row supplies exact half-open sounding-window alignment without collapsing voice or event identity.<br>`feature/sim-music/music-counterpoint` - The counterpoint row applies inspectable rule data to existing material and builds bounded graph-backed stretto relations while keeping analysis distinct from generation.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/stretto-graph` - The checked Lisp specimen exposes candidate entries, compatible couples, maximal cliques, and normalized chains with generation=false. |
| `route/analyze-metrical-patterns` | Quantize, compare, and discover repeated musical patterns | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - Music analysis supplies exact tempo/meter lattices, named feature extractors, invariance transforms, identity-bearing occurrences, support, and overlap policy while delegating DTW/search to sim-discrete and correlation to sim-numbers.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/metrical-similarity-pattern` - The checked descriptor states the straight/swing/tuplet quantization, DTW/correlation similarity, and bounded exact-verification contract. |
| `route/analyze-tonnetz-path` | Find a bounded neo-Riemannian path between canonical chords | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch-set owner supplies canonical masks and typed set relations; the chord owner supplies rooted chord identity; the existing Riemann namer supplies projection-only labels.<br>`feature/sim-music/exact-music-analysis-and-transform` - Music analysis applies enabled P/L/R generators over canonical major/minor identities and delegates deterministic shortest-path certificates to sim-lib-discrete-graph.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/tonnetz-path` - The checked descriptor gives the concrete C-major to A-minor call, explicit move set and bound, identity policy, one-edge R result, and reusable certificate contract. |
| `route/analyze-twelve-tone-row` | Validate, transform, and inspect a twelve-tone row without losing aliases or label conventions | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary supplies the canonical PitchClass identity; unordered PitchClassMask and pitch-set analysis stay in their existing owners.<br>`feature/sim-music/serial-series-calculus` - The generic serial layer supplies stable finite alphabet identity and exhaustive exactly-once aggregate validation without assuming pitch.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict ToneRow values, total normalized P/I/R/RI operations, alias-preserving RowFamilySet values, convention-explicit RowMatrix data and ASCII projection, operation-bearing RowForm values, explicit label conventions, and the narrow pitch/ToneRow Shape adapter.<br>`recipe/sim-music/crates/sim-lib-pitch-serial/01-basics/row-family-matrix` - The checked Rust scenario proves all 48 Op. 25 aliases against their operations, retains symmetry aliases across distinct-form reduction, and projects one source-and-convention-bearing matrix to structured coordinates and ASCII. |
| `route/assemble-agent-kind` | Assemble and run a different agent kind | user, code, framework | `feature/sim-agent-net/genai-assembly` - The existing GenAI assembly supplies the checked model exchange and late-bound provider placement.<br>`feature/sim-agent-net/manifest-selected-conduct` - AgentManifest binds roles, components, provider seats, authority, budgets, and the selected conduct independently.<br>`feature/sim-agent-net/agent-conduct` - Choose ReAct, plan-act-replan, phased, verify-retry, router-crew, or another certified data-only conduct and inspect its reflected contract before running it.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/react` - The checked ReAct specimen runs a catalog conduct without a kind-specific Rust loop.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/standard_steps` - The executable specification proves the standard Cards and typed validation boundary used by every catalog kind. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/audify-measured-series` | Hear and creatively transform measured evidence without category fraud or route lock-in | user, code, framework | `feature/sim-music/daw-session-runtime` - The session owner records reversible or explicitly lossy audification, disjoint artistic-mapping provenance, stable content identity, and append-only route substitutions.<br>`feature/sim-music/audio-lift-and-render` - The audio owner supplies bounded analysis and deterministic rendering without owning scientific meaning.<br>`spec-test/sim-music/crates/sim-lib-daw-session/src/listening` - The listening specimens prove route churn leaves musical and expedition identity stable, mapping families cannot decode as one another, all named target substitutions are typed, and unsupported targets retain exact evidence. |
| `route/audit-analytic-mechanical-impact` | Audit a mechanical contact impulse with momentum and work equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite retains one-sided impact states, an explicit impulse, independent kinetic stores, clean selection inputs, and momentum/work identities.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common one-sided event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for momentum, work, provider-envelope, and modal-root proofs. |
| `route/audit-analytic-switched-electrical-study` | Audit a switched lumped electrical study against independent equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite proves two oriented voltage/current ports, a discontinuity, terminal event, stiff interval, independent stores, refinement contraction, and a certified threshold.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for the public equation-derived composition and metamorphic cases. |
| `route/audit-conjugate-port-work` | Audit signed continuous and impulsive work across physical boundaries | code, framework, user | `feature/sim-physics/boundary-event-core` - Declare the boundary and crossing port identities.<br>`feature/sim-physics/conjugate-port-work` - Pair effort and flow, orient the port, split continuous work at events, and retain impulses separately.<br>`spec-test/sim-physics/crates/sim-lib-physics-power/tests/work` - The specimen proves analytic domains, orientation, discontinuities, simultaneous events, and pure impulses.<br>`recipe/sim-physics/01-power/switched-two-port` - Run a switched electrical-to-mechanical two-port audit. |
| `route/audit-independent-stored-energy` | Audit stored-energy balance without deriving endpoint stores from transferred work | code, framework, user | `feature/sim-physics/conjugate-port-work` - Retain signed continuous work and event transfers at the declared boundary.<br>`feature/sim-physics/stored-energy-audit` - Evaluate both endpoint states constitutively and compute the visible balance with typed evidence lanes.<br>`spec-test/sim-physics/crates/sim-lib-physics-audit/tests/audit` - The specimen proves independent perturbations, sign and omission failures, distinct residuals, and separated uncertainty.<br>`recipe/sim-physics/02-audit/stored-energy` - Inspect the equation, Shapes, and runtime exports. |
| `route/audit-search-provenance` | Audit a search result from final rank to immutable source evidence | user, code, framework | `feature/sim-web/search-audit-surface` - The offline surface distinguishes unverified provider claims from selector-checked quotations and exposes ranking, fetch, robots, fidelity, and policy provenance.<br>`recipe/sim-web/crates/sim-lib-view-search/01-basics/offline-audit` - The checked recipe demonstrates a complete audit projection without network authority. |
| `route/author-agent-conduct` | Author an agent conduct control loop | user, code, framework | `feature/sim-agent-net/agent-conduct-records` - The core records define the stable frame, step Card, budget, journal, replay, and fork vocabulary every conduct uses.<br>`feature/sim-agent-net/agent-conduct-topology-profile` - The topology profile certifies data-only conduct packages and binds their Cards through the shared topology engine.<br>`feature/sim-agent-net/agent-conduct` - The agent/conduct-v1 profile validates RunFrame Shapes, step Cards, outcome routing, roles, authority, budgets, terminal admission, and embedded tests.<br>`recipe/sim-agent-net/crates/sim-lib-agent-conduct/01-basics/echo-conduct` - The checked recipe demonstrates a minimal authored topology package loaded and executed through the shared engine.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/third-party-step` - The checked extension specimen adds a Card and implementation without editing conduct or topology source.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/router-crew` - The data-only triage and routing family proves a nontrivial authored graph remains catalog data rather than baked-in control flow. |
| `route/build-audio-workbench` | Build an audio workbench | user, code | `feature/sim-audio-daw/audio-graph-workbench` - The graph row collects the DSP and plugin recipes for local construction.<br>`feature/sim-audio-daw/audio-device-providers` - The provider row selects a modeled or host-backed audio target.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-core/01-basics/copy-node` - The copy-node recipe is the smallest graph example.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-dsp/01-basics/offline-chain` - The DSP recipe names the reusable filter, dynamics, delay, and modulation processor chain.<br>`spec-test/sim-audio-daw/crates/sim-lib-audio-dsp/src/tests` - The DSP specimen proves preallocated oscillator/resampler state, folded-harmonic suppression, impulse gain, passband response, and downsampling alias rejection.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-live/01-basics/live-config` - The live recipe selects bounded callback buffers and queues before host placement. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/algorithm-foundry` - The foundry row composes the complete performance plan without baking stage providers into the application.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/capture-web-evidence` | Capture and replay immutable web evidence | user, framework | `feature/sim-agent-net/immutable-web-capture` - The web/fetch owner composes legacy retrieval with explicit policy and immutable evidence.<br>`recipe/sim-agent-net/crates/sim-lib-web-fetch/01-basics/offline-capture` - The network-free fixture proves identical offline replay and versioned re-extraction. |
| `route/certify-physics-refinement` | Distinguish numerical convergence from a certified mathematical-model verdict | code, framework, user | `feature/sim-physics/stored-energy-audit` - Retain independent audit evidence lanes.<br>`feature/sim-physics/certified-orthogonal-refinement` - Refine each assumption explicitly and require a sealed enclosure for definite claims; its crate integration tests prove sign reversal, non-monotonicity, independent failures, refusal of estimates, continuation diagnoses, and reproducible identity.<br>`recipe/sim-physics/03-proof/certified-refinement` - Inspect event-exact meshing and runtime exports. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/complete-exact-score-consonance` | Complete an exact score with reversible consonance additions | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns identity-bearing Staff values and the generic additive staff apply/remove seam.<br>`feature/sim-music/exact-score-consonance` - The evaluation row supplies exact sounding windows and separately inspectable named metrics.<br>`feature/sim-music/reversible-consonance-completion` - The completion row composes typed additions, constraints, reports, content binding, changed windows, provenance, and exact inversion while delegating deterministic traversal, bounds, interrupts, and receipts to sim-lib-discrete-search.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/additive-consonance-completion` - The checked descriptor and paired Rust specimen show the bounded reversible completion contract. |
| `route/complete-serial-consonance` | Complete a realized serial plan with bounded reversible additions | user, code, framework | `feature/sim-music/serial-practice` - The serial row owns the immutable plan, stable event identities, strict realization, and the structural-versus-all-sounding reading boundary that the adapter must preserve.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row supplies the optional modal-projection evidence that the completion adapter can admit explicitly without pretending the result remains strict chromatic output.<br>`feature/sim-music/exact-score-consonance` - The exact score row establishes the identity-bearing staff surface and exact sounding timeline that additive completion must preserve.<br>`feature/sim-music/reversible-consonance-completion` - The reversible completion row defines the generic additive-search contract that the serial row narrows through serial-specific legality and post-completion ledgers.<br>`feature/sim-music/serial-consonance-completion` - The serial completion row filters generic candidates through serial allowances, leaves the structural plan unchanged, delegates deterministic finite traversal, work/result bounds, cancellation, and honest receipts to sim-lib-discrete-search, and returns structural plus all-sounding ledgers beside the reversible patch result.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/serial-consonance-completion` - The checked Rust scenario completes one realized row statement with a stated pitch-class addition, proves the structural ledger is unchanged, and records the added material only in the all-sounding ledger. |
| `route/compose-calm-flock-care-pack` | Care for a flock without measuring its worth | user, framework | `feature/sim-sdk/calm-flock-care-pack` - Compose flock identities, common care, observation, supply, attention, factual handoff, and copied claims as bounded data.<br>`recipe/sim-sdk/stewardship/calm-flock-care` - Exercise a single calm flock-level capture for eight synthetic birds with an isolated kitchen claim.<br>`spec-test/sim-sdk/tests/validate_flock_pack` - Prove the twenty-second budget, unknown-on-missing rule, factual handoff boundary, and absence of scoring, culling, diagnosis, treatment, and automated care. |
| `route/compose-chosen-media-current-references` | Keep only chosen media traces and current cited references | user, framework | `feature/sim-sdk/chosen-media-current-reference-packs` - Compose selected imports, authored meanings, local search, citation currency, and explicit removal as bounded data.<br>`recipe/sim-sdk/stewardship/chosen-media-traces` - Import and remove one selected synthetic trace with every remote and inference lane absent.<br>`spec-test/sim-sdk/tests/validate_media_reference_packs` - Prove source-policy excerpts, predicate separation, guidance metadata, removal semantics, and networkless operation. |
| `route/compose-cited-seasonal-garden-pack` | Rebuild a garden season without predicting or commanding work | user, framework | `feature/sim-sdk/cited-seasonal-garden-pack` - Compose source-bounded recommendations, optional projections, exact observations, and questions as data while Mia retains authority.<br>`recipe/sim-sdk/stewardship/cited-seasonal-garden` - Rebuild a season and add a bed and crop entirely through data while all optional provider lanes remain absent.<br>`spec-test/sim-sdk/tests/validate_garden_pack` - Prove independent Shapes, predicate separation, expiry behavior, calendar non-authority, the harvest claim boundary, and networkless operation. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-independent-atelier-room` | Compose one independent intellectual room | user, framework | `feature/sim-sdk/atelier-room-packs` - Select a data profile whose own route and authority ceiling compose the existing domain owner through the canonical pack graph.<br>`spec-test/sim-sdk/tests/validate_atelier_packs` - The offline specimen validates all six closures and their room-specific forbidden operations, including declined porch success and the data-only toy extension. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/compose-jvm-from-sdk` | Compose the bounded JVM from the SDK | user, code, framework | `feature/sim-sdk/jvm-composition` - One feature exposes the existing decoder, authority, profile, invocation, and specimen contracts without a facade-owned runtime.<br>`recipe/sim-sdk/jvm/product-surface` - The public recipe imports every JVM composition contract through sim alone.<br>`recipe/sim-sdk/jvm/lambda-interop` - The public recipe reaches both lambda interop directions through sim alone.<br>`spec-test/sim-sdk/tests/jvm_exports` - The downstream-only test proves every JVM entry is reachable through the SDK. |
| `route/compose-model-observatory-from-sdk` | Compose the provider-neutral model observatory from the SDK | user, code, framework | `feature/sim-sdk/model-test-facade` - The granular feature exposes the canonical observatory without changing the minimal facade.<br>`feature/sim-agent-net/model-test-product` - The owning product supplies command semantics, offline projections, and deterministic evidence export. |
| `route/compose-music-algorithms-from-sdk` | Compose the modular music algorithm foundry from the SDK | user, code, framework | `feature/sim-sdk/music-algorithm-composition` - The SDK row owns focused feature selection and curated re-exports over the frozen candidates while the music owner keeps the open data-plan registry and replaceable foundry stages.<br>`recipe/sim-sdk/music-algorithms/foundry-plan` - The Lisp load recipe reproduces the bounded foundry plan through the five focused candidate crates.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/music_algorithms` - The Rust facade specimen checks the same transform, search, ratio, consonance, counterpoint, and plan bounds. |
| `route/compose-music-carpet` | Compose and relatively encode a finite music carpet | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Music, rational time, audited conversions, and reusable transform chains; carpet addressing calls the shared discrete-rank crate rather than implementing rank locally.<br>`feature/sim-music/carpet-composition` - The carpet row owns finite axes, sparse cells, explicit layout policy, shared mixed-radix addressing, algebraic layout operations, and relative pitch/time forms.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/carpet_conformance` - The checked algebraic fixtures prove mixed-radix rank parity, explicit boundary policy, rotations, reflections, slices, overlays, and transform-chain composition.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/relative_conformance` - The checked conversion fixtures prove exact retained-origin round trips and named losses for omitted origins and source structure. |
| `route/compose-offline-realtime-audio` | Compose one timbre as deterministic offline audio and a realtime DSP preview | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The sound vocabulary owns additive partials, envelopes, filters, Karplus-Strong, and compact FM timbres.<br>`feature/sim-music/synth-performance-workbench` - The synth workbench owns oscillator, subtractive, DX7 FM, modular synth, offline-render, PCM16 WAV, and explicit Table/Dir recipe routing.<br>`recipe/sim-music/crates/sim-lib-music-synth/03-composition/offline-realtime-preview` - The recipe maps every catalog need to an existing owner, renders offline through sound-render, previews through graph/DSP, and uses non-overwriting Table/Dir handles.<br>`spec-test/sim-music/crates/sim-lib-music-synth/src/tests/reuse` - The paired conformance test decodes the recipe and checks its nine owner mappings, bounded callback policy, Table/Dir handles, and phase-43 conversion/dither deferral.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves additive, Karplus-Strong, and FM timbres produce finite deterministic offline PCM. |
| `route/compose-private-observation-pack` | Keep, review, export, or erase voluntary private observations | user, framework | `feature/sim-sdk/private-observation-pack` - Use the closed voluntary data contract and sealed lane while leaving every field absent unless Mia opts in.<br>`recipe/sim-sdk/stewardship/private-observation` - Inspect a networkless exact entry and locally encrypted export preview.<br>`spec-test/sim-sdk/tests/validate_private_observation_pack` - Prove closed operations, missingness, concurrency, sealed erasure semantics, and explicit export acknowledgement. |
| `route/compose-quiet-stewardship-cadence` | Compose a quiet cadence from minimum copied claims | user, framework | `feature/sim-sdk/quiet-stewardship-cadence` - Compose replaceable stewardship roles behind a calm, source-blind projection policy.<br>`recipe/sim-sdk/stewardship/quiet-cadence` - Replay a deterministic week containing bursts, silence, stale facts, refusals, unavailable placements, lock and revoke, paper operation, and pack removal.<br>`spec-test/sim-sdk/tests/validate_cadence_pack` - Prove daily interaction budgets, explicit finance detail, isolated source handles, and channel disclosure ceilings. |
| `route/compose-relations-from-sdk` | Compose a replaceable relational lifecycle from the SDK | user, code, framework | `feature/sim-sdk/relation-composition` - The SDK exposes each canonical owner behind one provider-neutral feature boundary.<br>`spec-test/sim-sdk/tests/relation_exports` - One admitted logical request runs unchanged through a recording site and SQLite.<br>`recipe/sim-sdk/relation/custom-domain-crud` - The recipe starts the complete custom-domain lifecycle using SDK paths only. |
| `route/compose-reversible-atelier-product` | Compose and replay an offline Atelier product | user, framework | `feature/sim-sdk/reversible-atelier-product` - Compose the delivered layers and private root-set input without adding facade or bootloader behavior.<br>`recipe/sim-sdk/atelier/reversible-product` - The deterministic pilot covers all current memos and rebuilds after deleting the derived worktable. |
| `route/compose-serial-music-from-sdk` | Compose serial music from the SDK | user, code, framework | `feature/sim-sdk/serial-music-composition` - The SDK row owns the single curated serial facade over the frozen candidates and the existing runtime-install surface for supporting shape and notation libs.<br>`recipe/sim-sdk/serial-music/row-matrix-analysis` - The theory recipe proves the facade reaches row-family and matrix analysis through public SDK names.<br>`recipe/sim-sdk/serial-music/modal-realization` - The adaptation recipe realizes one immutable plan through the registry-backed modal surface without mutating the source plan.<br>`recipe/sim-sdk/serial-music/reversible-completion` - The completion recipe proves additive serial completion is reversible through the facade.<br>`recipe/sim-sdk/serial-music/index-discovery` - The index recipe proves the generated feature and route ids are discoverable from the SDK repo surface.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/serial_music` - The public-facade conformance specimen checks reversibility, recipe parity, fail-closed behavior, and the feature claim. |
| `route/compose-staged-sequence-alignment` | Compose staged state selection with sequence alignment | code, framework, user | `feature/sim-discrete/discrete-algebra` - The discrete graph owner supplies certified layered shortest paths and DTW/edit alignment under shared work, memory, finite-cost, stable-tie, and verification policy.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/alignment/tests` - The alignment fixtures prove full and rolling certificates, windows, subsequence boundaries, work refusal, non-finite refusal, and tamper detection.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/layered` - The staged fixtures prove Bellman backpointers, forbidden transitions, bounded work, stable ties, and certificate verification.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/cookbook` - The checked composition invokes and verifies both engines and retains stable staged states plus DTW score, path length, cells, and edges without a copied domain-side DP loop.<br>`recipe/sim-discrete/crates/sim-lib-discrete-graph/02-scenarios/staged-alignment` - The public cargo-example recipe prints the same composition evidence for a human reader. |
| `route/compose-taste-led-kitchen-pack` | Prepare kitchen options and a shopping draft without choosing or buying | user, framework | `feature/sim-sdk/taste-led-kitchen-pack` - Compose the closed data contract and pure projection while Mia retains meal authority.<br>`recipe/sim-sdk/stewardship/taste-led-kitchen` - Exercise copied egg evidence, stale-stock questioning, exact unit labels, and an opaque optional cost reference with every provider lane absent.<br>`spec-test/sim-sdk/tests/validate_kitchen_pack` - Prove the Shapes, freshness lattice, source-room isolation, effect ceiling, and networkless fixture. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`feature/sim-construction/table-backed-project-books` - The persistence row injects the selected Table or Dir backend and keeps the append-only fact stream authoritative.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row joins stable controls to the existing Gantt plan and critical-path model.<br>`feature/sim-construction/mspdi-schedule-exchange` - The exchange row round-trips the portable Gantt plan across the MSPDI boundary.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row realizes modeled schedule placement while retaining the explicit live gates.<br>`feature/sim-construction/dalux-project-items` - The Dalux row realizes modeled field-item placement and preserves URL-free external and effect-ledger references.<br>`feature/sim-construction/production-lookahead-readiness` - The readiness row derives package and task readiness from accepted facts and schedule joins.<br>`feature/sim-construction/change-commercial-exposure` - The change row derives exact exposure while retaining reference-only ledger ownership.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row derives system and project burn-down from explicit evidence.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The closeout row admits only evidence-backed, disclosure-approved reference claims.<br>`feature/sim-construction/office-evidence-bridge` - The evidence bridge retains stable office references without copying construction truth or document payloads.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row projects the same filtered snapshot into existing Doc, Sheet, Deck, and suite surfaces.<br>`recipe/sim-construction/reference-project-control` - The checked primary Lisp recipe runs one entirely invented renovation twice through conflict, failure, bounded exception, correction, closeout, visibility-safe office review, and final reference admission while composing the public libraries, codecs, injected Table backend, and modeled vendor sites offline by default. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/describe-declarative-harmony` | Describe harmony palettes and rules as data | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies existing chord symbols, exact pitches and ratios, pitch sets, scale maps, voicings, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row assembles those owners into codec-neutral palettes, template chains, hard rules, soft metrics, evidence, voicing changes, and render settings.<br>`spec-test/sim-music/crates/sim-lib-pitch-chord/src/harmony_conformance` - The checked Lisp descriptors reproduce the catalog harmonizer and chord-cycle palette/filter chains without compiled policy. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/estimate-classical-spectrum` | Estimate a classical or autoregressive spectrum with reconstructable scaling | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns reference windows, periodogram/Welch/cross/coherence, Slepian multitaper, uneven-sample Lomb-Scargle, stable Burg/MEM models, explicit grids and scaling, and bounded evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/spectral_tests` - The checked tone/noise specimen proves window invariants, Parseval density, peak recovery, coherence, Slepian concentration, uneven-sample recovery, and fail-closed work limits.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/autoregressive_tests` - The generated AR-process specimen proves BIC order recovery, stable reflections, MEM resonance, bounded bidirectional prediction, and singular or unstable fail-closed behavior.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/spectral-estimators` - The runnable periodogram specimen prints coherent gain, equivalent noise bandwidth, peak power, degrees of freedom, and admitted work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/burg-mem-evidence` - The deterministic generated AR(2) recipe prints selected order, residual variance, reflection margin, MEM peak, and admitted work. |
| `route/evaluate-exact-score-consonance` | Evaluate exact score consonance | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical rational score time and identity-bearing Staff conversion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row supplies overlap- and pedal-aware realization before MIDI window evaluation.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row owns the pitch, ratio, contextual sonance, psychoacoustic, and tuning models.<br>`feature/sim-music/exact-score-consonance` - The consonance row composes those owners into multiplicity-preserving sounding windows and separate metric reports.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/exact-score-consonance` - The Lisp recipe shows an exact progression report with a retained doubling and no implicit aggregate. |
| `route/exchange-construction-schedule` | Exchange a construction schedule | user, code | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the XML codec and fidelity tests.<br>`recipe/sim-construction/crates/sim-codec-mspdi/01-basics/mspdi-round-trip` - The round-trip recipe shows a modeled Gantt schedule crossing the MSPDI boundary. |
| `route/exchange-score-musicxml` | Import and export a bounded MusicXML score | user, code, framework | `feature/sim-music/midi-notation-workflows` - The notation row owns the bounded MusicXML profile, stable exchange ids, loss reports, and one runtime import callable.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact score row supplies canonical rational Score semantics and the existing SIM expression/read-construct form.<br>`recipe/sim-music/crates/sim-lib-music-notation/01-basics/musicxml-import` - The notation recipe shows the bounded runtime call and profile limits.<br>`spec-test/sim-music/crates/sim-lib-music-notation/src/tests` - The checked fixture proves DTD rejection, bounds, ids, losses, and LilyPond/MusicXML/SIM semantic round trips. |
| `route/execute-stewardship-memo-acceptance` | Turn stewardship memo promises into checked synthetic evidence | user, framework | `feature/sim-sdk/stewardship-memo-acceptance` - Classify every promise before claiming that the composed product satisfies it.<br>`recipe/sim-sdk/stewardship/memo-acceptance` - Inspect the synthetic month, week, season, flock, dialogue, crew, and lifecycle trace plus every explicit unsupported and manual state.<br>`spec-test/sim-sdk/tests/memo_acceptance` - Prove matrix completeness, exact source arithmetic, human authority, prepare-only crew behavior, privacy hygiene, and bounded managed-erasure claims. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-foundation/index-vault-projection` - The projection row consumes the canonical inventory and proves every row has exactly one primary claim.<br>`feature/sim-codecs/index-vault-codec` - The codec row owns document dialect composition, v2 bundles, v1 reading, and semantic verification.<br>`feature/sim-index/vault-export` - The vault export row owns canonical loading, artifacts, reports, migration, and managed command behavior.<br>`spec-test/sim-codecs/crates/sim-codec-index-vault/tests/conformance` - The codec specimen proves all-profile round trips, semantic corruption rejection, and bounded v1 migration.<br>`spec-test/sim-tooling/src/index_vault_write_tests` - The checked materialization specimen proves managed writes preserve sibling notes and application configuration. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-numeric-root` | Find a scalar or vector root with reviewable evidence | code, framework, user | `feature/sim-numbers/root-finding` - The root owner distinguishes proved sign-changing brackets from residual-only estimates and records derivative, rank, work, termination, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-root/src/tests` - The checked tests cover endpoint and multiple roots, invalid and discontinuous brackets, flat derivatives, scaling, singular Jacobians, bounded Broyden resets, and deterministic replay.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/01-basics/scalar-root` - The scalar recipe demonstrates bracket-preserving Brent-Dekker search.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/02-vector/vector-root` - The vector recipe demonstrates rank-diagnosed damped Newton search. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/fit-point-clusters` | Fit deterministic point clusters with convergence and model-selection evidence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns seeded k-means++ restarts, empty-cluster repair, regularized full/diagonal GMM EM, log-domain likelihood, singular-component policy, and AIC/BIC evidence while leaving temporal alignment to sim-discrete.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/clustering_tests` - Generated, degenerate, permuted, bounded, and non-finite fixtures prove deterministic centroids, log-domain likelihood, covariance regularization, convergence, repairs, and fail-closed control.<br>`recipe/sim-numbers/crates/sim-lib-numbers-stats/01-basics/clustering-evidence` - The runnable generated-cluster recipe prints selected restart, centroids, likelihood, repairs, AIC, BIC, and charged work and points alignment callers to the discrete graph owner. |
| `route/generate-counterpoint` | Generate bounded reversible counterpoint | user, code, framework | `feature/sim-music/exact-score-consonance` - The consonance row owns the content-bound additive patch and exact inverse that preserve the fixed cantus staff.<br>`feature/sim-music/music-counterpoint` - The counterpoint row compiles rule data to finite CSP variables/domains, delegates traversal and receipts to discrete-search, propagates musical constraints, selects diverse legal results, and rechecks each result through the existing analyzer.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/bounded-generation` - The checked Lisp specimen generates two second-species voices under explicit work, result, frontier-default, and seed evidence. |
| `route/grow-bounded-music-catalog` | Grow and inspect a bounded recursive music catalog | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row supplies first-class partial pitch maps and scale-aware music transformations.<br>`feature/sim-music/bounded-rewrite-catalogs` - The rewrite row owns typed musical alphabets, productive context rules, scale-following state data, finite progression-tree topology, and derivation evidence while delegating ordinals and traversal to shared discrete rank/search.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/rewrite_conformance` - The checked conformance module proves typed contextual derivation, preflight rejection, rank round trips, bounded filtered search, scale-map materialization, and stable receipts. |
| `route/harmonize-melody` | Harmonize a melody under explicit rules and bounds | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies melody pitch sets, chord templates, scales, registers, voice-leading, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row supplies the palette plus separate hard legality and weighted score lanes.<br>`feature/sim-music/bounded-harmonization` - The harmonization row adapts the shared musical problem to exhaustive, factored, certified layered-DP, and beam strategies.<br>`recipe/sim-music/crates/sim-lib-music-combinators/01-basics/bounded-harmonization` - The Lisp recipe shows the public layered-DP call with explicit work, result, and seed bounds. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/infer-hidden-sequence` | Summarize a stream or infer a finite hidden-state sequence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns hard-bounded mergeable quantiles, shared finite transitions, normalized discrete/Gaussian HMM inference, and seeded bounded fitting evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/quantile_tests` - The checked monotone-stream specimen proves exact small data, bounded retained entries, rank accuracy, merge behavior, and transactional refusal.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/hmm_tests` - The enumerated tiny fixture proves likelihood, posterior, and Viterbi agreement; long, continuous, seeded-fit, and work-limit fixtures prove numerical and termination behavior. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/inspect-jvm-performance-evidence` | Inspect JVM prepared-execution performance evidence | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile publishes prepared-code, verified micro-op, and generated fusion coverage without owning benchmark statistics.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/performance-evidence` - The recipe maps every fidelity and performance claim to generated coverage and canonical benchmark harness raw samples.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/published_performance` - The checked publication fixture keeps raw report identities, summaries, generated coverage, and ownership guards connected. |
| `route/inspect-platform-command` | Inspect and verify the selected platform through the loadable command | user, code, framework | `feature/sim-run/platform-command` - The bootloader dispatches the fixed platform vocabulary through the loaded product library.<br>`spec-test/sim-run/crates/sim-run/src/platform` - The command specimen proves fixed dispatch, machine output, and typed refusal behavior. |
| `route/interpolate-periodic-spectrum` | Interpolate or analyze a periodic signal under explicit Fourier conventions | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns continuous DFT-series evaluation and integration, single-bin evaluation, Hilbert construction, unwrapped phase, instantaneous frequency, and envelope following under one explicit convention.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/interpolation_tests` - The generated analytic periodic-function specimen proves off-grid interpolation, exact integration, endpoint refusal, wrapping, and single-bin parity.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/analytic_tests` - The generated tone specimen proves Hilbert quadrature under every normalization, phase unwrapping, interval-centered frequency, and finite attack/release envelopes.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/dft-interpolation` - The Lisp recipe retains unitary scaling, period, origin, endpoint, wrapping, sign, Nyquist, phase, and work evidence. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/onset-beat-harmony` - The checked Lisp analysis call selects onsets, beats, MFCC, chroma, key, and chords under explicit varying-tempo, posterior/HMM, work, result, and seed policy; its generated click/chord fixture proves timing and labels.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/link-jvm-lambda` | Link and invoke a JVM lambda or method reference | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile owns bootstrap admission, direct-handle resolution, loader-local generated metadata, and Java descriptor adaptation.<br>`feature/sim-runtime/function-organ` - The shared function organ owns the neutral Callable projection and collector-visible captures used at the SIM boundary.<br>`feature/sim-runtime/class-semantics` - The shared class organ owns language-neutral class structure; defining-loader identity remains JVM policy.<br>`feature/sim-runtime/mutation-organ` - The managed graph owns lambda instances, captures, weak identities, and collection rather than a JVM-private graph or global cache.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/verifier_scope` - The verifier consumes the executor-owned admitted bootstrap registry and seals exact whole-class proof before preparation.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/java-lambda` - The published Java-to-SIM recipe links compiler-produced bytes and invokes the result through ordinary Callable.<br>`recipe/sim-sdk/jvm/lambda-interop` - The SDK recipe reaches both interop directions through public facade exports only. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/model-lumped-boundary-events` | Model a lumped dynamic system with explicit boundaries and events | code, framework, user | `feature/sim-physics/boundary-event-core` - The core owns solve-independent topology and signed-power boundary validation.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - The specimen proves canonical identity, simultaneous events, and fail-closed topology. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/open-web-workspace` | Open a web workspace | user | `feature/sim-web/web-shell-host` - The web shell row owns the serve, browse, and atelier commands.<br>`feature/sim-web/view-surface` - The view row supplies the reversible surfaces shown by the browser host.<br>`recipe/sim-web/crates/sim-web-shell/01-basics/open-workspace` - The open-workspace recipe is the shortest browser-host example. |
| `route/operate-continuity-phone-surface` | Operate a complete continuity session from a phone surface | user, code, framework | `feature/sim-web/continuity-phone-surface` - The phone codec projects journal truth and compiles only bounded semantic actions.<br>`spec-test/sim-web/crates/sim-lib-view-continuity-phone/src/tests` - The specimen proves silent interaction, stale-safe replay, lifecycle refusal, and an always-reachable stop. |
| `route/operate-estate-from-terminal` | Operate the estate from a terminal | user, code | `feature/sim-run/estate-command` - Boot the loadable command and emit the same typed calls used by other surfaces. |
| `route/operate-model-test-observatory` | Operate the model-test observatory through its loadable command | user, code, framework | `feature/sim-agent-net/model-test-product` - The product owns the complete command vocabulary while delegating lifecycle and provider authority to their existing owners.<br>`spec-test/sim-agent-net/crates/sim-lib-model-test/src/tests` - The product specimen proves fixed dispatch, bounded machine output, and provider-contact boundaries. |
| `route/operate-provider-organ` | Discover, validate, and fan out across provider seats | user, code, framework | `feature/sim-agent-net/provider-control` - The provider organ owns open family adapters, private inventory, explicit seats, shared conformance, and ordered fan-out.<br>`feature/sim-agent-net/model-runner-framework` - Every opened direct API, CLI broker, local-service, or extension seat executes through ModelRunner.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE owns the checked request and output contract shared by every family.<br>`recipe/sim-agent-net/crates/sim-lib-provider/01-basics/open-seat` - The checked recipe opens a provider seat through the same registry and card contract used by shipped and third-party families.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The unchanged checked GENAI recipe remains the canonical request source. |
| `route/operate-relational-data` | Discover inspect migrate query mutate or mount a database relation schema SQLite site or SQL projection | user, code, framework | `feature/sim-foundation/relation-core` - The provider-neutral logical relation framework owns rows, domains, and identities.<br>`feature/sim-shape/relation-shape` - Shape admits relation values and command artifacts.<br>`feature/sim-codecs/sql-projection` - The SQL codec is a projection of checked plans, never a command escape hatch.<br>`feature/sim-foundation/relation-migrate` - Migration admission and drift checks precede physical action.<br>`feature/sim-storage/bounded-relation-site` - The site contract gates provider effects and bounds.<br>`feature/sim-storage/sqlite-relation-locator` - SQLite is selected through a closed provider identity and opaque locator.<br>`feature/sim-storage/relation-table-dir-projection` - Results render through standard Table/Dir composition.<br>`feature/sim-office/office-site-workflows` - Office workflows reuse relational sites rather than copying storage behavior.<br>`feature/sim-ledger/ledger-libraries` - Ledger composition supplies durable domain reuse.<br>`feature/sim-sdk/relation-composition` - The SDK facade exposes the supported relation composition.<br>`feature/sim-storage/relation-command` - The loadable command parses only named, authorized, bounded operations.<br>`spec-test/sim-storage/crates/sim-lib-relation-cli/src/tests` - Checked specimens cover creation, inspection, adoption, drift refusal, query, mutation, providers, and mounts. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/plan-serial-practice` | Freeze and realize a serial plan with immutable provenance and chord-safe partial order | user, code, framework | `feature/sim-music/serial-series-calculus` - The generic serial layer owns stable finite ordered-symbol identity and aggregate validation without assuming pitch or score order.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict row values and operation-bearing RowForm provenance for each row instance.<br>`feature/sim-music/serial-practice` - The music-serial layer freezes row instances, stable event ids, ordinal references, role/origin provenance, explicit parents, simultaneous groups, and precedence into one immutable validated plan, then realizes strict statements with explicit register, duration, dynamics, rests, ties, and canonical music-core rendering.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/immutable-plan` - The checked Rust scenario proves that one row can span multiple voices while equal-onset material stays grouped without a fabricated chord order.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/strict-row-realization` - The checked Rust scenario realizes a strict row statement with a chord, rest, tie, and equal-pitch simultaneous notes while preserving plan, event, voice, and ordinal identity through the piano roll. |
| `route/prepare-balanced-ledger` | Prepare a balanced ledger | user, code | `feature/sim-ledger/ledger-command` - The command row owns the operational entry point.<br>`recipe/sim-ledger/crates/sim-ledger/01-basics/balanced-year` - The balanced-year recipe shows the expected yearly voucher shape. |
| `route/preserve-physics-finding-history` | Preserve and investigate a surprising physics result without erasing competing explanations | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Produce reviewable sweep evidence and stable study identity.<br>`feature/sim-physics/immutable-finding-history` - Publish the observation, append evidence-bearing successors, retain branches, and rebuild read-only projections from immutable records. Its crate integration suite proves identity, backend parity, interrupted and concurrent append, corruption refusal, branch reconciliation, rebuild, complete history, and redaction.<br>`recipe/sim-physics/06-findings/sweep-to-finding` - Convert one sweep residual into an open finding. |
| `route/process-sampled-signal` | Transform, smooth, solve, or interpolate sampled data | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns wavelet boundaries and inverse shapes, canonical Savitzky-Golay derivative scaling, stable Toeplitz diagnostics, and duplicate/extrapolation interpolation policy.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/wavelet_tests` - The checked wavelet specimen proves multilevel odd-length inverses under periodic, symmetric, and zero extension plus the Le Gall linear-polynomial law.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/smoothing_tests` - The checked smoothing specimen proves polynomial preservation, factorial and sample-spacing derivative scaling, Toeplitz residuals, and singular-pivot diagnostics.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/sample_interpolation_tests` - The checked interpolation specimen proves affine preservation, monotonicity, duplicate resolution, extrapolation policy, and fail-closed input.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/wavelet-smoothing` - The runnable recipe composes all four generic algorithms with their visible reports. |
| `route/project-reversible-expedition` | Project a reversible expedition product | user, code, framework | `feature/sim-web/reversible-worktable` - Replay owner-authored semantic edits into the disposable worktable without moving domain behavior into the surface.<br>`spec-test/sim-web/crates/sim-lib-view-worktable/src/lib` - The dependency-free specimen proves replay, undo, visible arming, manual continuation, and insufficient-evidence behavior. |
| `route/prove-benchmark-workload-neutrality` | Prove the benchmark harness is workload neutral | user, code, framework | `feature/sim-tooling/benchmark-cli` - The benchmark CLI composes sampling, process isolation, robust comparison, and durable reports without a domain-specific workload contract.<br>`spec-test/sim-tooling/src/bench/neutral_cpu_conformance` - The checked CPU-bound specimen proves deterministic sampling, matched comparison, host refusal, retained failure, and Table/Dir browsing without bytecode.<br>`spec-test/sim-tooling/src/bench/neutral_allocation_conformance` - The checked allocation-bound specimen proves local fingerprinting, high-dispersion refusal, retained failure, and durable report browsing without bytecode. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/realize-serial-spine` | Adapt an immutable serial plan onto a modal or caller-defined spine without hiding aggregate loss | user, code, framework | `feature/sim-music/serial-practice` - The serial owner keeps the immutable plan, stable event identities, row-form provenance, explicit strict specs, and strict chromatic baseline intact before any adaptation occurs.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary row owns named scales, caller-defined player scales, and contextual sonance models reused by the adaptation report.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns the first-class partial pitch-map policy that modal landing reuses instead of inventing another remap surface.<br>`feature/sim-music/open-serial-adaptation` - The open adaptation row owns landed modal realization, aggregate-relaxation receipts, and the independent spine, pitch, ordinal, and sonance report facets.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/third-party-adaptation` - The checked external-style scenario registers a seven-symbol non-pitch series, a custom practice rule, and a custom realizer through public APIs only, then proves the Lisp validation hook and realizer registry both fail closed on unknown components.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/modal-spine` - The checked Rust scenario realizes the same immutable plan through C Dorian, C Lydian, and a caller-defined scale, proves plan equality with differing sounding pitches, and inspects the spine report without claiming strict twelve-tone output. |
| `route/refuse-energy-influenced-selection` | Prove that a physical-study selection input is independent of energy | code, framework, user | `feature/sim-physics/no-energy-selection` - Apply the fail-closed influence domain over the canonical incremental engine, protected sinks, native type-state, and runtime Shape preparation; its crate integration suite proves direct and laundered influence, branches, joins, loops, opaque code, clean preparation, budget refusal, and incremental edits.<br>`recipe/sim-physics/04-influence/refused-selection` - Run a divide-by-time laundering refusal and inspect its causal path. |
| `route/repair-output-contract` | Repair an output contract | user, code | `feature/sim-agent-net/output-contract-repair-cli` - The repair command row owns the operational correction path.<br>`recipe/sim-agent-net/crates/sim-lib-agent-runner-core/01-basics/output-contract-repair` - The repair recipe provides the worked contract fixture. |
| `route/replay-content-addressed-fleet` | Replay a content-addressed fleet | user, code | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns the stream-fabric placement library.<br>`feature/sim-agent-net/fabric-fixture-cli` - The fixture command row exposes replay from the CLI surface.<br>`recipe/sim-agent-net/crates/sim-lib-stream-fabric/01-basics/content-addressed-fleet` - The content-addressed fleet recipe is the replay example. |
| `route/reproducible-federated-research` | Run reproducible federated research | user, code, framework | `feature/sim-agent-net/reproducible-federated-research` - Freeze intent, authority, sites, concurrency, budgets, and revisions before provider dispatch.<br>`feature/sim-agent-net/provider-neutral-http-search` - Collect provider claims and typed partial failures through provider-neutral Retriever cards.<br>`feature/sim-agent-net/reproducible-federated-research` - Cluster only evidenced aliases, fuse via shared RRF, capture selected pages under independent policy, verify selectors, and retain replay receipts.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/fixture-research` - The all-fixture recipe demonstrates intent through claims, capture, selectors, and replay without network access. |
| `route/research-web-through-cli-or-mcp` | Research the web through CLI or MCP | user, code, framework | `feature/sim-agent-net/search-product` - One product owns canonical configuration, execution, records, and stable ids.<br>`feature/sim-agent-net/mcp-server` - The existing Skill projection derives MCP schemas, capability checks, execution, and audit.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/cli-mcp-parity` - The checked fixture demonstrates identical CLI and MCP behavior without ambient network access. |
| `route/review-construction-project-chief-weekly-pack` | Review a construction project-chief weekly pack | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns authoritative append-only facts, accepted baselines, current and historical snapshots, deltas, evidence states, and stable control ids.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row filters facts before derivation, applies safety-first cadence ordering, preserves provenance and explained status, produces existing Doc, Sheet, and Deck values, and delegates presentation to the installed office suite surface.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/project-chief-weekly-pack` - The checked recipe shows the exact weekly provenance header, priority order, changed-since controls, non-green mandatory status, existing office values, and suite presentation. |
| `route/review-estate-on-surface` | Review and operate an estate plan through a device Surface | user | `feature/sim-web/estate-surface` - Render every authority field and compile only a current content-bound plan operation. |
| `route/review-office-document` | Review an office document | user, code | `feature/sim-office/document-codecs` - The codec row chooses the file format and round-trip path.<br>`feature/sim-office/document-surfaces` - The surface row renders the document into a reviewable view.<br>`feature/sim-office/office-site-workflows` - The site row connects the document to modeled workplace sources.<br>`recipe/sim-office/crates/sim-lib-doc-surface/01-basics/suite-surface-descriptor` - The suite-surface recipe is the shortest surface example. |
| `route/run-authorized-jvm` | Run authorized JVM classfile bytes | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The runtime row owns bounded JVM semantics while reusing the shared classfile decoder, machine, managed graph, exceptions, and source authority.<br>`feature/sim-runtime/machine-contract` - The shared machine owns slots, frames, iterative drive, and safepoint publication.<br>`feature/sim-runtime/gc-tracing` - The managed and tracing organs own object identity, graph edges, roots, and bounded collection.<br>`feature/sim-runtime/class-semantics` - The class organ owns language-neutral declared-class structure while JVM loader identity remains JVM policy.<br>`feature/sim-runtime/capabilities-read-eval` - The source-authority organ keeps class loading explicit and diminished rather than ambient.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/product_specimen` - The public specimen executes static, managed-object, array, guest-exception, and exact-concat cases.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/ownership_guard` - Policy fixtures reject every prohibited ownership fork and scan the production JVM sources.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/baseline` - Generated opcode and intrinsic coverage totals differ from their manifests by zero. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/provider-control` - Provider families and seats are one open organ with shared conformance and fan-out.<br>`feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-generic-study` | Run and decide a deterministic evidence-backed study | user, code, framework | `feature/sim-runtime/domain-free-study-lifecycle` - The study product owns the one sealed lifecycle, staged design, report graph, selection, and effect boundary.<br>`spec-test/sim-runtime/crates/sim-lib-study/src/design` - The checked deterministic fixture covers smoke, screening, fixed confirmation, budget refusal, and selection evidence. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-jvm-command` | Run the JVM product specimen | user, code | `feature/sim-run/bootloader` - The standard bootloader owns the runtime session and verb handoff.<br>`feature/sim-run/jvm-command` - The JVM command contributes only host factories, capabilities, and the loadable CLI adapter.<br>`recipe/sim-run/02-scenarios/jvm-product` - The runnable recipe enters through the published sim command.<br>`spec-test/sim-run/crates/sim-run/tests/jvm_product` - The published sim binary loads authorized bytes and reports static, object, array, exception, and concat outcomes. |
| `route/run-music-algorithm-foundry` | Assemble a replaceable end-to-end music algorithm application from plan data | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns bounded SMF decoding, exact realization policy, and canonical export.<br>`feature/sim-music/audio-lift-and-render` - The audio row owns the optional audio-input analysis adapter and finite offline rendering inputs.<br>`feature/sim-music/algorithm-foundry` - The foundry row owns data-plan validation, open stage registration, Shape ranking, result checks, trace, Table/Dir boundaries, and realize-target preview delegation.<br>`feature/sim-music/bounded-harmonization` - The harmony row supplies the selected layered-DP and alternate exhaustive implementations under the same rules and bounds.<br>`feature/sim-music/music-counterpoint` - The counterpoint row supplies inspectable species rules, legal transforms, and bounded generation/analysis.<br>`recipe/sim-music/music-algorithm-foundry` - The runnable fixture checks every intermediate summary, exact SMF/WAV frame digests, data-only alternate selection, and the missing optional-stage diagnostic. |
| `route/run-placement-transparent-physics-study` | Run a reviewable physics parameter sweep without coupling it to execution placement | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Bind the immutable plan, generate canonical coverage evidence, preserve statistical partitions, and submit independent points through EvalFabric; Tensor-backed models retain their ordinary TensorSite placement beneath that fabric boundary. The crate integration suite proves boundaries, partitions, sampler replay, placement parity, partial failure, interruption, deterministic replay, provider envelopes, and explicit gaps.<br>`feature/sim-physics/no-energy-selection` - Produce the clean influence proof required by the selection API while leaving energy outside the choice.<br>`recipe/sim-physics/05-study/placement-sweep` - Inspect a small inclusive sweep with an exact injected case and an explicit untested region. |
| `route/run-relation-command` | Run the standard relation command | user, code | `feature/sim-run/bootloader` - The bootloader selects loaded behavior from the relation product verb.<br>`feature/sim-storage/relation-command` - The storage library owns admitted relation operations and output.<br>`feature/sim-run/relation-command` - The standard distribution registers the library and declared SQLite site identity.<br>`spec-test/sim-run/crates/sim-run/src/relation` - The handoff specimen proves verb selection and standard site discovery. |
| `route/run-search-command` | Run canonical search from the terminal | user, code | `feature/sim-run/bootloader` - The standard bootloader owns command dispatch.<br>`feature/sim-run/search-command` - The command row selects the shared search product without implementing search.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/cli-mcp-parity` - The shared specimen proves the same record through CLI and MCP. |
| `route/run-serial-music-workbench` | Run a serial music workbench | user, code, framework | `feature/sim-music/twelve-tone-row-theory` - The pitch-serial row owns the strict source row, family/matrix inspection, and partition evidence that the workbench freezes before realization.<br>`feature/sim-music/serial-practice` - The serial practice row owns the immutable plan, strict realization context, simultaneous grouping, referential evidence, counter-voice techniques, arrays/time points, and integral parameter surfaces that the workbench assembles into one inspectable source.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row owns the registry-selected modal realizer and the spine report proving aggregate relaxation explicitly rather than silently mutating the source plan.<br>`feature/sim-music/serial-consonance-completion` - The completion row owns the reversible additive patch, structural-versus-all-sounding ledger audit, and exact inverse proof after completion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI and notation row owns SMF export, LilyPond projection, and the exact score lowering surface used for audition.<br>`feature/sim-music/serial-music-workbench` - The workbench row composes those replaceable owners into one executable recipe with fixture-manifest coverage and a hard no-copyrighted-score rule.<br>`recipe/sim-music/serial-workbench/01-end-to-end/row-to-audition` - The checked runnable recipe validates the fixture manifest, runs strict plus modal realization, completes and undoes one additive patch, audits the ledgers, exports MIDI plus LilyPond notation, and lowers the audition score end to end. |
| `route/run-study-command` | Run and inspect a generic study from the terminal | user, code | `feature/sim-run/bootloader` - The bootloader selects the study verb and loads its product library without acquiring study policy.<br>`feature/sim-runtime/domain-free-study-lifecycle` - The runtime library owns sealed coordinates, execution, evidence, reports, and decisions.<br>`feature/sim-run/study-command` - The command row wires the standard distribution to the loadable study surface.<br>`spec-test/sim-run/crates/sim-run/src/study` - The checked handoff specimen covers verb selection and loadable-library dispatch. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/sdk-search-research-audit` | Run sim search research through deterministic conformance to the audit view | user, code, framework | `feature/sim-run/search-command` - The bootloader selects the canonical search product.<br>`feature/sim-sdk/web-search-facade` - The SDK composes the stable public contracts and checked fake-world fixture.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/web_search_offline` - The fixture proves network-free query-to-citation replay and attack boundaries.<br>`feature/sim-web/search-audit-surface` - The inert view exposes ranking, capture, selector, office, and policy evidence. |
| `route/serve-over-mcp` | Serve over MCP | user, code | `feature/sim-agent-net/mcp-server` - The MCP row owns the command surface and loaded server libraries.<br>`recipe/sim-agent-net/crates/sim-mcp-server/01-basics/final-stack` - The final product recipe routes stdio, authenticated HTTP, compatibility, protected state, subscriptions, cancellation, shared Table, and extensions through one composition. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/sim-numbers/bounded-numerical-method` | Plan and inspect a bounded numerical method | framework, user | `feature/sim-numbers/method-evidence` - Reuse criterion-specific plans and common evidence while retaining domain certificates.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-method/src/tests` - The checked tests prove bounded plans, termination evidence, and domain-adapter separation.<br>`recipe/sim-numbers/crates/sim-lib-numbers-method/01-basics/method-evidence` - The runnable recipe exposes common method evidence beside its domain result. |
| `route/sim-numbers/coefficient-polynomial` | Compute with coefficient polynomials and evidence | framework, user | `feature/sim-numbers/coefficient-polynomial` - Choose the honest exponent domain, normalize coefficients, and retain exact or numerical evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-poly/src/tests` - The checked tests prove canonical forms, exact identities, guarded approximation, and root evidence.<br>`recipe/sim-numbers/crates/sim-lib-numbers-poly/01-basics/certified-evaluation` - The runnable recipe demonstrates certified coefficient-polynomial evaluation. |
| `route/sim-numbers/extended-precision-convergence` | Rerun a numerical kernel at extended precision | framework, user | `feature/sim-numbers/extended-precision-convergence` - Keep the algorithm fixed and select DoubleDouble at the RealScalar boundary.<br>`feature/sim-numbers/method-evidence` - Retain common convergence and bounded-work evidence beside the domain result.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-extended/src/tests` - The checked tests compare extended precision against exact rational oracles and binary64 behavior.<br>`recipe/sim-numbers/crates/sim-lib-numbers-extended/01-basics/convergence` - The runnable convergence recipe demonstrates the precision-domain substitution. |
| `route/sim-numbers/sealed-interval-certification` | Certify an enclosure and make a definite decision | framework, user | `feature/sim-numbers/sealed-interval-certification` - Start with exact inputs or an existing certificate and pass only reviewed certified intervals to definite classification.<br>`feature/sim-numbers/method-evidence` - Inspect the reviewed kernel and bounded method evidence without promoting an estimate into a certificate.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-interval/src/tests` - The checked tests prove sealed construction paths and refusal of forged or insufficient evidence.<br>`recipe/sim-numbers/crates/sim-lib-numbers-interval/01-basics/sealed-threshold` - The runnable threshold recipe demonstrates a definite decision from certified bounds. |
| `route/sim-numbers/semantic-quantity` | Represent and combine a physical quantity | framework, user | `feature/sim-numbers/semantic-quantities` - The quantity owner composes generic scalars, exact dimensions, open semantic kinds, affine units, roles, Shape, and read construction.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-quantity/src/tests` - Checked laws prove kind mismatch refusal, affine behavior, exact conversion, rational powers, bounded construction, and semantic data closure.<br>`recipe/sim-numbers/crates/sim-lib-numbers-quantity/01-basics/energy-and-torque` - The runtime recipe demonstrates the required equal-dimension, different-kind distinction. |
| `route/sim-numbers/special-functions-and-ranked-grid` | Compute special functions, distribution tails, or a ranked grid | framework, user | `feature/sim-numbers/special-functions-and-ranked-grid` - Select the single numerical owner and declare the tail or tensor policy before inspecting algorithm and bounded-work evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-special/src/tests` - The checked special-function tests prove numerical-region selection and stable probability complements.<br>`recipe/sim-numbers/crates/sim-lib-numbers-special/01-basics/gamma-tail` - The runnable gamma-tail recipe exposes the selected algorithm and retained evidence. |
| `route/sim-numbers/tensor-decomposition` | Factor a dense tensor with reviewable evidence | framework, user | `feature/sim-numbers/tensor-decomposition` - Choose QR or symmetric eigen and set explicit admission and accuracy policy before inspecting the independent certificate.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-decomp/src/tests` - The checked tests prove decomposition identities, residual evidence, rank policy, and fail-closed admission.<br>`recipe/sim-numbers/crates/sim-lib-numbers-tensor-decomp/01-basics/qr-eigen` - The runnable recipe demonstrates QR and eigen evidence on a dense tensor. |
| `route/sim-run/hotload-generation-lifecycle` | Operate and explain one hot generation | user, code, framework | `feature/sim-run/loadable-hot-generation` - Use the five loadable operations, separate capabilities, Shapes, Cards, and typed records.<br>`recipe/sim-run/02-scenarios/hotload-generation-lifecycle` - Verify the public lifecycle contract offline. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/solve-stiff-ode-or-index-one-dae` | Solve a stiff ODE or declared index-1 DAE | code, framework, user | `feature/sim-numbers/numeric-pipelines` - The common numeric problem, plan, trajectory, event, and registry contracts define the composition boundary.<br>`feature/sim-numbers/implicit-radau-dae` - The implicit owner supplies Radau collocation, bounded Newton, Jacobian policies, dense output, and DAE admission.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-implicit/src/tests` - The checked specimen proves order, stiff decay, scale separation, constraint retention, singular refusal, events, and work separation. |
| `route/start-a-compute-command` | Start a compute command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/compute` - The compute row owns dispatch into the loadable compute CLI and headless provider libraries.<br>`spec-test/sim-run/crates/sim-run/tests/compute` - The checked command test exercises modeled and automatic compute boot. |
| `route/start-a-glasses-plan` | Start a glasses plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/glasses` - The glasses row owns the glasses command surface and loaded glasses libraries.<br>`spec-test/sim-run/crates/sim-run/tests/glasses` - The checked command test exercises the glasses entry point. |
| `route/start-a-sim-session` | Start an interactive SIM session | user | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/repl` - The REPL is the interactive entry point.<br>`recipe/sim-run/01-basics/version` - The first recipe confirms the command path. |
| `route/start-a-watch-plan` | Start a watch plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/watch` - The watch row owns the watch command surface and loaded watch libraries.<br>`spec-test/sim-run/crates/sim-run/tests/watch` - The checked command test exercises the watch entry point. |
| `route/start-an-expression-tree-command` | Start an expression tree command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the standard command runtime.<br>`feature/sim-run/expression-tree-command` - The command row selects expr-tree and delegates the complete host composition to sim-lib-expr-tree-serve.<br>`spec-test/sim-run/crates/sim-run/tests/expr_tree` - The checked command specimen exercises the standard-distribution entry point. |
| `route/start-from-sdk` | Start from the SDK | user, code | `feature/sim-sdk/facade-runtime` - The facade row owns the public command and reversible view surface.<br>`feature/sim-sdk/device-recipes` - The device row gives wearable workflows through facade-level recipes.<br>`recipe/sim-sdk/continuity/expedition-thought` - The networkless walk carries a synthetic thought by immutable content identity through route loss and process death.<br>`recipe/sim-sdk/continuity/quiet-stewardship-cards` - A second synthetic policy uses identical continuity mechanics while bounding and filtering low-disclosure cards.<br>`recipe/sim-sdk/01-basics/boot-runtime` - The boot-runtime recipe checks the smallest facade path. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/trace-parameter-boundary` | Trace a parameter boundary through folds with bounded restartable evidence | code, framework, user | `feature/sim-numbers/pseudo-arclength-continuation` - The continuation owner validates seeds, predicts and corrects pseudo-arclength points, adapts bounded steps, and records distinct fold, rank, loop, and domain events.<br>`feature/sim-numbers/root-finding` - The bordered Newton solve exposes shared residual, rank, damping, work, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-continuation/src/tests` - Checked fixtures prove circle, fold/cusp, singular correction, reversed seeds, domain exit, resumption, and bounded work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-continuation/01-basics/limiting-boundary` - The runtime recipe discovers the limiting-boundary continuation schema. |
| `route/transform-and-normalize-audio` | Stretch, pitch, measure, normalize, and lower offline PCM | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio-lift row supplies bounded phase-preserving STFT frames and COLA evidence while delegating FFT conventions and phase unwrapping to numbers-signal.<br>`feature/sim-music/offline-audio-transform-and-loudness` - The offline transform row owns phase-vocoder policy, standards-named loudness/true-peak reports, visible normalization gain, and bounded PCM16 conversion.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves duration/pitch invariants, transient resets, EBU reference loudness, intersample true peak, and visible normalization gain.<br>`spec-test/sim-music/crates/sim-lib-stream-file/src/tests` - The interchange specimen proves bounded channel mapping, seeded dither determinism, clipping reports, and routing through canonical WAV encoding. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns loss-audited score conversion, identity-bearing progression algebra, certified voice-leading paths, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies deterministic chord voicing palettes plus the pitch, scale, and tuning vocabulary used by exact transforms.<br>`spec-test/sim-music/crates/sim-lib-music-transform/src/tests/exact` - The checked exact-transform fixture compares greedy movement with a certified optimum over exact score identities. |
| `route/transform-musical-series` | Validate a finite musical series before applying total symbolic transforms | user, code, framework | `feature/sim-music/serial-series-calculus` - The serial row owns stable finite alphabet identity, symbol-bearing order, aggregate-rule data, typed failures, validation ledgers, validated ordinal and symbol bijections, total transform composition, algebra certificates, the Shape adapter, and the Lisp validation call while delegating generic permutation rank to sim-discrete.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/custom-alphabet` - The checked Lisp recipe validates and ranks an exhaustive five-symbol gesture alphabet, proving that the reusable calculus has no pitch dependency.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/certified-transforms` - The checked Rust recipe composes retrograde and rotation, applies the returned inverse, and proves exact source recovery with aggregate-preservation evidence. |
| `route/transform-signal` | Transform a real or complex signal | code, framework, user | `feature/sim-numbers/tensors` - The tensor row owns canonical complex and f64 storage shared by signal inputs and outputs.<br>`feature/sim-numbers/signal-transforms` - The signal row owns explicit transform conventions, direct references, fast Fourier plans, typed convolution/correlation geometry, guarded spectral inverse, strided tensor axes, bounded blocks, and execution reports.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/conformance` - The checked one-dimensional specimen proves impulse and tone bins, orthonormal Parseval and round trips, prime-length coverage, and fail-closed inputs.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/multidimensional_tests` - The checked multidimensional specimen proves strided direct-definition agreement, scratch admission, stable plan evidence, and blocked/in-memory equality.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/convolution_tests` - The checked convolution specimen proves direct/FFT agreement, overlap-add/save equality and reports, correlation symmetry, identity/commutativity, and finite guarded deconvolution diagnostics.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/convolution-evidence` - The checked Lisp specimen exposes automatic convolution cost selection beside the retained result. |
| `route/web-capture-to-office-evidence` | Trace an office citation to its raw web capture and policy receipt | user, code, framework | `feature/sim-office/web-evidence` - The evidence adapter binds the citation to checked capture and representation ids while retaining the policy receipt.<br>`recipe/sim-office/crates/sim-lib-doc-web/01-basics/capture-to-citation` - The offline recipe shows the capture, representation, anchor, reload, and citation path without network access.<br>`spec-test/sim-office/crates/sim-lib-doc-web/src/tests` - The checked tests prove restart stability and fail-closed behavior for tampered capture, representation, selector, index, and codec metadata. |

## Code routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `local/sim-run/route/add-table-dir-backend` | Add a Table/Dir backend | code, framework | `feature/sim-run/index-table-dir` - The IndexDir implementation shows a runtime table exposed as an immutable directory.<br>`recipe/sim-run/02-scenarios/index-examples` - The examples recipe exercises table-backed feature-to-specimen lookup. |
| `local/sim-storage/route/add-table-dir-backend` | Add a Table or Dir backend | code, framework | `feature/sim-storage/table-dir-backends` - The backend row owns reusable Table and Dir implementations.<br>`feature/sim-storage/mounted-table-dir-namespace` - The mounted namespace row composes independently owned backends without copying their storage behavior.<br>`feature/sim-storage/contract-emitter` - The emitter row keeps generated storage contract evidence current.<br>`recipe/sim-storage/crates/sim-table-mount/01-basics/backend-composition` - The backend-composition recipe exercises the mounted Table/Dir surface. |
| `route/accept-estate-controller` | Prove the bounded estate controller before a reviewed physical placement | code, framework | `feature/sim-estate/controller-acceptance` - Exercise explicit non-dispatch and controller-loss boundaries without selecting a private target or performing the deferred MIALOVE pilot.<br>`spec-test/sim-estate/crates/sim-lib-estate/src/controller_acceptance_tests` - The focused model specimen proves retry is legal only before dispatch and ambiguity enters quarantine. |
| `route/add-a-codec` | Add a codec | code, framework | `feature/sim-codecs/codec` - Start with the shared codec contract and position rules.<br>`feature/sim-codecs/contract-emitter` - The emitter row keeps generated codec contract evidence current. |
| `route/add-a-language` | Add a language | code, framework | `feature/sim-codecs/codec` - The codec row owns shared positions, limits, and loadable codec contracts.<br>`feature/sim-codecs/expression-syntax-grammars` - Expression syntax rows expose language and grammar subjects with round-trip contracts.<br>`feature/sim-codecs/pratt` - Operator-shaped languages reuse the Pratt syntax surface. |
| `route/add-a-packet-codec` | Add a packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire grammar row groups packet and byte-level protocol contracts. |
| `route/add-a-search-wire-site` | Add a search wire codec site | code, framework | `feature/sim-codecs/codec` - Begin with the shared bounded codec contract and wire-syntax ownership rules.<br>`feature/sim-codecs/searxng-search-wire` - The SearXNG row demonstrates one transport-free SearchWireCodec site without becoming the search architecture.<br>`recipe/sim-codecs/crates/sim-codec-search-searxng/01-basics/request-response` - The checked pure recipe supplies request and saved-response bytes without opening a socket. |
| `route/add-automotive-workflow` | Add an automotive diagnostic or service workflow | code, framework, user | `feature/sim-auto/automotive-runtime` - The automotive runtime row owns the shared contracts, bounded lanes, codec, and view integration.<br>`spec-test/sim-auto/crates/sim-lib-auto-order/tests/conformance` - The conformance specimen checks the order boundary through the public runtime contract. |
| `route/add-bounded-search-engine` | Add a bounded search engine | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns reusable algorithmic helpers.<br>`feature/sim-discrete/bounded-search` - The bounded-search row owns deterministic state-space search policy, bounds, receipts, and specimens. |
| `route/add-bridge-packet-codec` | Add a Bridge packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar. |
| `route/add-capability-gated-read-eval` | Add capability-gated read-eval | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core row owns read policy, capability checks, and read-construct evidence.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/read_construct` - The read-construct test checks object round trips through standard core. |
| `route/add-citizen-read-construct` | Add a citizen read-construct | code, framework | `feature/sim-citizen/citizen-read-constructs` - The citizen row owns runtime members and read-construct projection.<br>`spec-test/sim-citizen/crates/sim-citizen/src/conformance` - The conformance test checks citizen round-trip behavior. |
| `route/add-cookbook-recipe` | Add a cookbook recipe | code, framework | `feature/sim-foundation/cookbook` - The cookbook row owns reusable recipe metadata. |
| `route/add-dataflow-analysis` | Add a proof-producing dataflow analysis | code, framework | `feature/sim-runtime/incremental-query-core` - The core owns the one generic worklist, lattice admission, widening, continuations, explanations, incremental reuse, and completion proofs; callers contribute only domain transfer rules.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/definite_assignment_specimen` - The forward definite-assignment specimen covers branches, loops, unreachable code, exceptional edges, diagnostics, and exact incremental invalidation.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/range_analysis_specimen` - The backward range specimen proves explicit fingerprinted widening, typed budget exhaustion, resumability, explanations, and deterministic proof identity.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/ownership_guard` - The structural guard rejects a second memoized worklist while explicitly classifying domain transfer rules and unrelated algorithms. |
| `route/add-decoded-instruction-machine` | Add a bounded decoded-instruction machine | code, framework | `feature/sim-runtime/machine-contract` - The machine row owns generic slots, operands, frames, the iterative driver, bounded control transfer, safepoints, interruption, and exact receipts while leaving instruction semantics to the guest.<br>`spec-test/sim-runtime/crates/sim-lib-machine/tests/neutral_machine_specimens` - The stack-calculator and register-file specimens share the same driver, permits, and receipts while proving that the machine contract does not select one storage discipline.<br>`spec-test/sim-runtime/crates/sim-lib-machine/tests/ownership_guard` - The source-fact ownership fixture rejects a guest frame stack and recursive driver but admits guest-owned opcodes and instruction semantics. |
| `route/add-discrete-domain-helper` | Add a discrete domain helper | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete row owns algebra, certified rectangular assignment, staged paths, edit/DTW alignment, graph paths, ranking, and spectral helpers. |
| `route/add-eval-fabric-site` | Add an EvalFabric site | code, framework | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns runner and fleet placement libraries. |
| `route/add-femm-domain-helper` | Add a FEMM domain helper | code, framework | `feature/sim-femm/femm-domain-stack` - The FEMM row owns mesh, field, material, solve, and ODE helpers. |
| `route/add-femm-linear-solver` | Add a FEMM linear solver | code, framework | `feature/sim-femm/linear-solvers` - The linear solver row owns solver contracts, factor handles, iterative kernels, and certificate evidence.<br>`spec-test/sim-femm/crates/sim-lib-femm-solve/src/implementation/tests` - The checked implementation specimen proves solver reuse metadata and residual evidence for certificates. |
| `route/add-finite-enumeration-adapter` | Add a finite enumeration adapter | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns combinatorics and rank integration.<br>`feature/sim-discrete/finite-enumeration` - The finite enumeration row owns lazy words, digit/radix adapters, cyclic canonicalization, and longest-only selection. |
| `route/add-generated-doc` | Add a generated doc | code, framework | `feature/sim-tooling/generated-docs` - The xtask documentation row owns generated doc lanes and index fragments. |
| `route/add-guest-language-profile` | Add a guest language profile | code, framework | `feature/sim-runtime/guest-language-profiles` - The profile row establishes the checked thin-profile ownership boundary before any guest-specific work and points to Lua as the no-compiler reference.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/tests` - The shared profile specimen fails closed unless reader, lowering, eval policy, organ reuse, capabilities, and gaps are explicit.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/conformance` - The Lua source-codec specimen proves direct source parsing and lowering through the positive reference profile.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/lib` - The Lua profile and matrix-row tests are the final shared-matrix registration specimen for a guest profile. |
| `route/add-hardware-host-surface` | Add a hardware host surface | code, framework | `feature/sim-stream-host/hardware-host-surfaces` - The host row owns placement, device adapters, and hardware-facing stream surfaces.<br>`feature/sim-stream-host/read-first-device-sessions` - Choose observation-only or registered effect authority before exposing the provider. |
| `route/add-host-exec-primitive` | Add a host exec primitive | code, framework | `feature/sim-runtime/host-exec` - The exec row keeps host process behavior in a loadable library. |
| `route/add-host-primitive` | Add a host primitive | code, framework | `feature/sim-foundation/host-primitives` - Neutral port cards, open identities, runtime-mechanics refusals, budgets, and lexical bindings belong in sim-host-core; concrete host realization belongs in platform and domain libraries. |
| `route/add-host-storage-primitive` | Add a host storage primitive | code, framework | `feature/sim-storage/host-storage-primitives` - The host storage row points to file, database, and HTTP backends. |
| `route/add-http-search-site` | Add a provider-neutral HTTP search site | user, code, framework | `feature/sim-agent-net/provider-neutral-http-search` - Configure the codec behind the bounded shared HTTP skill transport.<br>`recipe/sim-agent-net/crates/sim-lib-search-http/01-basics/fixture-site` - The fixture proves live and cassette execution without a provider-specific transport. |
| `route/add-incremental-query-organ` | Add an incremental query organ | code, framework | `feature/sim-runtime/incremental-query-core` - The core row owns the dependency-light memo graph and cutoff algorithm.<br>`feature/sim-runtime/incremental-query-organ` - The organ row owns SIM conversion, capability gates, shapes, reports, immutable completed-analysis projection, and loadable exports.<br>`spec-test/sim-runtime/crates/sim-lib-incremental/src/tests` - The checked tests exercise registration, invalidation, verification, cutoff, reports, Card projection, and proof-backed analysis browsing. |
| `route/add-index-graph-record` | Add an index graph record | code | `feature/sim-foundation/index-graph-core` - The index core row owns graph ids, records, edges, and validation. |
| `route/add-kernel-contract` | Add a kernel contract or reuse a plain test context | code | `feature/sim-kernel/kernel-contracts` - The kernel contract row owns the reusable protocol boundary plus bare Cx, eager Cx, and plain test context helpers.<br>`feature/sim-kernel/contract-emitter` - The emitter keeps generated contract evidence current. |
| `route/add-language-neutral-class-semantics` | Add checked language-neutral class semantics | code, framework | `feature/sim-runtime/class-semantics` - The class row owns checked descriptors, bounded lineage policies, kernel Class projection, and managed non-retaining derived caches.<br>`recipe/sim-runtime/crates/sim-lib-class/01-basics/neutral-class` - The terminal recipe covers a hand-authored diamond order, typed failures, clearing-receipt-backed cache reclamation, member Shape browsing, and read construction without a guest-language type.<br>`spec-test/sim-runtime/crates/sim-lib-class/tests/neutral_class_specimens` - The runnable integration specimen supplies the exact characterization outcomes and source-level ownership proof. |
| `route/add-library-macro` | Add a library macro | code | `feature/sim-foundation/library-macros` - The macro row owns checked Rust declarations for SIM libraries.<br>`feature/sim-foundation/contract-emitter` - The emitter row keeps generated contract evidence current. |
| `route/add-model-facing-packet-workflow` | Add a model-facing packet workflow | code, framework | `feature/sim-agent-net/bridge-runtime` - The workflow row ties agent model exchange to BRIDGE packet records. |
| `route/add-number-domain` | Add a number domain | code, framework | `feature/sim-numbers/numbers` - The number row owns reusable number domains and their runtime libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The tensor citizen test checks runtime projection for a number domain. |
| `route/add-relation-provider` | Add a logical relation provider | code, framework | `feature/sim-storage/bounded-relation-site` - The site owns placement, checked provider traits, bounds, capabilities, effects, transactions, and receipts.<br>`spec-test/sim-storage/crates/sim-relation-site/src/tests` - The recording provider proves the effect seam without implementing SQL or relational evaluation. |
| `route/add-resident-femm-csr-solver` | Add a resident FEMM CSR solver | code, framework | `feature/sim-compute/femm-resident-solvers` - The compute FEMM row owns resident CSR upload, Krylov execution, f64 refinement, and loadable solver export.<br>`recipe/sim-compute/crates/sim-lib-compute-femm/01-basics/resident-csr-solve` - The recipe shows upload reuse, resident work vectors, scalar synchronization, and f64 certificate acceptance. |
| `route/add-runtime-organ` | Add a runtime organ | code, framework | `feature/sim-runtime/organs` - The organ row groups reusable binding, control, logic, pattern, incremental, mutation, namespace, and sequence behavior.<br>`feature/sim-runtime/binding-organ` - The binding row owns lexical cells and binding mechanics shared by guest profiles.<br>`feature/sim-runtime/function-organ` - The function row owns typed managed instances and collector-visible capture composition.<br>`feature/sim-runtime/control-organ` - The control row owns resumable frames and control transfer shared by guest profiles.<br>`feature/sim-runtime/property-mechanics` - The property row owns descriptor dispatch and reusable property semantics.<br>`feature/sim-runtime/namespace-organ` - The namespace row owns bounded module resolution and caller-supplied source roots.<br>`feature/sim-runtime/contract-emitter` - The emitter row keeps generated runtime contract evidence current.<br>`recipe/sim-runtime/crates/sim-lib-function/01-basics/neutral-language` - The terminal checked specimen composes typed lexical capture, positional and named inputs, browsing, managed-cycle collection, and optional generic dispatch without importing an established guest runtime. |
| `route/add-server-backed-web-session` | Add a server-backed web session | code, framework | `feature/sim-web/codec-surface-sessions` - The bridge session owns reversible surface encode, decode, commit, and pane isolation.<br>`feature/sim-web/server-backed-web-sessions` - RemoteTransport composes that session with sim-lib-server transport negotiation and eval request frames.<br>`spec-test/sim-web/crates/sim-lib-web-bridge/src/remote_tests` - The remote transport test performs real in-process server round trips, reconnect, close, and stale-revision failure. |
| `route/add-shape-contract` | Add a Shape contract | code, framework | `feature/sim-shape/shape` - The Shape row owns matching, binding, and citizen projection.<br>`feature/sim-shape/contract-emitter` - The emitter row keeps Shape contract evidence current.<br>`spec-test/sim-shape/src/citizen/inventory` - The inventory test checks the exported shape citizen surface. |
| `route/add-shape-recipe` | Add a Shape recipe | code | `feature/sim-shape/shape-recipes` - The recipe row owns standalone Shape example crates.<br>`feature/sim-shape/shape` - The Shape row owns the contract the recipe demonstrates. |
| `route/add-stream-protocol-helper` | Add a stream protocol helper | code, framework | `feature/sim-stream/stream-protocol-helpers` - The stream row owns reusable stream, rank, topology, and device helpers.<br>`spec-test/sim-stream/crates/sim-lib-stream-core/src/read_construct` - The read-construct test checks stream metadata projection. |
| `route/add-table-dir-core` | Add a Table or Dir core contract | code, framework | `feature/sim-foundation/table-dir-core` - The table core row owns shared Table and Dir contracts.<br>`feature/sim-foundation/table-path-references` - The path reference row owns escaped absolute and relative Table/Dir path parsing, normalization, and resolution. |
| `route/add-value-helper` | Add or reuse a value helper, expression field reader, or map field access helper | code, framework | `feature/sim-foundation/value-helpers` - The value row owns shared runtime value conversion, expression builder, expression field reader, map field reader, and field access helpers. |
| `route/admit-bank-statement` | Admit a bank statement for reconciliation | code, framework | `feature/sim-ledger/statement-admission` - The ledger-owned profile contract turns declared delimited layouts into exact canonical rows.<br>`spec-test/sim-ledger/crates/sim-ledger/src/statement` - The checked specimen proves two synthetic layouts have identical canonical values and typed refusals stay outside the snapshot. |
| `route/admit-dynamic-source` | Admit dynamic guest source without copying authority | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core owner keeps trusted read policy, required caller powers, and diminished allowed powers in one host-built SourceAuthority.<br>`feature/sim-sdk/source-authority-facade` - The SDK presents the canonical authority, request, broker, and dynamic-policy builders directly without a facade envelope.<br>`recipe/sim-sdk/source-authority/dynamic-text` - The public Rust recipe constructs authority through the SDK and proves admitted and missing-caller-power outcomes through the shared broker.<br>`spec-test/sim-runtime/crates/sim-lib-core/src/source_authority_ownership_tests` - The source-fact guard permits guest semantic wrappers, rejects repeated authority fields and generic admission types, and classifies unwired guests without adding behavior. |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/agent-reproducible-instrument-adoption` | Adopt an instrument proposed through conversation without retaining the conversation | code, framework | `feature/sim-agent-net/reproducible-instrument-adoption` - Freeze intent and coordinate the indivisible local adoption transaction.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/atelier/instrument_adoption_tests` - Exercise the conversation-free adoption transaction with frozen recipe inputs and rollback evidence. |
| `route/analyze-counterpoint` | Analyze counterpoint and bounded stretto relations | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Counterpoint and identity-bearing Staff conversion plus audited transform operations.<br>`feature/sim-music/exact-score-consonance` - The consonance row supplies exact half-open sounding-window alignment without collapsing voice or event identity.<br>`feature/sim-music/music-counterpoint` - The counterpoint row applies inspectable rule data to existing material and builds bounded graph-backed stretto relations while keeping analysis distinct from generation.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/stretto-graph` - The checked Lisp specimen exposes candidate entries, compatible couples, maximal cliques, and normalized chains with generation=false. |
| `route/analyze-metrical-patterns` | Quantize, compare, and discover repeated musical patterns | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - Music analysis supplies exact tempo/meter lattices, named feature extractors, invariance transforms, identity-bearing occurrences, support, and overlap policy while delegating DTW/search to sim-discrete and correlation to sim-numbers.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/metrical-similarity-pattern` - The checked descriptor states the straight/swing/tuplet quantization, DTW/correlation similarity, and bounded exact-verification contract. |
| `route/analyze-tonnetz-path` | Find a bounded neo-Riemannian path between canonical chords | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch-set owner supplies canonical masks and typed set relations; the chord owner supplies rooted chord identity; the existing Riemann namer supplies projection-only labels.<br>`feature/sim-music/exact-music-analysis-and-transform` - Music analysis applies enabled P/L/R generators over canonical major/minor identities and delegates deterministic shortest-path certificates to sim-lib-discrete-graph.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/tonnetz-path` - The checked descriptor gives the concrete C-major to A-minor call, explicit move set and bound, identity policy, one-edge R result, and reusable certificate contract. |
| `route/analyze-twelve-tone-row` | Validate, transform, and inspect a twelve-tone row without losing aliases or label conventions | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary supplies the canonical PitchClass identity; unordered PitchClassMask and pitch-set analysis stay in their existing owners.<br>`feature/sim-music/serial-series-calculus` - The generic serial layer supplies stable finite alphabet identity and exhaustive exactly-once aggregate validation without assuming pitch.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict ToneRow values, total normalized P/I/R/RI operations, alias-preserving RowFamilySet values, convention-explicit RowMatrix data and ASCII projection, operation-bearing RowForm values, explicit label conventions, and the narrow pitch/ToneRow Shape adapter.<br>`recipe/sim-music/crates/sim-lib-pitch-serial/01-basics/row-family-matrix` - The checked Rust scenario proves all 48 Op. 25 aliases against their operations, retains symmetry aliases across distinct-form reduction, and projects one source-and-convention-bearing matrix to structured coordinates and ASCII. |
| `route/assemble-agent-kind` | Assemble and run a different agent kind | user, code, framework | `feature/sim-agent-net/genai-assembly` - The existing GenAI assembly supplies the checked model exchange and late-bound provider placement.<br>`feature/sim-agent-net/manifest-selected-conduct` - AgentManifest binds roles, components, provider seats, authority, budgets, and the selected conduct independently.<br>`feature/sim-agent-net/agent-conduct` - Choose ReAct, plan-act-replan, phased, verify-retry, router-crew, or another certified data-only conduct and inspect its reflected contract before running it.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/react` - The checked ReAct specimen runs a catalog conduct without a kind-specific Rust loop.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/standard_steps` - The executable specification proves the standard Cards and typed validation boundary used by every catalog kind. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/audify-measured-series` | Hear and creatively transform measured evidence without category fraud or route lock-in | user, code, framework | `feature/sim-music/daw-session-runtime` - The session owner records reversible or explicitly lossy audification, disjoint artistic-mapping provenance, stable content identity, and append-only route substitutions.<br>`feature/sim-music/audio-lift-and-render` - The audio owner supplies bounded analysis and deterministic rendering without owning scientific meaning.<br>`spec-test/sim-music/crates/sim-lib-daw-session/src/listening` - The listening specimens prove route churn leaves musical and expedition identity stable, mapping families cannot decode as one another, all named target substitutions are typed, and unsupported targets retain exact evidence. |
| `route/audit-analytic-mechanical-impact` | Audit a mechanical contact impulse with momentum and work equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite retains one-sided impact states, an explicit impulse, independent kinetic stores, clean selection inputs, and momentum/work identities.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common one-sided event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for momentum, work, provider-envelope, and modal-root proofs. |
| `route/audit-analytic-switched-electrical-study` | Audit a switched lumped electrical study against independent equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite proves two oriented voltage/current ports, a discontinuity, terminal event, stiff interval, independent stores, refinement contraction, and a certified threshold.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for the public equation-derived composition and metamorphic cases. |
| `route/audit-conjugate-port-work` | Audit signed continuous and impulsive work across physical boundaries | code, framework, user | `feature/sim-physics/boundary-event-core` - Declare the boundary and crossing port identities.<br>`feature/sim-physics/conjugate-port-work` - Pair effort and flow, orient the port, split continuous work at events, and retain impulses separately.<br>`spec-test/sim-physics/crates/sim-lib-physics-power/tests/work` - The specimen proves analytic domains, orientation, discontinuities, simultaneous events, and pure impulses.<br>`recipe/sim-physics/01-power/switched-two-port` - Run a switched electrical-to-mechanical two-port audit. |
| `route/audit-independent-stored-energy` | Audit stored-energy balance without deriving endpoint stores from transferred work | code, framework, user | `feature/sim-physics/conjugate-port-work` - Retain signed continuous work and event transfers at the declared boundary.<br>`feature/sim-physics/stored-energy-audit` - Evaluate both endpoint states constitutively and compute the visible balance with typed evidence lanes.<br>`spec-test/sim-physics/crates/sim-lib-physics-audit/tests/audit` - The specimen proves independent perturbations, sign and omission failures, distinct residuals, and separated uncertainty.<br>`recipe/sim-physics/02-audit/stored-energy` - Inspect the equation, Shapes, and runtime exports. |
| `route/audit-platform-membrane` | Audit every platform membrane realization | code, framework | `feature/sim-platform/product-membrane` - Begin with the permanent classification, closure, evidence, and ownership gate.<br>`feature/sim-platform/construction-integration-realization` - Check construction integration remains a capsule realization, not domain ownership.<br>`feature/sim-platform/device-physical-port` - Check physical device mechanics remain behind the shared device port.<br>`feature/sim-platform/native-midi-realization` - Check native MIDI mechanics remain behind the music-owned contract.<br>`feature/sim-platform/process-realization` - Check modeled and physical process providers share the runtime-owned port.<br>`feature/sim-platform/web-shell-realization` - Check native web hosting does not absorb presentation policy.<br>`feature/sim-platform/native-compute-probing` - Check hardware probing is injected into compute-owned executors.<br>`feature/sim-platform/native-media-realization` - Check audio and plugin mechanics remain behind media-owned ports.<br>`feature/sim-platform/host-directory-realization` - Check host paths remain confined behind the storage-owned port.<br>`feature/sim-platform/ledger-command-realization` - Check ledger composition consumes only preopened storage and clock services.<br>`feature/sim-platform/macos-capsule` - Verify the macOS Card and cross-build evidence independently.<br>`feature/sim-platform/windows-capsule` - Verify the Windows Card and cross-build evidence independently.<br>`feature/sim-platform/android-capsule` - Verify the Android Card, shell boundary, and target attestations independently.<br>`feature/sim-platform/ios-capsule` - Verify the iOS Card, shell boundary, and target attestations independently.<br>`feature/sim-platform/wasi-capsule` - Verify the declared WASI import surface and modeled/hosted evidence independently.<br>`feature/sim-platform/ubuntu-rpi-capsule` - Keep cross-build and physical Raspberry Pi support grades distinct.<br>`feature/sim-platform/amazfit-proxy-capsule` - Keep the bounded watch proxy separate from Android capsule ownership.<br>`feature/sim-platform/halo-proxy-capsule` - Keep the official-contract glasses proxy separate from its host capsule. |
| `route/audit-search-provenance` | Audit a search result from final rank to immutable source evidence | user, code, framework | `feature/sim-web/search-audit-surface` - The offline surface distinguishes unverified provider claims from selector-checked quotations and exposes ranking, fetch, robots, fidelity, and policy provenance.<br>`recipe/sim-web/crates/sim-lib-view-search/01-basics/offline-audit` - The checked recipe demonstrates a complete audit projection without network authority. |
| `route/author-agent-conduct` | Author an agent conduct control loop | user, code, framework | `feature/sim-agent-net/agent-conduct-records` - The core records define the stable frame, step Card, budget, journal, replay, and fork vocabulary every conduct uses.<br>`feature/sim-agent-net/agent-conduct-topology-profile` - The topology profile certifies data-only conduct packages and binds their Cards through the shared topology engine.<br>`feature/sim-agent-net/agent-conduct` - The agent/conduct-v1 profile validates RunFrame Shapes, step Cards, outcome routing, roles, authority, budgets, terminal admission, and embedded tests.<br>`recipe/sim-agent-net/crates/sim-lib-agent-conduct/01-basics/echo-conduct` - The checked recipe demonstrates a minimal authored topology package loaded and executed through the shared engine.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/third-party-step` - The checked extension specimen adds a Card and implementation without editing conduct or topology source.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/router-crew` - The data-only triage and routing family proves a nontrivial authored graph remains catalog data rather than baked-in control flow. |
| `route/author-estate-exposure` | Author a safe estate exposure | code, framework | `feature/sim-estate/exposure-authoring` - The closed compiler admits only reviewed templates and shaped values.<br>`feature/sim-estate/model-provider-conformance` - The deterministic provider proves lifecycle behavior before physical placement.<br>`feature/sim-estate/sealed-command-site` - The command site preserves the closed declaration at the process boundary.<br>`feature/sim-estate/ansible-provider-site` - The Ansible decoration adds sanitized discovery and durable typed callback evidence. |
| `route/author-platform-provider` | Author a platform provider | code, framework | `feature/sim-platform/platform-owner` - The sole owner validates capsule identity, declared services and shells, canonical bootstrap use, and product-closure isolation before any real provider can be claimed.<br>`feature/sim-platform/bootstrap-rind` - The bounded rind admits only the content-bound adjacent capsule through the stable native ABI.<br>`feature/sim-platform/platform-policy` - The pure resolver and modeled site provide the reusable service-selection and deterministic conformance surface. |
| `route/bind-relational-domain` | Bind a relational value through Shape | code, framework | `feature/sim-shape/relation-shape` - The relation adapter resolves DomainSpec Shape refs through Cx and owns cell/row binding.<br>`feature/sim-shape/shape` - The shared Shape protocol remains the only matcher and binder.<br>`spec-test/sim-shape/crates/sim-relation-shape/src/tests` - The checked specimen binds a custom UUID-over-text domain and rejects a boolean mismatch. |
| `route/build-audio-workbench` | Build an audio workbench | user, code | `feature/sim-audio-daw/audio-graph-workbench` - The graph row collects the DSP and plugin recipes for local construction.<br>`feature/sim-audio-daw/audio-device-providers` - The provider row selects a modeled or host-backed audio target.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-core/01-basics/copy-node` - The copy-node recipe is the smallest graph example.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-dsp/01-basics/offline-chain` - The DSP recipe names the reusable filter, dynamics, delay, and modulation processor chain.<br>`spec-test/sim-audio-daw/crates/sim-lib-audio-dsp/src/tests` - The DSP specimen proves preallocated oscillator/resampler state, folded-harmonic suppression, impulse gain, passband response, and downsampling alias rejection.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-live/01-basics/live-config` - The live recipe selects bounded callback buffers and queues before host placement. |
| `route/build-evidence-bearing-web-search` | Build provider-neutral web search with exact citation evidence | code, framework | `feature/sim-foundation/web-evidence-core` - The web row owns raw and normalized identities, exact selectors, and complete typed policy receipts.<br>`feature/sim-foundation/search-evidence-core` - The search row keeps provider claims distinct from observations and checked citations while exposing a pure wire boundary.<br>`recipe/sim-foundation/crates/sim-lib-search-core/01-basics/checked-citation` - The network-free checked specimen terminates at a provider claim beside an exact representation-bound citation. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-model-test-pack` | Build a reproducible model-test task pack | code, framework | `feature/sim-agent-net/model-test-packs` - The pack owner rebuilds pinned public fixtures, seals immutable epochs, and keeps private bytes behind caller-supplied control-plane loaders.<br>`spec-test/sim-agent-net/crates/sim-lib-model-test-packs/src/tests` - The checked pack specimen covers deterministic reconstruction, generated target/control pairs, private-loader boundaries, and sealed evaluator evidence. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/algorithm-foundry` - The foundry row composes the complete performance plan without baking stage providers into the application.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/carry-a-wasm-library` | Carry a wasm library across an explicit host boundary | code, framework | `feature/sim-codecs/wasm-abi-contract` - The ABI row owns inert manifests, byte frames, imports, and exports while the selected platform loader retains host authority.<br>`spec-test/sim-codecs/crates/sim-wasm-abi/src/tests/roundtrip` - The checked round-trip specimen proves ABI records remain pure data.<br>`spec-test/sim-codecs/crates/sim-codec-compare/src/platform_roundtrip_tests` - The cross-platform specimen proves the same contract survives platform codec projection. |
| `route/certify-physics-refinement` | Distinguish numerical convergence from a certified mathematical-model verdict | code, framework, user | `feature/sim-physics/stored-energy-audit` - Retain independent audit evidence lanes.<br>`feature/sim-physics/certified-orthogonal-refinement` - Refine each assumption explicitly and require a sealed enclosure for definite claims; its crate integration tests prove sign reversal, non-monotonicity, independent failures, refusal of estimates, continuation diagnoses, and reproducible identity.<br>`recipe/sim-physics/03-proof/certified-refinement` - Inspect event-exact meshing and runtime exports. |
| `route/characterize-a-refactor` | Capture behavior before a refactor and compare it afterward | code, framework | `feature/sim-runtime/characterization-captures` - Standard core owns bounded canonical captures, content identity, strict two-sided comparison, and located differences.<br>`feature/sim-sdk/characterization-facade` - The SDK presents the public migration types without exposing standard-core internals.<br>`recipe/sim-runtime/crates/sim-lib-standard-core/01-basics/characterize-migration` - The public recipe states the capture-before, refactor, capture-after, strict-compare contract.<br>`spec-test/sim-sdk/tests/characterization` - The downstream test proves identical captures and a located mismatch through SDK-visible types. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/check-sim-run-index` | Check the sim-run index fragment | code | `feature/sim-run/index` - The index row owns the generated fragment.<br>`feature/sim-run/runtime-index` - The runtime command reads the merged index.<br>`recipe/sim-run/publish-readiness/package-list` - The recipe exercises package metadata. |
| `route/collect-language-neutral-managed-graph` | Collect a language-neutral managed graph | code, framework | `feature/sim-runtime/mutation-organ` - The mutation row supplies allocation-deterministic managed ids, graph storage, roots, weak edges, ephemerons, and safepoints without selecting a guest language.<br>`feature/sim-runtime/gc-tracing` - The collector row supplies bounded synchronous tracing, atomic sweep, weak clearing, and isolated finalization admission without claiming language behavior.<br>`spec-test/sim-runtime/crates/sim-lib-gc-tracing/src/tests` - The checked non-language managed-graph specimen compares generated graphs and safepoint schedules with a model tracer and proves wasm-closure collection. |
| `route/compare-cross-language-callables` | Compare Python, JavaScript, and Lua callable and object-model semantics | code, framework | `feature/sim-runtime/function-organ` - The function organ supplies the neutral kernel Callable projection and browse Shapes shared by all three guests.<br>`feature/sim-runtime/class-semantics` - The class organ owns declared-parent lineage while explicitly excluding prototype and __index delegation.<br>`feature/sim-runtime/python-object-control-policy` - Python projects its declared superclass graph through the class protocol.<br>`feature/sim-runtime/javascript-core-profile` - JavaScript retains prototype lookup as guest object policy rather than declared lineage.<br>`feature/sim-runtime/guest-language-profiles` - Lua retains metatable __index lookup as guest policy rather than declared lineage.<br>`recipe/sim-runtime/crates/sim-lib-lang-matrix/01-basics/cross-language-callable` - The terminal checked recipe browses and calls all three functions, compares canonical outcomes, and proves that only Python answers the declared-parent query. |
| `route/compile-agent-intent` | Compile agent intent | code, framework | `feature/sim-agent-net/forge-intent` - The Forge row owns checked intent compilation.<br>`feature/sim-agent-net/agent-server-tools` - Server tool rows expose the host-facing model gateway. |
| `route/complete-exact-score-consonance` | Complete an exact score with reversible consonance additions | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns identity-bearing Staff values and the generic additive staff apply/remove seam.<br>`feature/sim-music/exact-score-consonance` - The evaluation row supplies exact sounding windows and separately inspectable named metrics.<br>`feature/sim-music/reversible-consonance-completion` - The completion row composes typed additions, constraints, reports, content binding, changed windows, provenance, and exact inversion while delegating deterministic traversal, bounds, interrupts, and receipts to sim-lib-discrete-search.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/additive-consonance-completion` - The checked descriptor and paired Rust specimen show the bounded reversible completion contract. |
| `route/complete-serial-consonance` | Complete a realized serial plan with bounded reversible additions | user, code, framework | `feature/sim-music/serial-practice` - The serial row owns the immutable plan, stable event identities, strict realization, and the structural-versus-all-sounding reading boundary that the adapter must preserve.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row supplies the optional modal-projection evidence that the completion adapter can admit explicitly without pretending the result remains strict chromatic output.<br>`feature/sim-music/exact-score-consonance` - The exact score row establishes the identity-bearing staff surface and exact sounding timeline that additive completion must preserve.<br>`feature/sim-music/reversible-consonance-completion` - The reversible completion row defines the generic additive-search contract that the serial row narrows through serial-specific legality and post-completion ledgers.<br>`feature/sim-music/serial-consonance-completion` - The serial completion row filters generic candidates through serial allowances, leaves the structural plan unchanged, delegates deterministic finite traversal, work/result bounds, cancellation, and honest receipts to sim-lib-discrete-search, and returns structural plus all-sounding ledgers beside the reversible patch result.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/serial-consonance-completion` - The checked Rust scenario completes one realized row statement with a stated pitch-class addition, proves the structural ledger is unchanged, and records the added material only in the all-sounding ledger. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/compose-jvm-from-sdk` | Compose the bounded JVM from the SDK | user, code, framework | `feature/sim-sdk/jvm-composition` - One feature exposes the existing decoder, authority, profile, invocation, and specimen contracts without a facade-owned runtime.<br>`recipe/sim-sdk/jvm/product-surface` - The public recipe imports every JVM composition contract through sim alone.<br>`recipe/sim-sdk/jvm/lambda-interop` - The public recipe reaches both lambda interop directions through sim alone.<br>`spec-test/sim-sdk/tests/jvm_exports` - The downstream-only test proves every JVM entry is reachable through the SDK. |
| `route/compose-model-observatory-from-sdk` | Compose the provider-neutral model observatory from the SDK | user, code, framework | `feature/sim-sdk/model-test-facade` - The granular feature exposes the canonical observatory without changing the minimal facade.<br>`feature/sim-agent-net/model-test-product` - The owning product supplies command semantics, offline projections, and deterministic evidence export. |
| `route/compose-music-algorithms-from-sdk` | Compose the modular music algorithm foundry from the SDK | user, code, framework | `feature/sim-sdk/music-algorithm-composition` - The SDK row owns focused feature selection and curated re-exports over the frozen candidates while the music owner keeps the open data-plan registry and replaceable foundry stages.<br>`recipe/sim-sdk/music-algorithms/foundry-plan` - The Lisp load recipe reproduces the bounded foundry plan through the five focused candidate crates.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/music_algorithms` - The Rust facade specimen checks the same transform, search, ratio, consonance, counterpoint, and plan bounds. |
| `route/compose-music-carpet` | Compose and relatively encode a finite music carpet | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Music, rational time, audited conversions, and reusable transform chains; carpet addressing calls the shared discrete-rank crate rather than implementing rank locally.<br>`feature/sim-music/carpet-composition` - The carpet row owns finite axes, sparse cells, explicit layout policy, shared mixed-radix addressing, algebraic layout operations, and relative pitch/time forms.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/carpet_conformance` - The checked algebraic fixtures prove mixed-radix rank parity, explicit boundary policy, rotations, reflections, slices, overlays, and transform-chain composition.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/relative_conformance` - The checked conversion fixtures prove exact retained-origin round trips and named losses for omitted origins and source structure. |
| `route/compose-offline-realtime-audio` | Compose one timbre as deterministic offline audio and a realtime DSP preview | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The sound vocabulary owns additive partials, envelopes, filters, Karplus-Strong, and compact FM timbres.<br>`feature/sim-music/synth-performance-workbench` - The synth workbench owns oscillator, subtractive, DX7 FM, modular synth, offline-render, PCM16 WAV, and explicit Table/Dir recipe routing.<br>`recipe/sim-music/crates/sim-lib-music-synth/03-composition/offline-realtime-preview` - The recipe maps every catalog need to an existing owner, renders offline through sound-render, previews through graph/DSP, and uses non-overwriting Table/Dir handles.<br>`spec-test/sim-music/crates/sim-lib-music-synth/src/tests/reuse` - The paired conformance test decodes the recipe and checks its nine owner mappings, bounded callback policy, Table/Dir handles, and phase-43 conversion/dither deferral.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves additive, Karplus-Strong, and FM timbres produce finite deterministic offline PCM. |
| `route/compose-platform-time` | Compose wall time, monotonic time, timers, and cancellation into a request lifetime | code, framework | `feature/sim-foundation/host-primitives` - The existing platform-time contracts own wall and monotonic values, timer behavior, explicit bindings, and the deterministic model.<br>`feature/sim-foundation/cancellation` - Cancellation composes with a supplied deadline without choosing an executor, timer implementation, or transport.<br>`recipe/sim-foundation/crates/sim-cancel/01-basics/request-lifetime` - The checked recipe demonstrates explicit host-to-request ownership; the crate race suite proves registration, propagation, timeout conversion, completion, and dropped-waiter behavior. |
| `route/compose-relations-from-sdk` | Compose a replaceable relational lifecycle from the SDK | user, code, framework | `feature/sim-sdk/relation-composition` - The SDK exposes each canonical owner behind one provider-neutral feature boundary.<br>`spec-test/sim-sdk/tests/relation_exports` - One admitted logical request runs unchanged through a recording site and SQLite.<br>`recipe/sim-sdk/relation/custom-domain-crud` - The recipe starts the complete custom-domain lifecycle using SDK paths only. |
| `route/compose-serial-music-from-sdk` | Compose serial music from the SDK | user, code, framework | `feature/sim-sdk/serial-music-composition` - The SDK row owns the single curated serial facade over the frozen candidates and the existing runtime-install surface for supporting shape and notation libs.<br>`recipe/sim-sdk/serial-music/row-matrix-analysis` - The theory recipe proves the facade reaches row-family and matrix analysis through public SDK names.<br>`recipe/sim-sdk/serial-music/modal-realization` - The adaptation recipe realizes one immutable plan through the registry-backed modal surface without mutating the source plan.<br>`recipe/sim-sdk/serial-music/reversible-completion` - The completion recipe proves additive serial completion is reversible through the facade.<br>`recipe/sim-sdk/serial-music/index-discovery` - The index recipe proves the generated feature and route ids are discoverable from the SDK repo surface.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/serial_music` - The public-facade conformance specimen checks reversibility, recipe parity, fail-closed behavior, and the feature claim. |
| `route/compose-staged-sequence-alignment` | Compose staged state selection with sequence alignment | code, framework, user | `feature/sim-discrete/discrete-algebra` - The discrete graph owner supplies certified layered shortest paths and DTW/edit alignment under shared work, memory, finite-cost, stable-tie, and verification policy.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/alignment/tests` - The alignment fixtures prove full and rolling certificates, windows, subsequence boundaries, work refusal, non-finite refusal, and tamper detection.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/layered` - The staged fixtures prove Bellman backpointers, forbidden transitions, bounded work, stable ties, and certificate verification.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/cookbook` - The checked composition invokes and verifies both engines and retains stable staged states plus DTW score, path length, cells, and edges without a copied domain-side DP loop.<br>`recipe/sim-discrete/crates/sim-lib-discrete-graph/02-scenarios/staged-alignment` - The public cargo-example recipe prints the same composition evidence for a human reader. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`feature/sim-construction/table-backed-project-books` - The persistence row injects the selected Table or Dir backend and keeps the append-only fact stream authoritative.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row joins stable controls to the existing Gantt plan and critical-path model.<br>`feature/sim-construction/mspdi-schedule-exchange` - The exchange row round-trips the portable Gantt plan across the MSPDI boundary.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row realizes modeled schedule placement while retaining the explicit live gates.<br>`feature/sim-construction/dalux-project-items` - The Dalux row realizes modeled field-item placement and preserves URL-free external and effect-ledger references.<br>`feature/sim-construction/production-lookahead-readiness` - The readiness row derives package and task readiness from accepted facts and schedule joins.<br>`feature/sim-construction/change-commercial-exposure` - The change row derives exact exposure while retaining reference-only ledger ownership.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row derives system and project burn-down from explicit evidence.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The closeout row admits only evidence-backed, disclosure-approved reference claims.<br>`feature/sim-construction/office-evidence-bridge` - The evidence bridge retains stable office references without copying construction truth or document payloads.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row projects the same filtered snapshot into existing Doc, Sheet, Deck, and suite surfaces.<br>`recipe/sim-construction/reference-project-control` - The checked primary Lisp recipe runs one entirely invented renovation twice through conflict, failure, bounded exception, correction, closeout, visibility-safe office review, and final reference admission while composing the public libraries, codecs, injected Table backend, and modeled vendor sites offline by default. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/describe-declarative-harmony` | Describe harmony palettes and rules as data | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies existing chord symbols, exact pitches and ratios, pitch sets, scale maps, voicings, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row assembles those owners into codec-neutral palettes, template chains, hard rules, soft metrics, evidence, voicing changes, and render settings.<br>`spec-test/sim-music/crates/sim-lib-pitch-chord/src/harmony_conformance` - The checked Lisp descriptors reproduce the catalog harmonizer and chord-cycle palette/filter chains without compiled policy. |
| `route/embed-estate-through-sdk` | Embed portable estate behavior | code | `feature/sim-sdk/estate-facade` - Select only the portable facade features required by the embedding application. |
| `route/encode-openai-provider-wire` | Encode canonical model traffic for an OpenAI provider wire | code, framework | `feature/sim-codecs/openai-provider-wires` - The wire row selects Chat Completions or Responses while preserving one canonical bounded model contract.<br>`spec-test/sim-codecs/crates/sim-codec-chat/src/tests/openai_responses` - The checked specimen covers Responses requests, responses, SSE events, and provider-private reasoning payloads. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/estimate-classical-spectrum` | Estimate a classical or autoregressive spectrum with reconstructable scaling | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns reference windows, periodogram/Welch/cross/coherence, Slepian multitaper, uneven-sample Lomb-Scargle, stable Burg/MEM models, explicit grids and scaling, and bounded evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/spectral_tests` - The checked tone/noise specimen proves window invariants, Parseval density, peak recovery, coherence, Slepian concentration, uneven-sample recovery, and fail-closed work limits.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/autoregressive_tests` - The generated AR-process specimen proves BIC order recovery, stable reflections, MEM resonance, bounded bidirectional prediction, and singular or unstable fail-closed behavior.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/spectral-estimators` - The runnable periodogram specimen prints coherent gain, equivalent noise bandwidth, peak power, degrees of freedom, and admitted work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/burg-mem-evidence` - The deterministic generated AR(2) recipe prints selected order, residual variance, reflection margin, MEM peak, and admitted work. |
| `route/evaluate-exact-score-consonance` | Evaluate exact score consonance | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical rational score time and identity-bearing Staff conversion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row supplies overlap- and pedal-aware realization before MIDI window evaluation.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row owns the pitch, ratio, contextual sonance, psychoacoustic, and tuning models.<br>`feature/sim-music/exact-score-consonance` - The consonance row composes those owners into multiplicity-preserving sounding windows and separate metric reports.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/exact-score-consonance` - The Lisp recipe shows an exact progression report with a retained doubling and no implicit aggregate. |
| `route/exchange-construction-schedule` | Exchange a construction schedule | user, code | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the XML codec and fidelity tests.<br>`recipe/sim-construction/crates/sim-codec-mspdi/01-basics/mspdi-round-trip` - The round-trip recipe shows a modeled Gantt schedule crossing the MSPDI boundary. |
| `route/exchange-score-musicxml` | Import and export a bounded MusicXML score | user, code, framework | `feature/sim-music/midi-notation-workflows` - The notation row owns the bounded MusicXML profile, stable exchange ids, loss reports, and one runtime import callable.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact score row supplies canonical rational Score semantics and the existing SIM expression/read-construct form.<br>`recipe/sim-music/crates/sim-lib-music-notation/01-basics/musicxml-import` - The notation recipe shows the bounded runtime call and profile limits.<br>`spec-test/sim-music/crates/sim-lib-music-notation/src/tests` - The checked fixture proves DTD rejection, bounds, ids, losses, and LilyPond/MusicXML/SIM semantic round trips. |
| `route/execute-tensor-at-site` | Execute tensor at a site | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the canonical Tensor value and checked host or resident observation.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns TensorExecutor, TensorSite, and element-wise broadcast request routing.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/tests/execution` - The checked execution tests prove direct CPU execution, TensorSite realization, capability checks, and environment restoration.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-bcast/src/tests` - The checked broadcast tests prove active executor routing, CPU fallback, zero-extent broadcasting, alias safety, and decline errors. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/explore-interference-field` | Explore an interference field | code, framework | `feature/sim-web/heatmap-surface` - The domain-neutral math view validates the already reduced finite scalar grid, mask, detector label, advisory, and SurfaceCaps budget.<br>`feature/sim-web/interference-surface` - The reversible surface selects the detector reduction, preserves Study evidence, and compiles edits to interference/project or interference/solve.<br>`recipe/sim-web/crates/sim-lib-view-interference/01-basics/interference-study` - The checked recipe encodes a Study, realizes projection and model Operations, refreshes the Scene, and proves desktop, phone, and glance budgets. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-foundation/index-vault-projection` - The projection row consumes the canonical inventory and proves every row has exactly one primary claim.<br>`feature/sim-codecs/index-vault-codec` - The codec row owns document dialect composition, v2 bundles, v1 reading, and semantic verification.<br>`feature/sim-index/vault-export` - The vault export row owns canonical loading, artifacts, reports, migration, and managed command behavior.<br>`spec-test/sim-codecs/crates/sim-codec-index-vault/tests/conformance` - The codec specimen proves all-profile round trips, semantic corruption rejection, and bounded v1 migration.<br>`spec-test/sim-tooling/src/index_vault_write_tests` - The checked materialization specimen proves managed writes preserve sibling notes and application configuration. |
| `route/expose-expression-tree-view` | Expose an expression tree view | code, framework | `feature/sim-expr-tree/expression-tree-view` - The expression-tree view row owns the bounded reversible outline over standard Scene, Intent, SurfaceCodec, and SurfaceCaps contracts.<br>`spec-test/sim-expr-tree/crates/sim-lib-view-expr-tree/src/tests` - The focused conformance module proves full rendering, intent reversal, lazy disclosure, continuation, freshness, hostile values, stale revisions, accessibility, large trees, layout projection, and authorization. |
| `route/expose-view-surface` | Expose a view surface | code, framework | `feature/sim-web/view-surface` - The sim-lib-view row owns the default view and reversible edit surfaces.<br>`feature/sim-web/codec-surface-sessions` - The bridge session consumes one SurfaceCodec for encode, decode, commit, projection, and isolated pane state.<br>`feature/sim-web/device-surfaces` - Device profiles show how projection adapts to concrete surface caps.<br>`feature/sim-web/web-shell-host` - The web shell row shows how a browser host loads those surfaces. |
| `route/find-a-grammar` | Find a grammar | code, framework | `feature/sim-codecs/expression-syntax-grammars` - The expression syntax row carries language and grammar subjects.<br>`feature/sim-codecs/domain-syntax-grammars` - The domain syntax row carries data, document, config, and packet syntax contracts.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire row carries packet and byte-level grammar contracts. |
| `route/find-code-behind-command` | Find code behind a command | code | `feature/sim-run/bootloader` - The bootloader owns command dispatch into loaded libraries.<br>`feature/sim-run/runtime-index` - The index trace command resolves command surfaces to owning crates and anchors.<br>`recipe/sim-run/02-scenarios/index-trace-cli` - The checked trace recipe follows a CLI surface back to code facts. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-numeric-root` | Find a scalar or vector root with reviewable evidence | code, framework, user | `feature/sim-numbers/root-finding` - The root owner distinguishes proved sign-changing brackets from residual-only estimates and records derivative, rank, work, termination, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-root/src/tests` - The checked tests cover endpoint and multiple roots, invalid and discontinuous brackets, flat derivatives, scaling, singular Jacobians, bounded Broyden resets, and deterministic replay.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/01-basics/scalar-root` - The scalar recipe demonstrates bracket-preserving Brent-Dekker search.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/02-vector/vector-root` - The vector recipe demonstrates rank-diagnosed damped Newton search. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/fit-finite-transition-model` | Fit an inspectable finite transition model | code, framework | `feature/sim-numbers/statistical-inference` - The statistics framework owns generic finite Markov counts, additive smoothing, held-out likelihood, deterministic serialization, and corpus provenance without domain-specific state types.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/markov_tests` - The checked non-music weather specimen proves smoothing, held-out scoring, fail-closed unknown states, and byte-stable serialization. |
| `route/fit-point-clusters` | Fit deterministic point clusters with convergence and model-selection evidence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns seeded k-means++ restarts, empty-cluster repair, regularized full/diagonal GMM EM, log-domain likelihood, singular-component policy, and AIC/BIC evidence while leaving temporal alignment to sim-discrete.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/clustering_tests` - Generated, degenerate, permuted, bounded, and non-finite fixtures prove deterministic centroids, log-domain likelihood, covariance regularization, convergence, repairs, and fail-closed control.<br>`recipe/sim-numbers/crates/sim-lib-numbers-stats/01-basics/clustering-evidence` - The runnable generated-cluster recipe prints selected restart, centroids, likelihood, repairs, AIC, BIC, and charged work and points alignment callers to the discrete graph owner. |
| `route/fuse-ranked-results` | Fuse bounded ranked result lists deterministically | code, framework | `feature/sim-stream/stream-protocol-helpers` - sim-lib-rank owns provider-neutral reciprocal-rank fusion and its inspectable receipt.<br>`recipe/sim-stream/crates/sim-lib-rank/01-basics/rank-fusion` - The three-list recipe demonstrates stable keys, weighted sources, deterministic output, and receipt ownership. |
| `route/generate-counterpoint` | Generate bounded reversible counterpoint | user, code, framework | `feature/sim-music/exact-score-consonance` - The consonance row owns the content-bound additive patch and exact inverse that preserve the fixed cantus staff.<br>`feature/sim-music/music-counterpoint` - The counterpoint row compiles rule data to finite CSP variables/domains, delegates traversal and receipts to discrete-search, propagates musical constraints, selects diverse legal results, and rechecks each result through the existing analyzer.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/bounded-generation` - The checked Lisp specimen generates two second-species voices under explicit work, result, frontier-default, and seed evidence. |
| `route/grow-bounded-music-catalog` | Grow and inspect a bounded recursive music catalog | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row supplies first-class partial pitch maps and scale-aware music transformations.<br>`feature/sim-music/bounded-rewrite-catalogs` - The rewrite row owns typed musical alphabets, productive context rules, scale-following state data, finite progression-tree topology, and derivation evidence while delegating ordinals and traversal to shared discrete rank/search.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/rewrite_conformance` - The checked conformance module proves typed contextual derivation, preflight rejection, rank round trips, bounded filtered search, scale-map materialization, and stable receipts. |
| `route/guard-an-operation` | Guard and replay a capability-scoped operation | code, framework | `feature/sim-runtime/guarded-operations` - The generic gate owns explicit policy, exact approval, atomic use, records, and the performer boundary.<br>`recipe/sim-runtime/crates/sim-lib-operation-gate/01-basics/two-manifests` - Two unrelated fixture manifests prove the gate contains no automotive assumption. |
| `route/harmonize-melody` | Harmonize a melody under explicit rules and bounds | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies melody pitch sets, chord templates, scales, registers, voice-leading, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row supplies the palette plus separate hard legality and weighted score lanes.<br>`feature/sim-music/bounded-harmonization` - The harmonization row adapts the shared musical problem to exhaustive, factored, certified layered-DP, and beam strategies.<br>`recipe/sim-music/crates/sim-lib-music-combinators/01-basics/bounded-harmonization` - The Lisp recipe shows the public layered-DP call with explicit work, result, and seed bounds. |
| `route/host-sim-in-browser` | Host SIM in a browser without moving presentation policy into the platform | code, framework | `feature/sim-platform/browser-capsule` - The browser capsule contributes capability-honest handles and lifecycle events through canonical named-call frames.<br>`feature/sim-web/view-surface` - sim-web remains the sole owner of reversible browser presentation and edit behavior. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/infer-hidden-sequence` | Summarize a stream or infer a finite hidden-state sequence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns hard-bounded mergeable quantiles, shared finite transitions, normalized discrete/Gaussian HMM inference, and seeded bounded fitting evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/quantile_tests` - The checked monotone-stream specimen proves exact small data, bounded retained entries, rank accuracy, merge behavior, and transactional refusal.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/hmm_tests` - The enumerated tiny fixture proves likelihood, posterior, and Viterbi agreement; long, continuous, seeded-fit, and work-limit fixtures prove numerical and termination behavior. |
| `route/inject-server-wall-clock` | Inject a server wall clock | code, framework | `feature/sim-agent-net/server-wall-clock` - The server row documents consumption and compatibility re-exports without claiming time ownership. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/inspect-jvm-classfile` | Inspect or extend JVM classfile structure | code, framework | `feature/sim-codecs/classfile-inspection` - The classfile codec is the sole byte parser and publishes manifest-derived opcode, constant, and attribute coverage.<br>`recipe/sim-codecs/crates/sim-codec-classfile/01-inspection/retained-classfile` - The recipe demonstrates bounded JVM-free inspection with retained origins.<br>`spec-test/sim-codecs/crates/sim-codec-classfile/tests/index_coverage` - The guard fixture proves duplicate inventories, consumer byte parsers, lossy modified UTF-8, and hand-edited generated coverage fail closed. |
| `route/inspect-jvm-performance-evidence` | Inspect JVM prepared-execution performance evidence | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile publishes prepared-code, verified micro-op, and generated fusion coverage without owning benchmark statistics.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/performance-evidence` - The recipe maps every fidelity and performance claim to generated coverage and canonical benchmark harness raw samples.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/published_performance` - The checked publication fixture keeps raw report identities, summaries, generated coverage, and ownership guards connected. |
| `route/inspect-platform-command` | Inspect and verify the selected platform through the loadable command | user, code, framework | `feature/sim-run/platform-command` - The bootloader dispatches the fixed platform vocabulary through the loaded product library.<br>`spec-test/sim-run/crates/sim-run/src/platform` - The command specimen proves fixed dispatch, machine output, and typed refusal behavior. |
| `route/integrate-femm-index-one-dae` | Integrate a FEMM index-1 DAE | code, framework | `feature/sim-femm/implicit-residual-adapter` - The FEMM adapter owns the residual/Jacobian boundary and preserves FEMM field and mesh ownership.<br>`spec-test/sim-femm/crates/sim-lib-femm-ode/src/tests` - The checked adapter specimen proves that a mixed differential/algebraic FEMM residual becomes the public implicit form. |
| `route/interpolate-periodic-spectrum` | Interpolate or analyze a periodic signal under explicit Fourier conventions | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns continuous DFT-series evaluation and integration, single-bin evaluation, Hilbert construction, unwrapped phase, instantaneous frequency, and envelope following under one explicit convention.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/interpolation_tests` - The generated analytic periodic-function specimen proves off-grid interpolation, exact integration, endpoint refusal, wrapping, and single-bin parity.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/analytic_tests` - The generated tone specimen proves Hilbert quadrature under every normalization, phase unwrapping, interval-centered frequency, and finite attack/release envelopes.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/dft-interpolation` - The Lisp recipe retains unitary scaling, period, origin, endpoint, wrapping, sign, Nyquist, phase, and work evidence. |
| `route/interpret-saved-web-documents` | Interpret saved HTML, feeds, and robots | code, framework | `feature/sim-codecs/saved-web-documents` - The codec organ owns bounded inert parsing and projections for all three saved web formats.<br>`spec-test/sim-codecs/crates/sim-codec-doc/src/html` - The checked document specimen proves bounded inert HTML decoding into the shared document model. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/onset-beat-harmony` - The checked Lisp analysis call selects onsets, beats, MFCC, chroma, key, and chords under explicit varying-tempo, posterior/HMM, work, result, and seed policy; its generated click/chord fixture proves timing and labels.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/link-jvm-lambda` | Link and invoke a JVM lambda or method reference | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile owns bootstrap admission, direct-handle resolution, loader-local generated metadata, and Java descriptor adaptation.<br>`feature/sim-runtime/function-organ` - The shared function organ owns the neutral Callable projection and collector-visible captures used at the SIM boundary.<br>`feature/sim-runtime/class-semantics` - The shared class organ owns language-neutral class structure; defining-loader identity remains JVM policy.<br>`feature/sim-runtime/mutation-organ` - The managed graph owns lambda instances, captures, weak identities, and collection rather than a JVM-private graph or global cache.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/verifier_scope` - The verifier consumes the executor-owned admitted bootstrap registry and seals exact whole-class proof before preparation.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/java-lambda` - The published Java-to-SIM recipe links compiler-produced bytes and invokes the result through ordinary Callable.<br>`recipe/sim-sdk/jvm/lambda-interop` - The SDK recipe reaches both interop directions through public facade exports only. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/load-runtime-library` | Load a runtime library | code, framework | `feature/sim-runtime/library-loading` - The loading row owns standard and language-profile runtime libraries. |
| `route/load-runtime-surface` | Load a runtime surface | code, framework | `feature/sim-run/loaders` - The loader row owns native, source, and re-exported runtime library discovery.<br>`feature/sim-run/terminal-surface` - The terminal surface row shows a loadable view surface provided by a runtime library.<br>`recipe/sim-run/02-scenarios/host-verb` - The checked host-verb recipe exercises the terminal surface path. |
| `route/lower-javascript-regexp` | Lower bounded JavaScript RegExp syntax into the shared pattern organ | code, framework | `feature/sim-runtime/javascript-core-profile` - The bounded JavaScript profile owns ECMAScript admission, exclusions, and UTF-16 syntax policy.<br>`feature/sim-runtime/pattern-cursor-domains` - Admitted RegExp syntax lowers to the shared automaton and executes with exact code-unit offsets and bounded receipts.<br>`spec-test/sim-runtime/crates/sim-lib-lang-javascript/src/regexp` - The JavaScript specimen checks the real RegExp adapter, exact UTF-16 captures, work limits, and fail-closed unsupported clauses.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The organ specimen independently demonstrates exact byte, scalar, and code-unit domains plus bounded adversarial ambiguity. |
| `route/lower-lua-pattern` | Lower Lua pattern syntax into the shared pattern organ | code, framework | `feature/sim-runtime/guest-language-profiles` - Lua retains its surface syntax and character-class policy while composing runtime organs.<br>`feature/sim-runtime/pattern-cursor-domains` - The shared pattern organ validates the lowering and executes it with typed scalar offsets and bounded receipts.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The runnable cross-dialect specimen lowers Lua syntax beside glob and neutral IR, then executes all three through the same organ. |
| `route/lower-shell-glob` | Lower shell glob syntax into the shared pattern organ | code, framework | `feature/sim-runtime/pattern-cursor-domains` - The glob adapter lowers its anchored surface into the shared validated IR and bounded scalar-domain executor.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The runnable specimen proves glob syntax reaches the same organ as Lua and neutral IR without copying an executor. |
| `route/match-exact-code-unit-text` | Match exact UTF-16 text without losing code-unit positions | code, framework | `feature/sim-foundation/exact-code-unit-text` - The sim-text value preserves paired and lone surrogate units and owns checked conversion between scalar and code-unit offsets.<br>`feature/sim-runtime/pattern-cursor-domains` - The pattern organ executes directly over exact units and returns domain-typed match and capture offsets.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cursor_domains` - The checked specimen matches a lone surrogate and stops between a surrogate pair's halves without admitting that position as a scalar offset. |
| `route/model-lumped-boundary-events` | Model a lumped dynamic system with explicit boundaries and events | code, framework, user | `feature/sim-physics/boundary-event-core` - The core owns solve-independent topology and signed-power boundary validation.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - The specimen proves canonical identity, simultaneous events, and fail-closed topology. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/operate-continuity-phone-surface` | Operate a complete continuity session from a phone surface | user, code, framework | `feature/sim-web/continuity-phone-surface` - The phone codec projects journal truth and compiles only bounded semantic actions.<br>`spec-test/sim-web/crates/sim-lib-view-continuity-phone/src/tests` - The specimen proves silent interaction, stale-safe replay, lifecycle refusal, and an always-reachable stop. |
| `route/operate-estate-from-terminal` | Operate the estate from a terminal | user, code | `feature/sim-run/estate-command` - Boot the loadable command and emit the same typed calls used by other surfaces. |
| `route/operate-model-test-observatory` | Operate the model-test observatory through its loadable command | user, code, framework | `feature/sim-agent-net/model-test-product` - The product owns the complete command vocabulary while delegating lifecycle and provider authority to their existing owners.<br>`spec-test/sim-agent-net/crates/sim-lib-model-test/src/tests` - The product specimen proves fixed dispatch, bounded machine output, and provider-contact boundaries. |
| `route/operate-provider-organ` | Discover, validate, and fan out across provider seats | user, code, framework | `feature/sim-agent-net/provider-control` - The provider organ owns open family adapters, private inventory, explicit seats, shared conformance, and ordered fan-out.<br>`feature/sim-agent-net/model-runner-framework` - Every opened direct API, CLI broker, local-service, or extension seat executes through ModelRunner.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE owns the checked request and output contract shared by every family.<br>`recipe/sim-agent-net/crates/sim-lib-provider/01-basics/open-seat` - The checked recipe opens a provider seat through the same registry and card contract used by shipped and third-party families.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The unchanged checked GENAI recipe remains the canonical request source. |
| `route/operate-relational-data` | Discover inspect migrate query mutate or mount a database relation schema SQLite site or SQL projection | user, code, framework | `feature/sim-foundation/relation-core` - The provider-neutral logical relation framework owns rows, domains, and identities.<br>`feature/sim-shape/relation-shape` - Shape admits relation values and command artifacts.<br>`feature/sim-codecs/sql-projection` - The SQL codec is a projection of checked plans, never a command escape hatch.<br>`feature/sim-foundation/relation-migrate` - Migration admission and drift checks precede physical action.<br>`feature/sim-storage/bounded-relation-site` - The site contract gates provider effects and bounds.<br>`feature/sim-storage/sqlite-relation-locator` - SQLite is selected through a closed provider identity and opaque locator.<br>`feature/sim-storage/relation-table-dir-projection` - Results render through standard Table/Dir composition.<br>`feature/sim-office/office-site-workflows` - Office workflows reuse relational sites rather than copying storage behavior.<br>`feature/sim-ledger/ledger-libraries` - Ledger composition supplies durable domain reuse.<br>`feature/sim-sdk/relation-composition` - The SDK facade exposes the supported relation composition.<br>`feature/sim-storage/relation-command` - The loadable command parses only named, authorized, bounded operations.<br>`spec-test/sim-storage/crates/sim-lib-relation-cli/src/tests` - Checked specimens cover creation, inspection, adoption, drift refusal, query, mutation, providers, and mounts. |
| `route/parse-javascript-source` | Parse bounded JavaScript source | code, framework | `feature/sim-codecs/javascript-source-frontend` - The JavaScript frontend owns bounded ECMAScript tokenization, structural parsing, source identity, and expression lowering.<br>`spec-test/sim-codecs/crates/sim-codec-javascript/tests/conformance` - The conformance specimen checks accepted syntax, stable lowering, round trips, and fail-closed limits. |
| `route/parse-operator-language` | Parse an operator language | code, framework | `feature/sim-codecs/pratt` - The Pratt codec is the operator-language parser surface. |
| `route/parse-python-source` | Parse bounded Python source | code, framework | `feature/sim-codecs/python-source-frontend` - The Python frontend owns bounded Python tokenization, layout, source identity, and expression lowering.<br>`spec-test/sim-codecs/crates/sim-codec-python/src/tests` - The conformance specimen checks admitted syntax, stable lowering, round trips, and fail-closed limits. |
| `route/parse-typescript-notation` | Parse bounded TypeScript notation | code, framework | `feature/sim-codecs/typescript-syntax` - The TypeScript frontend owns lossless notation and direct erasure of the compiler-independent subset.<br>`spec-test/sim-codecs/crates/sim-codec-typescript/src/tests` - The conformance specimen checks TypeScript and TSX parsing, lossless identity, and fail-closed erasure gaps. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/plan-review-apply-estate-change` | Plan, review, and apply an estate change | code, framework | `feature/sim-estate/exposure-authoring` - Compile a closed operation and shaped values.<br>`feature/sim-estate/guarded-operation-lifecycle` - Intern the exact plan, consume review once inside the gate, lease targets, and dispatch durably.<br>`feature/sim-estate/durable-operation-book` - Browse the linked event truth and rebuild projections. |
| `route/plan-serial-practice` | Freeze and realize a serial plan with immutable provenance and chord-safe partial order | user, code, framework | `feature/sim-music/serial-series-calculus` - The generic serial layer owns stable finite ordered-symbol identity and aggregate validation without assuming pitch or score order.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict row values and operation-bearing RowForm provenance for each row instance.<br>`feature/sim-music/serial-practice` - The music-serial layer freezes row instances, stable event ids, ordinal references, role/origin provenance, explicit parents, simultaneous groups, and precedence into one immutable validated plan, then realizes strict statements with explicit register, duration, dynamics, rests, ties, and canonical music-core rendering.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/immutable-plan` - The checked Rust scenario proves that one row can span multiple voices while equal-onset material stays grouped without a fabricated chord order.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/strict-row-realization` - The checked Rust scenario realizes a strict row statement with a chord, rest, tie, and equal-pitch simultaneous notes while preserving plan, event, voice, and ordinal identity through the piano roll. |
| `route/prepare-balanced-ledger` | Prepare a balanced ledger | user, code | `feature/sim-ledger/ledger-command` - The command row owns the operational entry point.<br>`recipe/sim-ledger/crates/sim-ledger/01-basics/balanced-year` - The balanced-year recipe shows the expected yearly voucher shape. |
| `route/preserve-physics-finding-history` | Preserve and investigate a surprising physics result without erasing competing explanations | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Produce reviewable sweep evidence and stable study identity.<br>`feature/sim-physics/immutable-finding-history` - Publish the observation, append evidence-bearing successors, retain branches, and rebuild read-only projections from immutable records. Its crate integration suite proves identity, backend parity, interrupted and concurrent append, corruption refusal, branch reconciliation, rebuild, complete history, and redaction.<br>`recipe/sim-physics/06-findings/sweep-to-finding` - Convert one sweep residual into an open finding. |
| `route/process-sampled-signal` | Transform, smooth, solve, or interpolate sampled data | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns wavelet boundaries and inverse shapes, canonical Savitzky-Golay derivative scaling, stable Toeplitz diagnostics, and duplicate/extrapolation interpolation policy.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/wavelet_tests` - The checked wavelet specimen proves multilevel odd-length inverses under periodic, symmetric, and zero extension plus the Le Gall linear-polynomial law.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/smoothing_tests` - The checked smoothing specimen proves polynomial preservation, factorial and sample-spacing derivative scaling, Toeplitz residuals, and singular-pivot diagnostics.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/sample_interpolation_tests` - The checked interpolation specimen proves affine preservation, monotonicity, duplicate resolution, extrapolation policy, and fail-closed input.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/wavelet-smoothing` - The runnable recipe composes all four generic algorithms with their visible reports. |
| `route/project-masked-scalar-heatmap` | Project a masked scalar grid as a heatmap | code, framework | `feature/sim-web/heatmap-surface` - The heatmap row validates caller-prepared finite values, masks, metadata, and SurfaceCaps-derived cell and byte budgets without applying a domain reduction rule.<br>`recipe/sim-web/crates/sim-lib-view-math/01-basics/heatmap-grid` - The checked recipe projects a bounded masked scalar grid into the domain-neutral scene/heatmap contract. |
| `route/project-reversible-expedition` | Project a reversible expedition product | user, code, framework | `feature/sim-web/reversible-worktable` - Replay owner-authored semantic edits into the disposable worktable without moving domain behavior into the surface.<br>`spec-test/sim-web/crates/sim-lib-view-worktable/src/lib` - The dependency-free specimen proves replay, undo, visible arming, manual continuation, and insufficient-evidence behavior. |
| `route/protect-opaque-state` | Protect opaque continuation or handoff state with rotation and optional single-use consumption | code, framework | `feature/sim-foundation/host-primitives` - The shared WallClock contract supplies the explicit expiry observation without ambient host time.<br>`feature/sim-foundation/protected-state` - The protected-state owner supplies the bounded envelope, collision-free binding, injected key and nonce capabilities, rotation behavior, uniform rejection, and canonical Table consumption adapter.<br>`recipe/sim-foundation/crates/sim-lib-protected-state/01-basics/exact-binding` - The checked recipe keeps caller serialization, key custody, clock ownership, and optional replay claims explicit. |
| `route/prove-benchmark-workload-neutrality` | Prove the benchmark harness is workload neutral | user, code, framework | `feature/sim-tooling/benchmark-cli` - The benchmark CLI composes sampling, process isolation, robust comparison, and durable reports without a domain-specific workload contract.<br>`spec-test/sim-tooling/src/bench/neutral_cpu_conformance` - The checked CPU-bound specimen proves deterministic sampling, matched comparison, host refusal, retained failure, and Table/Dir browsing without bytecode.<br>`spec-test/sim-tooling/src/bench/neutral_allocation_conformance` - The checked allocation-bound specimen proves local fingerprinting, high-dispersion refusal, retained failure, and durable report browsing without bytecode. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/raise-guest-value-from-sdk` | Raise and match a guest value from the SDK | code, framework | `feature/sim-runtime/organs` - The runtime organ owns Raised, bounded class matching, and ManagedException stable relation edges.<br>`feature/sim-sdk/raised-exception-composition` - The SDK row exposes those canonical contracts together under sim::exceptions. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/realize-host-need` | Realize a host need through the absolute platform membrane | code, framework | `feature/sim-foundation/host-primitives` - Begin with the neutral primitive, open identity, bounded refusal, and lexical binding contract.<br>`feature/sim-storage/host-storage-primitives` - Put storage semantics in the domain-owned port; substitute the corresponding domain port for non-storage needs.<br>`feature/sim-platform/platform-policy` - Use the seeded model as the deterministic, host-free behavior specimen before selecting physical mechanics.<br>`feature/sim-platform/ubuntu-pc-capsule` - Implement the need in one explicitly selected capsule whose Card declares the offered service.<br>`feature/sim-platform/transport-realization` - Run the shared port conformance at the capsule boundary so native results are compared semantically.<br>`feature/sim-platform/platform-owner` - Retain the matching Card, artifact, attestation, and evidence-derived support row in the composed bundle. |
| `route/realize-serial-spine` | Adapt an immutable serial plan onto a modal or caller-defined spine without hiding aggregate loss | user, code, framework | `feature/sim-music/serial-practice` - The serial owner keeps the immutable plan, stable event identities, row-form provenance, explicit strict specs, and strict chromatic baseline intact before any adaptation occurs.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary row owns named scales, caller-defined player scales, and contextual sonance models reused by the adaptation report.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns the first-class partial pitch-map policy that modal landing reuses instead of inventing another remap surface.<br>`feature/sim-music/open-serial-adaptation` - The open adaptation row owns landed modal realization, aggregate-relaxation receipts, and the independent spine, pitch, ordinal, and sonance report facets.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/third-party-adaptation` - The checked external-style scenario registers a seven-symbol non-pitch series, a custom practice rule, and a custom realizer through public APIs only, then proves the Lisp validation hook and realizer registry both fail closed on unknown components.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/modal-spine` - The checked Rust scenario realizes the same immutable plan through C Dorian, C Lydian, and a caller-defined scale, proves plan equality with differing sounding pitches, and inspects the spine report without claiming strict twelve-tone output. |
| `route/reconcile-quarantined-estate-run` | Reconcile or resolve a quarantined estate run | code, framework | `feature/sim-estate/durable-operation-book` - Read the existing run and retained evidence without dispatch.<br>`feature/sim-estate/guarded-operation-lifecycle` - Close, renew, release, or retain quarantine from observed artifacts; a mutation needs a new reviewed plan.<br>`feature/sim-estate/model-provider-conformance` - Prove unknown-after-dispatch and reviewed resolution before using a physical site. |
| `route/refresh-expression-tree-docs` | Refresh expression-tree documentation | code | `feature/sim-expr-tree/generated-docs` - The repository emitter owns generated package, recipe, rustdoc, and Index evidence. |
| `route/refuse-energy-influenced-selection` | Prove that a physical-study selection input is independent of energy | code, framework, user | `feature/sim-physics/no-energy-selection` - Apply the fail-closed influence domain over the canonical incremental engine, protected sinks, native type-state, and runtime Shape preparation; its crate integration suite proves direct and laundered influence, branches, joins, loops, opaque code, clean preparation, budget refusal, and incremental edits.<br>`recipe/sim-physics/04-influence/refused-selection` - Run a divide-by-time laundering refusal and inspect its causal path. |
| `route/repair-output-contract` | Repair an output contract | user, code | `feature/sim-agent-net/output-contract-repair-cli` - The repair command row owns the operational correction path.<br>`recipe/sim-agent-net/crates/sim-lib-agent-runner-core/01-basics/output-contract-repair` - The repair recipe provides the worked contract fixture. |
| `route/replay-content-addressed-fleet` | Replay a content-addressed fleet | user, code | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns the stream-fabric placement library.<br>`feature/sim-agent-net/fabric-fixture-cli` - The fixture command row exposes replay from the CLI surface.<br>`recipe/sim-agent-net/crates/sim-lib-stream-fabric/01-basics/content-addressed-fleet` - The content-addressed fleet recipe is the replay example. |
| `route/represent-interference-study-at-runtime` | Represent an interference study as runtime data | code, framework | `feature/sim-interference/tensor-runtime-records` - The runtime feature composes canonical Tensor storage, Citizen read-constructs, and Shape checks without adding a domain parser or grid storage.<br>`spec-test/sim-interference/crates/sim-lib-interference-runtime/src/citizen` - The checked Citizen inventory specimen proves every interference record can be installed and reconstructed. |
| `route/reproducible-federated-research` | Run reproducible federated research | user, code, framework | `feature/sim-agent-net/reproducible-federated-research` - Freeze intent, authority, sites, concurrency, budgets, and revisions before provider dispatch.<br>`feature/sim-agent-net/provider-neutral-http-search` - Collect provider claims and typed partial failures through provider-neutral Retriever cards.<br>`feature/sim-agent-net/reproducible-federated-research` - Cluster only evidenced aliases, fuse via shared RRF, capture selected pages under independent policy, verify selectors, and retain replay receipts.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/fixture-research` - The all-fixture recipe demonstrates intent through claims, capture, selectors, and replay without network access. |
| `route/research-web-through-cli-or-mcp` | Research the web through CLI or MCP | user, code, framework | `feature/sim-agent-net/search-product` - One product owns canonical configuration, execution, records, and stable ids.<br>`feature/sim-agent-net/mcp-server` - The existing Skill projection derives MCP schemas, capability checks, execution, and audit.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/cli-mcp-parity` - The checked fixture demonstrates identical CLI and MCP behavior without ambient network access. |
| `route/review-construction-project-chief-weekly-pack` | Review a construction project-chief weekly pack | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns authoritative append-only facts, accepted baselines, current and historical snapshots, deltas, evidence states, and stable control ids.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row filters facts before derivation, applies safety-first cadence ordering, preserves provenance and explained status, produces existing Doc, Sheet, and Deck values, and delegates presentation to the installed office suite surface.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/project-chief-weekly-pack` - The checked recipe shows the exact weekly provenance header, priority order, changed-since controls, non-green mandatory status, existing office values, and suite presentation. |
| `route/review-office-document` | Review an office document | user, code | `feature/sim-office/document-codecs` - The codec row chooses the file format and round-trip path.<br>`feature/sim-office/document-surfaces` - The surface row renders the document into a reviewable view.<br>`feature/sim-office/office-site-workflows` - The site row connects the document to modeled workplace sources.<br>`recipe/sim-office/crates/sim-lib-doc-surface/01-basics/suite-surface-descriptor` - The suite-surface recipe is the shortest surface example. |
| `route/run-authorized-jvm` | Run authorized JVM classfile bytes | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The runtime row owns bounded JVM semantics while reusing the shared classfile decoder, machine, managed graph, exceptions, and source authority.<br>`feature/sim-runtime/machine-contract` - The shared machine owns slots, frames, iterative drive, and safepoint publication.<br>`feature/sim-runtime/gc-tracing` - The managed and tracing organs own object identity, graph edges, roots, and bounded collection.<br>`feature/sim-runtime/class-semantics` - The class organ owns language-neutral declared-class structure while JVM loader identity remains JVM policy.<br>`feature/sim-runtime/capabilities-read-eval` - The source-authority organ keeps class loading explicit and diminished rather than ambient.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/product_specimen` - The public specimen executes static, managed-object, array, guest-exception, and exact-concat cases.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/ownership_guard` - Policy fixtures reject every prohibited ownership fork and scan the production JVM sources.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/baseline` - Generated opcode and intrinsic coverage totals differ from their manifests by zero. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/provider-control` - Provider families and seats are one open organ with shared conformance and fan-out.<br>`feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-generic-study` | Run and decide a deterministic evidence-backed study | user, code, framework | `feature/sim-runtime/domain-free-study-lifecycle` - The study product owns the one sealed lifecycle, staged design, report graph, selection, and effect boundary.<br>`spec-test/sim-runtime/crates/sim-lib-study/src/design` - The checked deterministic fixture covers smoke, screening, fixed confirmation, budget refusal, and selection evidence. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-jvm-command` | Run the JVM product specimen | user, code | `feature/sim-run/bootloader` - The standard bootloader owns the runtime session and verb handoff.<br>`feature/sim-run/jvm-command` - The JVM command contributes only host factories, capabilities, and the loadable CLI adapter.<br>`recipe/sim-run/02-scenarios/jvm-product` - The runnable recipe enters through the published sim command.<br>`spec-test/sim-run/crates/sim-run/tests/jvm_product` - The published sim binary loads authorized bytes and reports static, object, array, exception, and concat outcomes. |
| `route/run-music-algorithm-foundry` | Assemble a replaceable end-to-end music algorithm application from plan data | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns bounded SMF decoding, exact realization policy, and canonical export.<br>`feature/sim-music/audio-lift-and-render` - The audio row owns the optional audio-input analysis adapter and finite offline rendering inputs.<br>`feature/sim-music/algorithm-foundry` - The foundry row owns data-plan validation, open stage registration, Shape ranking, result checks, trace, Table/Dir boundaries, and realize-target preview delegation.<br>`feature/sim-music/bounded-harmonization` - The harmony row supplies the selected layered-DP and alternate exhaustive implementations under the same rules and bounds.<br>`feature/sim-music/music-counterpoint` - The counterpoint row supplies inspectable species rules, legal transforms, and bounded generation/analysis.<br>`recipe/sim-music/music-algorithm-foundry` - The runnable fixture checks every intermediate summary, exact SMF/WAV frame digests, data-only alternate selection, and the missing optional-stage diagnostic. |
| `route/run-numeric-pipeline` | Run a numeric pipeline | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and the numeric registry execution path.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-numeric/src/pipeline_run` - The checked pipeline-run specimen proves composed run input parsing and tensor-state admission. |
| `route/run-placement-transparent-physics-study` | Run a reviewable physics parameter sweep without coupling it to execution placement | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Bind the immutable plan, generate canonical coverage evidence, preserve statistical partitions, and submit independent points through EvalFabric; Tensor-backed models retain their ordinary TensorSite placement beneath that fabric boundary. The crate integration suite proves boundaries, partitions, sampler replay, placement parity, partial failure, interruption, deterministic replay, provider envelopes, and explicit gaps.<br>`feature/sim-physics/no-energy-selection` - Produce the clean influence proof required by the selection API while leaving energy outside the choice.<br>`recipe/sim-physics/05-study/placement-sweep` - Inspect a small inclusive sweep with an exact injected case and an explicit untested region. |
| `route/run-relation-command` | Run the standard relation command | user, code | `feature/sim-run/bootloader` - The bootloader selects loaded behavior from the relation product verb.<br>`feature/sim-storage/relation-command` - The storage library owns admitted relation operations and output.<br>`feature/sim-run/relation-command` - The standard distribution registers the library and declared SQLite site identity.<br>`spec-test/sim-run/crates/sim-run/src/relation` - The handoff specimen proves verb selection and standard site discovery. |
| `route/run-search-command` | Run canonical search from the terminal | user, code | `feature/sim-run/bootloader` - The standard bootloader owns command dispatch.<br>`feature/sim-run/search-command` - The command row selects the shared search product without implementing search.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/cli-mcp-parity` - The shared specimen proves the same record through CLI and MCP. |
| `route/run-serial-music-workbench` | Run a serial music workbench | user, code, framework | `feature/sim-music/twelve-tone-row-theory` - The pitch-serial row owns the strict source row, family/matrix inspection, and partition evidence that the workbench freezes before realization.<br>`feature/sim-music/serial-practice` - The serial practice row owns the immutable plan, strict realization context, simultaneous grouping, referential evidence, counter-voice techniques, arrays/time points, and integral parameter surfaces that the workbench assembles into one inspectable source.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row owns the registry-selected modal realizer and the spine report proving aggregate relaxation explicitly rather than silently mutating the source plan.<br>`feature/sim-music/serial-consonance-completion` - The completion row owns the reversible additive patch, structural-versus-all-sounding ledger audit, and exact inverse proof after completion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI and notation row owns SMF export, LilyPond projection, and the exact score lowering surface used for audition.<br>`feature/sim-music/serial-music-workbench` - The workbench row composes those replaceable owners into one executable recipe with fixture-manifest coverage and a hard no-copyrighted-score rule.<br>`recipe/sim-music/serial-workbench/01-end-to-end/row-to-audition` - The checked runnable recipe validates the fixture manifest, runs strict plus modal realization, completes and undoes one additive patch, audits the ledgers, exports MIDI plus LilyPond notation, and lowers the audition score end to end. |
| `route/run-study-command` | Run and inspect a generic study from the terminal | user, code | `feature/sim-run/bootloader` - The bootloader selects the study verb and loads its product library without acquiring study policy.<br>`feature/sim-runtime/domain-free-study-lifecycle` - The runtime library owns sealed coordinates, execution, evidence, reports, and decisions.<br>`feature/sim-run/study-command` - The command row wires the standard distribution to the loadable study surface.<br>`spec-test/sim-run/crates/sim-run/src/study` - The checked handoff specimen covers verb selection and loadable-library dispatch. |
| `route/run-tensor-ode` | Run a tensor ODE | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and numeric registry execution.<br>`feature/sim-numbers/tensor-ode` - The tensor ODE row owns tensor-state admission, CPU fallback, and hardware RHS restrictions.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns Env-bound executor routing and CPU fallback for tensor operations.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-rk/src/tests` - The RK tests prove tensor state across fixed/adaptive methods, hardware refusals, and CPU agreement. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/sdk-conversational-instrument-adoption` | Rebuild and atomically adopt a conversationally proposed instrument | code, framework | `feature/sim-agent-net/reproducible-instrument-adoption` - Use the canonical BRIDGE, ChangeCapsule, pack, rebuild, adoption, and rollback transaction.<br>`recipe/sim-sdk/hotload/conversational-instrument-adoption` - Follow the checked conversation-free SDK recipe. |
| `route/sdk-public-hotload-generation-loop` | Build and safely replace a native library generation through the public SDK | code, framework | `feature/sim-sdk/hotload-facade` - Use the focused facade records while keeping concrete host providers outside application code.<br>`spec-test/sim-sdk/tests/hotload_generation` - Run the deterministic build, admission, activation, refusal, reclamation, and replay proof.<br>`recipe/sim-sdk/hotload/public-generation-loop` - Reuse the two tiny sealed source generations from the cookbook surface. |
| `route/sdk-search-research-audit` | Run sim search research through deterministic conformance to the audit view | user, code, framework | `feature/sim-run/search-command` - The bootloader selects the canonical search product.<br>`feature/sim-sdk/web-search-facade` - The SDK composes the stable public contracts and checked fake-world fixture.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/web_search_offline` - The fixture proves network-free query-to-citation replay and attack boundaries.<br>`feature/sim-web/search-audit-surface` - The inert view exposes ranking, capture, selector, office, and policy evidence. |
| `route/seal-table-dir-backend` | Seal a Table/Dir backend | framework, code | `feature/sim-foundation/table-dir-core` - Keep the canonical Table and Dir contracts.<br>`feature/sim-storage/table-dir-backends` - Select the persistent backend that continues to own storage semantics.<br>`feature/sim-storage/sealed-table-decorator` - Inject host-owned key and nonce services, then wrap the backend with lane-bound authenticated encryption.<br>`spec-test/sim-storage/crates/sim-table-sealed/src/tests` - The checked fixture proves the host/Lisp authority boundary, persistent Dir composition, and fail-closed authenticated bindings. |
| `route/serve-authoritative-expression-tree` | Serve an authoritative expression tree | code, framework | `feature/sim-expr-tree/expression-tree-server` - The server row owns bounded authoritative sessions, EvalSite and EvalFabric routing, caller authority, logical-time lifecycle policy, optimistic revisions, and backpressured watches.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-server/src/tests` - The checked server specimen proves multi-client isolation, reconnect, cancellation, backpressure, authority diminution, stale commits, wall observations, idle expiry, and restart behavior. |
| `route/serve-over-mcp` | Serve over MCP | user, code | `feature/sim-agent-net/mcp-server` - The MCP row owns the command surface and loaded server libraries.<br>`recipe/sim-agent-net/crates/sim-mcp-server/01-basics/final-stack` - The final product recipe routes stdio, authenticated HTTP, compatibility, protected state, subscriptions, cancellation, shared Table, and extensions through one composition. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/sim-run/hotload-generation-lifecycle` | Operate and explain one hot generation | user, code, framework | `feature/sim-run/loadable-hot-generation` - Use the five loadable operations, separate capabilities, Shapes, Cards, and typed records.<br>`recipe/sim-run/02-scenarios/hotload-generation-lifecycle` - Verify the public lifecycle contract offline. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/solve-stiff-ode-or-index-one-dae` | Solve a stiff ODE or declared index-1 DAE | code, framework, user | `feature/sim-numbers/numeric-pipelines` - The common numeric problem, plan, trajectory, event, and registry contracts define the composition boundary.<br>`feature/sim-numbers/implicit-radau-dae` - The implicit owner supplies Radau collocation, bounded Newton, Jacobian policies, dense output, and DAE admission.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-implicit/src/tests` - The checked specimen proves order, stiff decay, scale separation, constraint retention, singular refusal, events, and work separation. |
| `route/start-a-compute-command` | Start a compute command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/compute` - The compute row owns dispatch into the loadable compute CLI and headless provider libraries.<br>`spec-test/sim-run/crates/sim-run/tests/compute` - The checked command test exercises modeled and automatic compute boot. |
| `route/start-a-glasses-plan` | Start a glasses plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/glasses` - The glasses row owns the glasses command surface and loaded glasses libraries.<br>`spec-test/sim-run/crates/sim-run/tests/glasses` - The checked command test exercises the glasses entry point. |
| `route/start-a-watch-plan` | Start a watch plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/watch` - The watch row owns the watch command surface and loaded watch libraries.<br>`spec-test/sim-run/crates/sim-run/tests/watch` - The checked command test exercises the watch entry point. |
| `route/start-an-expression-tree-command` | Start an expression tree command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the standard command runtime.<br>`feature/sim-run/expression-tree-command` - The command row selects expr-tree and delegates the complete host composition to sim-lib-expr-tree-serve.<br>`spec-test/sim-run/crates/sim-run/tests/expr_tree` - The checked command specimen exercises the standard-distribution entry point. |
| `route/start-from-sdk` | Start from the SDK | user, code | `feature/sim-sdk/facade-runtime` - The facade row owns the public command and reversible view surface.<br>`feature/sim-sdk/device-recipes` - The device row gives wearable workflows through facade-level recipes.<br>`recipe/sim-sdk/continuity/expedition-thought` - The networkless walk carries a synthetic thought by immutable content identity through route loss and process death.<br>`recipe/sim-sdk/continuity/quiet-stewardship-cards` - A second synthetic policy uses identical continuity mechanics while bounding and filtering low-disclosure cards.<br>`recipe/sim-sdk/01-basics/boot-runtime` - The boot-runtime recipe checks the smallest facade path. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/supply-guest-host-services` | Supply explicit host services to a guest runtime | code, framework | `feature/sim-runtime/guest-host-service-boundary` - The boundary keeps source, import, process, class-loading, and runtime configuration explicit and fail-closed.<br>`spec-test/sim-runtime/crates/sim-lib-exec/src/tests` - The execution specimen proves that process effects cross the structured caller-supplied ProcessPort boundary.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/conformance` - The Lua conformance specimen exercises supplied runtime configuration without ambient host inspection.<br>`spec-test/sim-runtime/crates/sim-lib-lang-python/src/lib` - The Python specimen exercises source and module access through explicit SourceAuthority and roots.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/product_specimen` - The JVM product specimen loads authorized class bytes through the checked loader surface. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/trace-parameter-boundary` | Trace a parameter boundary through folds with bounded restartable evidence | code, framework, user | `feature/sim-numbers/pseudo-arclength-continuation` - The continuation owner validates seeds, predicts and corrects pseudo-arclength points, adapts bounded steps, and records distinct fold, rank, loop, and domain events.<br>`feature/sim-numbers/root-finding` - The bordered Newton solve exposes shared residual, rank, damping, work, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-continuation/src/tests` - Checked fixtures prove circle, fold/cusp, singular correction, reversed seeds, domain exit, resumption, and bounded work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-continuation/01-basics/limiting-boundary` - The runtime recipe discovers the limiting-boundary continuation schema. |
| `route/transform-and-normalize-audio` | Stretch, pitch, measure, normalize, and lower offline PCM | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio-lift row supplies bounded phase-preserving STFT frames and COLA evidence while delegating FFT conventions and phase unwrapping to numbers-signal.<br>`feature/sim-music/offline-audio-transform-and-loudness` - The offline transform row owns phase-vocoder policy, standards-named loudness/true-peak reports, visible normalization gain, and bounded PCM16 conversion.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves duration/pitch invariants, transient resets, EBU reference loudness, intersample true peak, and visible normalization gain.<br>`spec-test/sim-music/crates/sim-lib-stream-file/src/tests` - The interchange specimen proves bounded channel mapping, seeded dither determinism, clipping reports, and routing through canonical WAV encoding. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns loss-audited score conversion, identity-bearing progression algebra, certified voice-leading paths, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies deterministic chord voicing palettes plus the pitch, scale, and tuning vocabulary used by exact transforms.<br>`spec-test/sim-music/crates/sim-lib-music-transform/src/tests/exact` - The checked exact-transform fixture compares greedy movement with a certified optimum over exact score identities. |
| `route/transform-musical-series` | Validate a finite musical series before applying total symbolic transforms | user, code, framework | `feature/sim-music/serial-series-calculus` - The serial row owns stable finite alphabet identity, symbol-bearing order, aggregate-rule data, typed failures, validation ledgers, validated ordinal and symbol bijections, total transform composition, algebra certificates, the Shape adapter, and the Lisp validation call while delegating generic permutation rank to sim-discrete.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/custom-alphabet` - The checked Lisp recipe validates and ranks an exhaustive five-symbol gesture alphabet, proving that the reusable calculus has no pitch dependency.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/certified-transforms` - The checked Rust recipe composes retrograde and rotation, applies the returned inverse, and proves exact source recovery with aggregate-preservation evidence. |
| `route/transform-signal` | Transform a real or complex signal | code, framework, user | `feature/sim-numbers/tensors` - The tensor row owns canonical complex and f64 storage shared by signal inputs and outputs.<br>`feature/sim-numbers/signal-transforms` - The signal row owns explicit transform conventions, direct references, fast Fourier plans, typed convolution/correlation geometry, guarded spectral inverse, strided tensor axes, bounded blocks, and execution reports.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/conformance` - The checked one-dimensional specimen proves impulse and tone bins, orthonormal Parseval and round trips, prime-length coverage, and fail-closed inputs.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/multidimensional_tests` - The checked multidimensional specimen proves strided direct-definition agreement, scratch admission, stable plan evidence, and blocked/in-memory equality.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/convolution_tests` - The checked convolution specimen proves direct/FFT agreement, overlap-add/save equality and reports, correlation symmetry, identity/commutativity, and finite guarded deconvolution diagnostics.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/convolution-evidence` - The checked Lisp specimen exposes automatic convolution cost selection beside the retained result. |
| `route/use-blocking-http` | Issue a bounded blocking HTTP request from a capsule-backed adapter | code, framework | `feature/sim-foundation/host-primitives` - The capsule supplies DNS, socket, clock, cancellation, and trust realization without ambient host access.<br>`feature/sim-foundation/blocking-http-client` - The shared HTTP boundary owns framing, resource caps, sensitive headers, deadlines, cancellation, and authority policy. |
| `route/use-exact-utf16-text` | Preserve, inspect, encode, or round-trip exact UTF-16 code units | code, framework | `feature/sim-foundation/exact-code-unit-text` - The neutral sim-text owner preserves paired and lone surrogate code units without depending on a guest-language string or pattern type. |
| `route/use-javascript-core-profile` | Use the bounded JavaScript core profile | code, framework | `feature/sim-runtime/javascript-core-profile` - The JavaScript profile composes the shared organs under bounded ECMAScript policy without adding an engine or host loop.<br>`spec-test/sim-runtime/crates/sim-lib-lang-javascript/src/lib` - The crate conformance suite checks the bounded profile, objects, jobs, modules, collections, JSON, text, and RegExp behavior. |
| `route/use-python-core-profile` | Use the capability-scoped Python core profile | code, framework | `feature/sim-runtime/python-authorized-library-core` - The authorized core owns source admission, supplied-root modules, diminished eval and exec, and explicit fidelity gaps.<br>`feature/sim-runtime/python-object-control-policy` - The object-control row composes checked classes, descriptors, resumable control, and managed identity over shared organs.<br>`spec-test/sim-runtime/crates/sim-lib-lang-python/src/lib` - The crate conformance suite checks the authorized Python profile and its shared-organ composition. |
| `route/use-tensor-operations` | Use tensor operations | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the storage-polymorphic runtime value, checked observation, typed tensor descriptors, broadcasting, and matrix-operation libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The checked tensor citizen specimen proves runtime projection and semantic reconstruction for the canonical Tensor value. |
| `route/use-typescript-notation` | Use TypeScript notation; does not type-check | code, framework | `feature/sim-runtime/typescript-notation-profile` - TypeScript notation; does not type-check. The profile erases admitted notation directly to the unchanged JavaScript evaluator and retains only faithful browse metadata.<br>`recipe/sim-runtime/crates/sim-lib-lang-typescript/01-basics/browsable-metadata` - The notation-plus-metadata recipe is the terminal public specimen. |
| `route/verify-ledger-reconciliation` | Verify an exact ledger reconciliation certificate | code, framework | `feature/sim-ledger/statement-admission` - Start from a cutoff-frozen canonical statement snapshot.<br>`feature/sim-ledger/exact-reconciliation` - Reload the frozen inputs and immutable human decisions, then recompute coverage and bridge arithmetic independently.<br>`spec-test/sim-ledger/crates/sim-ledger/src/reconciliation` - Mutation and adversarial fixtures prove that altered evidence, duplicate coverage, overflow, and unresolved differences fail closed. |
| `route/web-capture-to-office-evidence` | Trace an office citation to its raw web capture and policy receipt | user, code, framework | `feature/sim-office/web-evidence` - The evidence adapter binds the citation to checked capture and representation ids while retaining the policy receipt.<br>`recipe/sim-office/crates/sim-lib-doc-web/01-basics/capture-to-citation` - The offline recipe shows the capture, representation, anchor, reload, and citation path without network access.<br>`spec-test/sim-office/crates/sim-lib-doc-web/src/tests` - The checked tests prove restart stability and fail-closed behavior for tampered capture, representation, selector, index, and codec metadata. |

## Framework routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `local/sim-run/route/add-table-dir-backend` | Add a Table/Dir backend | code, framework | `feature/sim-run/index-table-dir` - The IndexDir implementation shows a runtime table exposed as an immutable directory.<br>`recipe/sim-run/02-scenarios/index-examples` - The examples recipe exercises table-backed feature-to-specimen lookup. |
| `local/sim-storage/route/add-table-dir-backend` | Add a Table or Dir backend | code, framework | `feature/sim-storage/table-dir-backends` - The backend row owns reusable Table and Dir implementations.<br>`feature/sim-storage/mounted-table-dir-namespace` - The mounted namespace row composes independently owned backends without copying their storage behavior.<br>`feature/sim-storage/contract-emitter` - The emitter row keeps generated storage contract evidence current.<br>`recipe/sim-storage/crates/sim-table-mount/01-basics/backend-composition` - The backend-composition recipe exercises the mounted Table/Dir surface. |
| `route/accept-estate-controller` | Prove the bounded estate controller before a reviewed physical placement | code, framework | `feature/sim-estate/controller-acceptance` - Exercise explicit non-dispatch and controller-loss boundaries without selecting a private target or performing the deferred MIALOVE pilot.<br>`spec-test/sim-estate/crates/sim-lib-estate/src/controller_acceptance_tests` - The focused model specimen proves retry is legal only before dispatch and ambiguity enters quarantine. |
| `route/add-a-codec` | Add a codec | code, framework | `feature/sim-codecs/codec` - Start with the shared codec contract and position rules.<br>`feature/sim-codecs/contract-emitter` - The emitter row keeps generated codec contract evidence current. |
| `route/add-a-language` | Add a language | code, framework | `feature/sim-codecs/codec` - The codec row owns shared positions, limits, and loadable codec contracts.<br>`feature/sim-codecs/expression-syntax-grammars` - Expression syntax rows expose language and grammar subjects with round-trip contracts.<br>`feature/sim-codecs/pratt` - Operator-shaped languages reuse the Pratt syntax surface. |
| `route/add-a-packet-codec` | Add a packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire grammar row groups packet and byte-level protocol contracts. |
| `route/add-a-search-wire-site` | Add a search wire codec site | code, framework | `feature/sim-codecs/codec` - Begin with the shared bounded codec contract and wire-syntax ownership rules.<br>`feature/sim-codecs/searxng-search-wire` - The SearXNG row demonstrates one transport-free SearchWireCodec site without becoming the search architecture.<br>`recipe/sim-codecs/crates/sim-codec-search-searxng/01-basics/request-response` - The checked pure recipe supplies request and saved-response bytes without opening a socket. |
| `route/add-automotive-workflow` | Add an automotive diagnostic or service workflow | code, framework, user | `feature/sim-auto/automotive-runtime` - The automotive runtime row owns the shared contracts, bounded lanes, codec, and view integration.<br>`spec-test/sim-auto/crates/sim-lib-auto-order/tests/conformance` - The conformance specimen checks the order boundary through the public runtime contract. |
| `route/add-bounded-search-engine` | Add a bounded search engine | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns reusable algorithmic helpers.<br>`feature/sim-discrete/bounded-search` - The bounded-search row owns deterministic state-space search policy, bounds, receipts, and specimens. |
| `route/add-bridge-packet-codec` | Add a Bridge packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar. |
| `route/add-capability-gated-read-eval` | Add capability-gated read-eval | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core row owns read policy, capability checks, and read-construct evidence.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/read_construct` - The read-construct test checks object round trips through standard core. |
| `route/add-citizen-read-construct` | Add a citizen read-construct | code, framework | `feature/sim-citizen/citizen-read-constructs` - The citizen row owns runtime members and read-construct projection.<br>`spec-test/sim-citizen/crates/sim-citizen/src/conformance` - The conformance test checks citizen round-trip behavior. |
| `route/add-cookbook-recipe` | Add a cookbook recipe | code, framework | `feature/sim-foundation/cookbook` - The cookbook row owns reusable recipe metadata. |
| `route/add-dataflow-analysis` | Add a proof-producing dataflow analysis | code, framework | `feature/sim-runtime/incremental-query-core` - The core owns the one generic worklist, lattice admission, widening, continuations, explanations, incremental reuse, and completion proofs; callers contribute only domain transfer rules.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/definite_assignment_specimen` - The forward definite-assignment specimen covers branches, loops, unreachable code, exceptional edges, diagnostics, and exact incremental invalidation.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/range_analysis_specimen` - The backward range specimen proves explicit fingerprinted widening, typed budget exhaustion, resumability, explanations, and deterministic proof identity.<br>`spec-test/sim-runtime/crates/sim-incremental-core/tests/ownership_guard` - The structural guard rejects a second memoized worklist while explicitly classifying domain transfer rules and unrelated algorithms. |
| `route/add-decoded-instruction-machine` | Add a bounded decoded-instruction machine | code, framework | `feature/sim-runtime/machine-contract` - The machine row owns generic slots, operands, frames, the iterative driver, bounded control transfer, safepoints, interruption, and exact receipts while leaving instruction semantics to the guest.<br>`spec-test/sim-runtime/crates/sim-lib-machine/tests/neutral_machine_specimens` - The stack-calculator and register-file specimens share the same driver, permits, and receipts while proving that the machine contract does not select one storage discipline.<br>`spec-test/sim-runtime/crates/sim-lib-machine/tests/ownership_guard` - The source-fact ownership fixture rejects a guest frame stack and recursive driver but admits guest-owned opcodes and instruction semantics. |
| `route/add-discrete-domain-helper` | Add a discrete domain helper | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete row owns algebra, certified rectangular assignment, staged paths, edit/DTW alignment, graph paths, ranking, and spectral helpers. |
| `route/add-eval-fabric-site` | Add an EvalFabric site | code, framework | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns runner and fleet placement libraries. |
| `route/add-femm-domain-helper` | Add a FEMM domain helper | code, framework | `feature/sim-femm/femm-domain-stack` - The FEMM row owns mesh, field, material, solve, and ODE helpers. |
| `route/add-femm-linear-solver` | Add a FEMM linear solver | code, framework | `feature/sim-femm/linear-solvers` - The linear solver row owns solver contracts, factor handles, iterative kernels, and certificate evidence.<br>`spec-test/sim-femm/crates/sim-lib-femm-solve/src/implementation/tests` - The checked implementation specimen proves solver reuse metadata and residual evidence for certificates. |
| `route/add-finite-enumeration-adapter` | Add a finite enumeration adapter | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns combinatorics and rank integration.<br>`feature/sim-discrete/finite-enumeration` - The finite enumeration row owns lazy words, digit/radix adapters, cyclic canonicalization, and longest-only selection. |
| `route/add-generated-doc` | Add a generated doc | code, framework | `feature/sim-tooling/generated-docs` - The xtask documentation row owns generated doc lanes and index fragments. |
| `route/add-guest-language-profile` | Add a guest language profile | code, framework | `feature/sim-runtime/guest-language-profiles` - The profile row establishes the checked thin-profile ownership boundary before any guest-specific work and points to Lua as the no-compiler reference.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/tests` - The shared profile specimen fails closed unless reader, lowering, eval policy, organ reuse, capabilities, and gaps are explicit.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/conformance` - The Lua source-codec specimen proves direct source parsing and lowering through the positive reference profile.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/lib` - The Lua profile and matrix-row tests are the final shared-matrix registration specimen for a guest profile. |
| `route/add-hardware-host-surface` | Add a hardware host surface | code, framework | `feature/sim-stream-host/hardware-host-surfaces` - The host row owns placement, device adapters, and hardware-facing stream surfaces.<br>`feature/sim-stream-host/read-first-device-sessions` - Choose observation-only or registered effect authority before exposing the provider. |
| `route/add-host-exec-primitive` | Add a host exec primitive | code, framework | `feature/sim-runtime/host-exec` - The exec row keeps host process behavior in a loadable library. |
| `route/add-host-primitive` | Add a host primitive | code, framework | `feature/sim-foundation/host-primitives` - Neutral port cards, open identities, runtime-mechanics refusals, budgets, and lexical bindings belong in sim-host-core; concrete host realization belongs in platform and domain libraries. |
| `route/add-host-storage-primitive` | Add a host storage primitive | code, framework | `feature/sim-storage/host-storage-primitives` - The host storage row points to file, database, and HTTP backends. |
| `route/add-http-search-site` | Add a provider-neutral HTTP search site | user, code, framework | `feature/sim-agent-net/provider-neutral-http-search` - Configure the codec behind the bounded shared HTTP skill transport.<br>`recipe/sim-agent-net/crates/sim-lib-search-http/01-basics/fixture-site` - The fixture proves live and cassette execution without a provider-specific transport. |
| `route/add-incremental-query-organ` | Add an incremental query organ | code, framework | `feature/sim-runtime/incremental-query-core` - The core row owns the dependency-light memo graph and cutoff algorithm.<br>`feature/sim-runtime/incremental-query-organ` - The organ row owns SIM conversion, capability gates, shapes, reports, immutable completed-analysis projection, and loadable exports.<br>`spec-test/sim-runtime/crates/sim-lib-incremental/src/tests` - The checked tests exercise registration, invalidation, verification, cutoff, reports, Card projection, and proof-backed analysis browsing. |
| `route/add-language-neutral-class-semantics` | Add checked language-neutral class semantics | code, framework | `feature/sim-runtime/class-semantics` - The class row owns checked descriptors, bounded lineage policies, kernel Class projection, and managed non-retaining derived caches.<br>`recipe/sim-runtime/crates/sim-lib-class/01-basics/neutral-class` - The terminal recipe covers a hand-authored diamond order, typed failures, clearing-receipt-backed cache reclamation, member Shape browsing, and read construction without a guest-language type.<br>`spec-test/sim-runtime/crates/sim-lib-class/tests/neutral_class_specimens` - The runnable integration specimen supplies the exact characterization outcomes and source-level ownership proof. |
| `route/add-model-facing-packet-workflow` | Add a model-facing packet workflow | code, framework | `feature/sim-agent-net/bridge-runtime` - The workflow row ties agent model exchange to BRIDGE packet records. |
| `route/add-number-domain` | Add a number domain | code, framework | `feature/sim-numbers/numbers` - The number row owns reusable number domains and their runtime libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The tensor citizen test checks runtime projection for a number domain. |
| `route/add-relation-provider` | Add a logical relation provider | code, framework | `feature/sim-storage/bounded-relation-site` - The site owns placement, checked provider traits, bounds, capabilities, effects, transactions, and receipts.<br>`spec-test/sim-storage/crates/sim-relation-site/src/tests` - The recording provider proves the effect seam without implementing SQL or relational evaluation. |
| `route/add-resident-femm-csr-solver` | Add a resident FEMM CSR solver | code, framework | `feature/sim-compute/femm-resident-solvers` - The compute FEMM row owns resident CSR upload, Krylov execution, f64 refinement, and loadable solver export.<br>`recipe/sim-compute/crates/sim-lib-compute-femm/01-basics/resident-csr-solve` - The recipe shows upload reuse, resident work vectors, scalar synchronization, and f64 certificate acceptance. |
| `route/add-runtime-organ` | Add a runtime organ | code, framework | `feature/sim-runtime/organs` - The organ row groups reusable binding, control, logic, pattern, incremental, mutation, namespace, and sequence behavior.<br>`feature/sim-runtime/binding-organ` - The binding row owns lexical cells and binding mechanics shared by guest profiles.<br>`feature/sim-runtime/function-organ` - The function row owns typed managed instances and collector-visible capture composition.<br>`feature/sim-runtime/control-organ` - The control row owns resumable frames and control transfer shared by guest profiles.<br>`feature/sim-runtime/property-mechanics` - The property row owns descriptor dispatch and reusable property semantics.<br>`feature/sim-runtime/namespace-organ` - The namespace row owns bounded module resolution and caller-supplied source roots.<br>`feature/sim-runtime/contract-emitter` - The emitter row keeps generated runtime contract evidence current.<br>`recipe/sim-runtime/crates/sim-lib-function/01-basics/neutral-language` - The terminal checked specimen composes typed lexical capture, positional and named inputs, browsing, managed-cycle collection, and optional generic dispatch without importing an established guest runtime. |
| `route/add-server-backed-web-session` | Add a server-backed web session | code, framework | `feature/sim-web/codec-surface-sessions` - The bridge session owns reversible surface encode, decode, commit, and pane isolation.<br>`feature/sim-web/server-backed-web-sessions` - RemoteTransport composes that session with sim-lib-server transport negotiation and eval request frames.<br>`spec-test/sim-web/crates/sim-lib-web-bridge/src/remote_tests` - The remote transport test performs real in-process server round trips, reconnect, close, and stale-revision failure. |
| `route/add-shape-contract` | Add a Shape contract | code, framework | `feature/sim-shape/shape` - The Shape row owns matching, binding, and citizen projection.<br>`feature/sim-shape/contract-emitter` - The emitter row keeps Shape contract evidence current.<br>`spec-test/sim-shape/src/citizen/inventory` - The inventory test checks the exported shape citizen surface. |
| `route/add-stream-protocol-helper` | Add a stream protocol helper | code, framework | `feature/sim-stream/stream-protocol-helpers` - The stream row owns reusable stream, rank, topology, and device helpers.<br>`spec-test/sim-stream/crates/sim-lib-stream-core/src/read_construct` - The read-construct test checks stream metadata projection. |
| `route/add-table-dir-core` | Add a Table or Dir core contract | code, framework | `feature/sim-foundation/table-dir-core` - The table core row owns shared Table and Dir contracts.<br>`feature/sim-foundation/table-path-references` - The path reference row owns escaped absolute and relative Table/Dir path parsing, normalization, and resolution. |
| `route/add-value-helper` | Add or reuse a value helper, expression field reader, or map field access helper | code, framework | `feature/sim-foundation/value-helpers` - The value row owns shared runtime value conversion, expression builder, expression field reader, map field reader, and field access helpers. |
| `route/admit-bank-statement` | Admit a bank statement for reconciliation | code, framework | `feature/sim-ledger/statement-admission` - The ledger-owned profile contract turns declared delimited layouts into exact canonical rows.<br>`spec-test/sim-ledger/crates/sim-ledger/src/statement` - The checked specimen proves two synthetic layouts have identical canonical values and typed refusals stay outside the snapshot. |
| `route/admit-dynamic-source` | Admit dynamic guest source without copying authority | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core owner keeps trusted read policy, required caller powers, and diminished allowed powers in one host-built SourceAuthority.<br>`feature/sim-sdk/source-authority-facade` - The SDK presents the canonical authority, request, broker, and dynamic-policy builders directly without a facade envelope.<br>`recipe/sim-sdk/source-authority/dynamic-text` - The public Rust recipe constructs authority through the SDK and proves admitted and missing-caller-power outcomes through the shared broker.<br>`spec-test/sim-runtime/crates/sim-lib-core/src/source_authority_ownership_tests` - The source-fact guard permits guest semantic wrappers, rejects repeated authority fields and generic admission types, and classifies unwired guests without adding behavior. |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/adopt-and-subtract-stewardship-composition` | Review, adopt, rebuild, and make a stewardship composition smaller | user, framework | `feature/sim-sdk/stewardship-adoption-subtraction` - Use the generic transaction and subtraction discipline without embedding household choices in public fixtures.<br>`recipe/sim-sdk/stewardship/adopt-subtract-composition` - Inspect the data-only composition, complete preview, rebuild inputs, tombstones, and declined mutual-porch outcome.<br>`spec-test/sim-sdk/tests/validate_adopt_subtract_composition` - Prove ephemeral-state-independent rebuild and absence of retired parts from every surface and replay. |
| `route/advance-bounded-roadmap-execution` | Safely advance, interrupt, replay, and resume one grounded roadmap leaf | framework | `feature/sim-agent-net/bounded-roadmap-runner-service` - The service pins every effect-bearing identity, delegates readiness, journals intent before adapter use, and admits receipts only through the pure reducer.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/tests` - The deterministic service fixture proves direct and replayed observations produce identical state and rejects package identity replacement before another effect. |
| `route/agent-reproducible-instrument-adoption` | Adopt an instrument proposed through conversation without retaining the conversation | code, framework | `feature/sim-agent-net/reproducible-instrument-adoption` - Freeze intent and coordinate the indivisible local adoption transaction.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/atelier/instrument_adoption_tests` - Exercise the conversation-free adoption transaction with frozen recipe inputs and rollback evidence. |
| `route/analyze-counterpoint` | Analyze counterpoint and bounded stretto relations | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Counterpoint and identity-bearing Staff conversion plus audited transform operations.<br>`feature/sim-music/exact-score-consonance` - The consonance row supplies exact half-open sounding-window alignment without collapsing voice or event identity.<br>`feature/sim-music/music-counterpoint` - The counterpoint row applies inspectable rule data to existing material and builds bounded graph-backed stretto relations while keeping analysis distinct from generation.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/stretto-graph` - The checked Lisp specimen exposes candidate entries, compatible couples, maximal cliques, and normalized chains with generation=false. |
| `route/analyze-metrical-patterns` | Quantize, compare, and discover repeated musical patterns | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - Music analysis supplies exact tempo/meter lattices, named feature extractors, invariance transforms, identity-bearing occurrences, support, and overlap policy while delegating DTW/search to sim-discrete and correlation to sim-numbers.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/metrical-similarity-pattern` - The checked descriptor states the straight/swing/tuplet quantization, DTW/correlation similarity, and bounded exact-verification contract. |
| `route/analyze-tonnetz-path` | Find a bounded neo-Riemannian path between canonical chords | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch-set owner supplies canonical masks and typed set relations; the chord owner supplies rooted chord identity; the existing Riemann namer supplies projection-only labels.<br>`feature/sim-music/exact-music-analysis-and-transform` - Music analysis applies enabled P/L/R generators over canonical major/minor identities and delegates deterministic shortest-path certificates to sim-lib-discrete-graph.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/tonnetz-path` - The checked descriptor gives the concrete C-major to A-minor call, explicit move set and bound, identity policy, one-edge R result, and reusable certificate contract. |
| `route/analyze-twelve-tone-row` | Validate, transform, and inspect a twelve-tone row without losing aliases or label conventions | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary supplies the canonical PitchClass identity; unordered PitchClassMask and pitch-set analysis stay in their existing owners.<br>`feature/sim-music/serial-series-calculus` - The generic serial layer supplies stable finite alphabet identity and exhaustive exactly-once aggregate validation without assuming pitch.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict ToneRow values, total normalized P/I/R/RI operations, alias-preserving RowFamilySet values, convention-explicit RowMatrix data and ASCII projection, operation-bearing RowForm values, explicit label conventions, and the narrow pitch/ToneRow Shape adapter.<br>`recipe/sim-music/crates/sim-lib-pitch-serial/01-basics/row-family-matrix` - The checked Rust scenario proves all 48 Op. 25 aliases against their operations, retains symmetry aliases across distinct-form reduction, and projects one source-and-convention-bearing matrix to structured coordinates and ASCII. |
| `route/assemble-agent-kind` | Assemble and run a different agent kind | user, code, framework | `feature/sim-agent-net/genai-assembly` - The existing GenAI assembly supplies the checked model exchange and late-bound provider placement.<br>`feature/sim-agent-net/manifest-selected-conduct` - AgentManifest binds roles, components, provider seats, authority, budgets, and the selected conduct independently.<br>`feature/sim-agent-net/agent-conduct` - Choose ReAct, plan-act-replan, phased, verify-retry, router-crew, or another certified data-only conduct and inspect its reflected contract before running it.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/react` - The checked ReAct specimen runs a catalog conduct without a kind-specific Rust loop.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/standard_steps` - The executable specification proves the standard Cards and typed validation boundary used by every catalog kind. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/audify-measured-series` | Hear and creatively transform measured evidence without category fraud or route lock-in | user, code, framework | `feature/sim-music/daw-session-runtime` - The session owner records reversible or explicitly lossy audification, disjoint artistic-mapping provenance, stable content identity, and append-only route substitutions.<br>`feature/sim-music/audio-lift-and-render` - The audio owner supplies bounded analysis and deterministic rendering without owning scientific meaning.<br>`spec-test/sim-music/crates/sim-lib-daw-session/src/listening` - The listening specimens prove route churn leaves musical and expedition identity stable, mapping families cannot decode as one another, all named target substitutions are typed, and unsupported targets retain exact evidence. |
| `route/audit-analytic-mechanical-impact` | Audit a mechanical contact impulse with momentum and work equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite retains one-sided impact states, an explicit impulse, independent kinetic stores, clean selection inputs, and momentum/work identities.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common one-sided event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for momentum, work, provider-envelope, and modal-root proofs. |
| `route/audit-analytic-switched-electrical-study` | Audit a switched lumped electrical study against independent equations | code, framework, user | `feature/sim-physics/self-contained-analytic-studies` - The cross-layer integration suite proves two oriented voltage/current ports, a discontinuity, terminal event, stiff interval, independent stores, refinement contraction, and a certified threshold.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - Check the common event/topology contract, then run the repository's analytic_studies and analytic_study_routes targets for the public equation-derived composition and metamorphic cases. |
| `route/audit-conjugate-port-work` | Audit signed continuous and impulsive work across physical boundaries | code, framework, user | `feature/sim-physics/boundary-event-core` - Declare the boundary and crossing port identities.<br>`feature/sim-physics/conjugate-port-work` - Pair effort and flow, orient the port, split continuous work at events, and retain impulses separately.<br>`spec-test/sim-physics/crates/sim-lib-physics-power/tests/work` - The specimen proves analytic domains, orientation, discontinuities, simultaneous events, and pure impulses.<br>`recipe/sim-physics/01-power/switched-two-port` - Run a switched electrical-to-mechanical two-port audit. |
| `route/audit-independent-stored-energy` | Audit stored-energy balance without deriving endpoint stores from transferred work | code, framework, user | `feature/sim-physics/conjugate-port-work` - Retain signed continuous work and event transfers at the declared boundary.<br>`feature/sim-physics/stored-energy-audit` - Evaluate both endpoint states constitutively and compute the visible balance with typed evidence lanes.<br>`spec-test/sim-physics/crates/sim-lib-physics-audit/tests/audit` - The specimen proves independent perturbations, sign and omission failures, distinct residuals, and separated uncertainty.<br>`recipe/sim-physics/02-audit/stored-energy` - Inspect the equation, Shapes, and runtime exports. |
| `route/audit-platform-membrane` | Audit every platform membrane realization | code, framework | `feature/sim-platform/product-membrane` - Begin with the permanent classification, closure, evidence, and ownership gate.<br>`feature/sim-platform/construction-integration-realization` - Check construction integration remains a capsule realization, not domain ownership.<br>`feature/sim-platform/device-physical-port` - Check physical device mechanics remain behind the shared device port.<br>`feature/sim-platform/native-midi-realization` - Check native MIDI mechanics remain behind the music-owned contract.<br>`feature/sim-platform/process-realization` - Check modeled and physical process providers share the runtime-owned port.<br>`feature/sim-platform/web-shell-realization` - Check native web hosting does not absorb presentation policy.<br>`feature/sim-platform/native-compute-probing` - Check hardware probing is injected into compute-owned executors.<br>`feature/sim-platform/native-media-realization` - Check audio and plugin mechanics remain behind media-owned ports.<br>`feature/sim-platform/host-directory-realization` - Check host paths remain confined behind the storage-owned port.<br>`feature/sim-platform/ledger-command-realization` - Check ledger composition consumes only preopened storage and clock services.<br>`feature/sim-platform/macos-capsule` - Verify the macOS Card and cross-build evidence independently.<br>`feature/sim-platform/windows-capsule` - Verify the Windows Card and cross-build evidence independently.<br>`feature/sim-platform/android-capsule` - Verify the Android Card, shell boundary, and target attestations independently.<br>`feature/sim-platform/ios-capsule` - Verify the iOS Card, shell boundary, and target attestations independently.<br>`feature/sim-platform/wasi-capsule` - Verify the declared WASI import surface and modeled/hosted evidence independently.<br>`feature/sim-platform/ubuntu-rpi-capsule` - Keep cross-build and physical Raspberry Pi support grades distinct.<br>`feature/sim-platform/amazfit-proxy-capsule` - Keep the bounded watch proxy separate from Android capsule ownership.<br>`feature/sim-platform/halo-proxy-capsule` - Keep the official-contract glasses proxy separate from its host capsule. |
| `route/audit-search-provenance` | Audit a search result from final rank to immutable source evidence | user, code, framework | `feature/sim-web/search-audit-surface` - The offline surface distinguishes unverified provider claims from selector-checked quotations and exposes ranking, fetch, robots, fidelity, and policy provenance.<br>`recipe/sim-web/crates/sim-lib-view-search/01-basics/offline-audit` - The checked recipe demonstrates a complete audit projection without network authority. |
| `route/author-agent-conduct` | Author an agent conduct control loop | user, code, framework | `feature/sim-agent-net/agent-conduct-records` - The core records define the stable frame, step Card, budget, journal, replay, and fork vocabulary every conduct uses.<br>`feature/sim-agent-net/agent-conduct-topology-profile` - The topology profile certifies data-only conduct packages and binds their Cards through the shared topology engine.<br>`feature/sim-agent-net/agent-conduct` - The agent/conduct-v1 profile validates RunFrame Shapes, step Cards, outcome routing, roles, authority, budgets, terminal admission, and embedded tests.<br>`recipe/sim-agent-net/crates/sim-lib-agent-conduct/01-basics/echo-conduct` - The checked recipe demonstrates a minimal authored topology package loaded and executed through the shared engine.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/third-party-step` - The checked extension specimen adds a Card and implementation without editing conduct or topology source.<br>`recipe/sim-agent-net/crates/sim-lib-agent/50-conducts/router-crew` - The data-only triage and routing family proves a nontrivial authored graph remains catalog data rather than baked-in control flow. |
| `route/author-estate-exposure` | Author a safe estate exposure | code, framework | `feature/sim-estate/exposure-authoring` - The closed compiler admits only reviewed templates and shaped values.<br>`feature/sim-estate/model-provider-conformance` - The deterministic provider proves lifecycle behavior before physical placement.<br>`feature/sim-estate/sealed-command-site` - The command site preserves the closed declaration at the process boundary.<br>`feature/sim-estate/ansible-provider-site` - The Ansible decoration adds sanitized discovery and durable typed callback evidence. |
| `route/author-platform-provider` | Author a platform provider | code, framework | `feature/sim-platform/platform-owner` - The sole owner validates capsule identity, declared services and shells, canonical bootstrap use, and product-closure isolation before any real provider can be claimed.<br>`feature/sim-platform/bootstrap-rind` - The bounded rind admits only the content-bound adjacent capsule through the stable native ABI.<br>`feature/sim-platform/platform-policy` - The pure resolver and modeled site provide the reusable service-selection and deterministic conformance surface. |
| `route/bind-relational-domain` | Bind a relational value through Shape | code, framework | `feature/sim-shape/relation-shape` - The relation adapter resolves DomainSpec Shape refs through Cx and owns cell/row binding.<br>`feature/sim-shape/shape` - The shared Shape protocol remains the only matcher and binder.<br>`spec-test/sim-shape/crates/sim-relation-shape/src/tests` - The checked specimen binds a custom UUID-over-text domain and rejects a boolean mismatch. |
| `route/build-evidence-bearing-web-search` | Build provider-neutral web search with exact citation evidence | code, framework | `feature/sim-foundation/web-evidence-core` - The web row owns raw and normalized identities, exact selectors, and complete typed policy receipts.<br>`feature/sim-foundation/search-evidence-core` - The search row keeps provider claims distinct from observations and checked citations while exposing a pure wire boundary.<br>`recipe/sim-foundation/crates/sim-lib-search-core/01-basics/checked-citation` - The network-free checked specimen terminates at a provider claim beside an exact representation-bound citation. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-model-test-pack` | Build a reproducible model-test task pack | code, framework | `feature/sim-agent-net/model-test-packs` - The pack owner rebuilds pinned public fixtures, seals immutable epochs, and keeps private bytes behind caller-supplied control-plane loaders.<br>`spec-test/sim-agent-net/crates/sim-lib-model-test-packs/src/tests` - The checked pack specimen covers deterministic reconstruction, generated target/control pairs, private-loader boundaries, and sealed evaluator evidence. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/algorithm-foundry` - The foundry row composes the complete performance plan without baking stage providers into the application.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/capture-web-evidence` | Capture and replay immutable web evidence | user, framework | `feature/sim-agent-net/immutable-web-capture` - The web/fetch owner composes legacy retrieval with explicit policy and immutable evidence.<br>`recipe/sim-agent-net/crates/sim-lib-web-fetch/01-basics/offline-capture` - The network-free fixture proves identical offline replay and versioned re-extraction. |
| `route/carry-a-wasm-library` | Carry a wasm library across an explicit host boundary | code, framework | `feature/sim-codecs/wasm-abi-contract` - The ABI row owns inert manifests, byte frames, imports, and exports while the selected platform loader retains host authority.<br>`spec-test/sim-codecs/crates/sim-wasm-abi/src/tests/roundtrip` - The checked round-trip specimen proves ABI records remain pure data.<br>`spec-test/sim-codecs/crates/sim-codec-compare/src/platform_roundtrip_tests` - The cross-platform specimen proves the same contract survives platform codec projection. |
| `route/certify-physics-refinement` | Distinguish numerical convergence from a certified mathematical-model verdict | code, framework, user | `feature/sim-physics/stored-energy-audit` - Retain independent audit evidence lanes.<br>`feature/sim-physics/certified-orthogonal-refinement` - Refine each assumption explicitly and require a sealed enclosure for definite claims; its crate integration tests prove sign reversal, non-monotonicity, independent failures, refusal of estimates, continuation diagnoses, and reproducible identity.<br>`recipe/sim-physics/03-proof/certified-refinement` - Inspect event-exact meshing and runtime exports. |
| `route/characterize-a-refactor` | Capture behavior before a refactor and compare it afterward | code, framework | `feature/sim-runtime/characterization-captures` - Standard core owns bounded canonical captures, content identity, strict two-sided comparison, and located differences.<br>`feature/sim-sdk/characterization-facade` - The SDK presents the public migration types without exposing standard-core internals.<br>`recipe/sim-runtime/crates/sim-lib-standard-core/01-basics/characterize-migration` - The public recipe states the capture-before, refactor, capture-after, strict-compare contract.<br>`spec-test/sim-sdk/tests/characterization` - The downstream test proves identical captures and a located mismatch through SDK-visible types. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/collect-language-neutral-managed-graph` | Collect a language-neutral managed graph | code, framework | `feature/sim-runtime/mutation-organ` - The mutation row supplies allocation-deterministic managed ids, graph storage, roots, weak edges, ephemerons, and safepoints without selecting a guest language.<br>`feature/sim-runtime/gc-tracing` - The collector row supplies bounded synchronous tracing, atomic sweep, weak clearing, and isolated finalization admission without claiming language behavior.<br>`spec-test/sim-runtime/crates/sim-lib-gc-tracing/src/tests` - The checked non-language managed-graph specimen compares generated graphs and safepoint schedules with a model tracer and proves wasm-closure collection. |
| `route/compare-cross-language-callables` | Compare Python, JavaScript, and Lua callable and object-model semantics | code, framework | `feature/sim-runtime/function-organ` - The function organ supplies the neutral kernel Callable projection and browse Shapes shared by all three guests.<br>`feature/sim-runtime/class-semantics` - The class organ owns declared-parent lineage while explicitly excluding prototype and __index delegation.<br>`feature/sim-runtime/python-object-control-policy` - Python projects its declared superclass graph through the class protocol.<br>`feature/sim-runtime/javascript-core-profile` - JavaScript retains prototype lookup as guest object policy rather than declared lineage.<br>`feature/sim-runtime/guest-language-profiles` - Lua retains metatable __index lookup as guest policy rather than declared lineage.<br>`recipe/sim-runtime/crates/sim-lib-lang-matrix/01-basics/cross-language-callable` - The terminal checked recipe browses and calls all three functions, compares canonical outcomes, and proves that only Python answers the declared-parent query. |
| `route/compile-agent-intent` | Compile agent intent | code, framework | `feature/sim-agent-net/forge-intent` - The Forge row owns checked intent compilation.<br>`feature/sim-agent-net/agent-server-tools` - Server tool rows expose the host-facing model gateway. |
| `route/complete-exact-score-consonance` | Complete an exact score with reversible consonance additions | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns identity-bearing Staff values and the generic additive staff apply/remove seam.<br>`feature/sim-music/exact-score-consonance` - The evaluation row supplies exact sounding windows and separately inspectable named metrics.<br>`feature/sim-music/reversible-consonance-completion` - The completion row composes typed additions, constraints, reports, content binding, changed windows, provenance, and exact inversion while delegating deterministic traversal, bounds, interrupts, and receipts to sim-lib-discrete-search.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/additive-consonance-completion` - The checked descriptor and paired Rust specimen show the bounded reversible completion contract. |
| `route/complete-serial-consonance` | Complete a realized serial plan with bounded reversible additions | user, code, framework | `feature/sim-music/serial-practice` - The serial row owns the immutable plan, stable event identities, strict realization, and the structural-versus-all-sounding reading boundary that the adapter must preserve.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row supplies the optional modal-projection evidence that the completion adapter can admit explicitly without pretending the result remains strict chromatic output.<br>`feature/sim-music/exact-score-consonance` - The exact score row establishes the identity-bearing staff surface and exact sounding timeline that additive completion must preserve.<br>`feature/sim-music/reversible-consonance-completion` - The reversible completion row defines the generic additive-search contract that the serial row narrows through serial-specific legality and post-completion ledgers.<br>`feature/sim-music/serial-consonance-completion` - The serial completion row filters generic candidates through serial allowances, leaves the structural plan unchanged, delegates deterministic finite traversal, work/result bounds, cancellation, and honest receipts to sim-lib-discrete-search, and returns structural plus all-sounding ledgers beside the reversible patch result.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/serial-consonance-completion` - The checked Rust scenario completes one realized row statement with a stated pitch-class addition, proves the structural ledger is unchanged, and records the added material only in the all-sounding ledger. |
| `route/compose-calm-flock-care-pack` | Care for a flock without measuring its worth | user, framework | `feature/sim-sdk/calm-flock-care-pack` - Compose flock identities, common care, observation, supply, attention, factual handoff, and copied claims as bounded data.<br>`recipe/sim-sdk/stewardship/calm-flock-care` - Exercise a single calm flock-level capture for eight synthetic birds with an isolated kitchen claim.<br>`spec-test/sim-sdk/tests/validate_flock_pack` - Prove the twenty-second budget, unknown-on-missing rule, factual handoff boundary, and absence of scoring, culling, diagnosis, treatment, and automated care. |
| `route/compose-chosen-media-current-references` | Keep only chosen media traces and current cited references | user, framework | `feature/sim-sdk/chosen-media-current-reference-packs` - Compose selected imports, authored meanings, local search, citation currency, and explicit removal as bounded data.<br>`recipe/sim-sdk/stewardship/chosen-media-traces` - Import and remove one selected synthetic trace with every remote and inference lane absent.<br>`spec-test/sim-sdk/tests/validate_media_reference_packs` - Prove source-policy excerpts, predicate separation, guidance metadata, removal semantics, and networkless operation. |
| `route/compose-cited-seasonal-garden-pack` | Rebuild a garden season without predicting or commanding work | user, framework | `feature/sim-sdk/cited-seasonal-garden-pack` - Compose source-bounded recommendations, optional projections, exact observations, and questions as data while Mia retains authority.<br>`recipe/sim-sdk/stewardship/cited-seasonal-garden` - Rebuild a season and add a bed and crop entirely through data while all optional provider lanes remain absent.<br>`spec-test/sim-sdk/tests/validate_garden_pack` - Prove independent Shapes, predicate separation, expiry behavior, calendar non-authority, the harvest claim boundary, and networkless operation. |
| `route/compose-content-addressed-capability-pack` | Compose a room as a checked content-addressed runtime closure | framework | `feature/sim-agent-net/content-addressed-capability-packs` - PackDir lookup, deterministic graph resolution, diminishing ceilings, complete preflight, and separated loading form the reusable composition boundary.<br>`spec-test/sim-agent-net/crates/sim-lib-capability-pack/src/tests` - Two independent roots share one deterministic import while cycle and authority-widening fixtures refuse before load. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-independent-atelier-room` | Compose one independent intellectual room | user, framework | `feature/sim-sdk/atelier-room-packs` - Select a data profile whose own route and authority ceiling compose the existing domain owner through the canonical pack graph.<br>`spec-test/sim-sdk/tests/validate_atelier_packs` - The offline specimen validates all six closures and their room-specific forbidden operations, including declined porch success and the data-only toy extension. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/compose-jvm-from-sdk` | Compose the bounded JVM from the SDK | user, code, framework | `feature/sim-sdk/jvm-composition` - One feature exposes the existing decoder, authority, profile, invocation, and specimen contracts without a facade-owned runtime.<br>`recipe/sim-sdk/jvm/product-surface` - The public recipe imports every JVM composition contract through sim alone.<br>`recipe/sim-sdk/jvm/lambda-interop` - The public recipe reaches both lambda interop directions through sim alone.<br>`spec-test/sim-sdk/tests/jvm_exports` - The downstream-only test proves every JVM entry is reachable through the SDK. |
| `route/compose-model-observatory-from-sdk` | Compose the provider-neutral model observatory from the SDK | user, code, framework | `feature/sim-sdk/model-test-facade` - The granular feature exposes the canonical observatory without changing the minimal facade.<br>`feature/sim-agent-net/model-test-product` - The owning product supplies command semantics, offline projections, and deterministic evidence export. |
| `route/compose-music-algorithms-from-sdk` | Compose the modular music algorithm foundry from the SDK | user, code, framework | `feature/sim-sdk/music-algorithm-composition` - The SDK row owns focused feature selection and curated re-exports over the frozen candidates while the music owner keeps the open data-plan registry and replaceable foundry stages.<br>`recipe/sim-sdk/music-algorithms/foundry-plan` - The Lisp load recipe reproduces the bounded foundry plan through the five focused candidate crates.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/music_algorithms` - The Rust facade specimen checks the same transform, search, ratio, consonance, counterpoint, and plan bounds. |
| `route/compose-music-carpet` | Compose and relatively encode a finite music carpet | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical Music, rational time, audited conversions, and reusable transform chains; carpet addressing calls the shared discrete-rank crate rather than implementing rank locally.<br>`feature/sim-music/carpet-composition` - The carpet row owns finite axes, sparse cells, explicit layout policy, shared mixed-radix addressing, algebraic layout operations, and relative pitch/time forms.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/carpet_conformance` - The checked algebraic fixtures prove mixed-radix rank parity, explicit boundary policy, rotations, reflections, slices, overlays, and transform-chain composition.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/relative_conformance` - The checked conversion fixtures prove exact retained-origin round trips and named losses for omitted origins and source structure. |
| `route/compose-offline-realtime-audio` | Compose one timbre as deterministic offline audio and a realtime DSP preview | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The sound vocabulary owns additive partials, envelopes, filters, Karplus-Strong, and compact FM timbres.<br>`feature/sim-music/synth-performance-workbench` - The synth workbench owns oscillator, subtractive, DX7 FM, modular synth, offline-render, PCM16 WAV, and explicit Table/Dir recipe routing.<br>`recipe/sim-music/crates/sim-lib-music-synth/03-composition/offline-realtime-preview` - The recipe maps every catalog need to an existing owner, renders offline through sound-render, previews through graph/DSP, and uses non-overwriting Table/Dir handles.<br>`spec-test/sim-music/crates/sim-lib-music-synth/src/tests/reuse` - The paired conformance test decodes the recipe and checks its nine owner mappings, bounded callback policy, Table/Dir handles, and phase-43 conversion/dither deferral.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves additive, Karplus-Strong, and FM timbres produce finite deterministic offline PCM. |
| `route/compose-platform-time` | Compose wall time, monotonic time, timers, and cancellation into a request lifetime | code, framework | `feature/sim-foundation/host-primitives` - The existing platform-time contracts own wall and monotonic values, timer behavior, explicit bindings, and the deterministic model.<br>`feature/sim-foundation/cancellation` - Cancellation composes with a supplied deadline without choosing an executor, timer implementation, or transport.<br>`recipe/sim-foundation/crates/sim-cancel/01-basics/request-lifetime` - The checked recipe demonstrates explicit host-to-request ownership; the crate race suite proves registration, propagation, timeout conversion, completion, and dropped-waiter behavior. |
| `route/compose-private-observation-pack` | Keep, review, export, or erase voluntary private observations | user, framework | `feature/sim-sdk/private-observation-pack` - Use the closed voluntary data contract and sealed lane while leaving every field absent unless Mia opts in.<br>`recipe/sim-sdk/stewardship/private-observation` - Inspect a networkless exact entry and locally encrypted export preview.<br>`spec-test/sim-sdk/tests/validate_private_observation_pack` - Prove closed operations, missingness, concurrency, sealed erasure semantics, and explicit export acknowledgement. |
| `route/compose-quiet-stewardship-cadence` | Compose a quiet cadence from minimum copied claims | user, framework | `feature/sim-sdk/quiet-stewardship-cadence` - Compose replaceable stewardship roles behind a calm, source-blind projection policy.<br>`recipe/sim-sdk/stewardship/quiet-cadence` - Replay a deterministic week containing bursts, silence, stale facts, refusals, unavailable placements, lock and revoke, paper operation, and pack removal.<br>`spec-test/sim-sdk/tests/validate_cadence_pack` - Prove daily interaction budgets, explicit finance detail, isolated source handles, and channel disclosure ceilings. |
| `route/compose-relations-from-sdk` | Compose a replaceable relational lifecycle from the SDK | user, code, framework | `feature/sim-sdk/relation-composition` - The SDK exposes each canonical owner behind one provider-neutral feature boundary.<br>`spec-test/sim-sdk/tests/relation_exports` - One admitted logical request runs unchanged through a recording site and SQLite.<br>`recipe/sim-sdk/relation/custom-domain-crud` - The recipe starts the complete custom-domain lifecycle using SDK paths only. |
| `route/compose-reversible-atelier-product` | Compose and replay an offline Atelier product | user, framework | `feature/sim-sdk/reversible-atelier-product` - Compose the delivered layers and private root-set input without adding facade or bootloader behavior.<br>`recipe/sim-sdk/atelier/reversible-product` - The deterministic pilot covers all current memos and rebuilds after deleting the derived worktable. |
| `route/compose-serial-music-from-sdk` | Compose serial music from the SDK | user, code, framework | `feature/sim-sdk/serial-music-composition` - The SDK row owns the single curated serial facade over the frozen candidates and the existing runtime-install surface for supporting shape and notation libs.<br>`recipe/sim-sdk/serial-music/row-matrix-analysis` - The theory recipe proves the facade reaches row-family and matrix analysis through public SDK names.<br>`recipe/sim-sdk/serial-music/modal-realization` - The adaptation recipe realizes one immutable plan through the registry-backed modal surface without mutating the source plan.<br>`recipe/sim-sdk/serial-music/reversible-completion` - The completion recipe proves additive serial completion is reversible through the facade.<br>`recipe/sim-sdk/serial-music/index-discovery` - The index recipe proves the generated feature and route ids are discoverable from the SDK repo surface.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/serial_music` - The public-facade conformance specimen checks reversibility, recipe parity, fail-closed behavior, and the feature claim. |
| `route/compose-staged-sequence-alignment` | Compose staged state selection with sequence alignment | code, framework, user | `feature/sim-discrete/discrete-algebra` - The discrete graph owner supplies certified layered shortest paths and DTW/edit alignment under shared work, memory, finite-cost, stable-tie, and verification policy.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/alignment/tests` - The alignment fixtures prove full and rolling certificates, windows, subsequence boundaries, work refusal, non-finite refusal, and tamper detection.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/layered` - The staged fixtures prove Bellman backpointers, forbidden transitions, bounded work, stable ties, and certificate verification.<br>`spec-test/sim-discrete/crates/sim-lib-discrete-graph/src/cookbook` - The checked composition invokes and verifies both engines and retains stable staged states plus DTW score, path length, cells, and edges without a copied domain-side DP loop.<br>`recipe/sim-discrete/crates/sim-lib-discrete-graph/02-scenarios/staged-alignment` - The public cargo-example recipe prints the same composition evidence for a human reader. |
| `route/compose-taste-led-kitchen-pack` | Prepare kitchen options and a shopping draft without choosing or buying | user, framework | `feature/sim-sdk/taste-led-kitchen-pack` - Compose the closed data contract and pure projection while Mia retains meal authority.<br>`recipe/sim-sdk/stewardship/taste-led-kitchen` - Exercise copied egg evidence, stale-stock questioning, exact unit labels, and an opaque optional cost reference with every provider lane absent.<br>`spec-test/sim-sdk/tests/validate_kitchen_pack` - Prove the Shapes, freshness lattice, source-room isolation, effect ceiling, and networkless fixture. |
| `route/continue-intermittent-session` | Replay and continue an intermittent multi-role session without hidden host authority | framework | `feature/sim-agent-net/pure-continuity` - The pure reducer validates policy and derives identical turns and intents from fenced journal history.<br>`recipe/sim-agent-net/crates/sim-lib-continuity/01-basics/hostile-replay` - The checked hostile trace proves empty-cache replay and typed refusal behavior. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`feature/sim-construction/table-backed-project-books` - The persistence row injects the selected Table or Dir backend and keeps the append-only fact stream authoritative.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row joins stable controls to the existing Gantt plan and critical-path model.<br>`feature/sim-construction/mspdi-schedule-exchange` - The exchange row round-trips the portable Gantt plan across the MSPDI boundary.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row realizes modeled schedule placement while retaining the explicit live gates.<br>`feature/sim-construction/dalux-project-items` - The Dalux row realizes modeled field-item placement and preserves URL-free external and effect-ledger references.<br>`feature/sim-construction/production-lookahead-readiness` - The readiness row derives package and task readiness from accepted facts and schedule joins.<br>`feature/sim-construction/change-commercial-exposure` - The change row derives exact exposure while retaining reference-only ledger ownership.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row derives system and project burn-down from explicit evidence.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The closeout row admits only evidence-backed, disclosure-approved reference claims.<br>`feature/sim-construction/office-evidence-bridge` - The evidence bridge retains stable office references without copying construction truth or document payloads.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row projects the same filtered snapshot into existing Doc, Sheet, Deck, and suite surfaces.<br>`recipe/sim-construction/reference-project-control` - The checked primary Lisp recipe runs one entirely invented renovation twice through conflict, failure, bounded exception, correction, closeout, visibility-safe office review, and final reference admission while composing the public libraries, codecs, injected Table backend, and modeled vendor sites offline by default. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/describe-declarative-harmony` | Describe harmony palettes and rules as data | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies existing chord symbols, exact pitches and ratios, pitch sets, scale maps, voicings, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row assembles those owners into codec-neutral palettes, template chains, hard rules, soft metrics, evidence, voicing changes, and render settings.<br>`spec-test/sim-music/crates/sim-lib-pitch-chord/src/harmony_conformance` - The checked Lisp descriptors reproduce the catalog harmonizer and chord-cycle palette/filter chains without compiled policy. |
| `route/encode-openai-provider-wire` | Encode canonical model traffic for an OpenAI provider wire | code, framework | `feature/sim-codecs/openai-provider-wires` - The wire row selects Chat Completions or Responses while preserving one canonical bounded model contract.<br>`spec-test/sim-codecs/crates/sim-codec-chat/src/tests/openai_responses` - The checked specimen covers Responses requests, responses, SSE events, and provider-private reasoning payloads. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/estimate-classical-spectrum` | Estimate a classical or autoregressive spectrum with reconstructable scaling | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns reference windows, periodogram/Welch/cross/coherence, Slepian multitaper, uneven-sample Lomb-Scargle, stable Burg/MEM models, explicit grids and scaling, and bounded evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/spectral_tests` - The checked tone/noise specimen proves window invariants, Parseval density, peak recovery, coherence, Slepian concentration, uneven-sample recovery, and fail-closed work limits.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/autoregressive_tests` - The generated AR-process specimen proves BIC order recovery, stable reflections, MEM resonance, bounded bidirectional prediction, and singular or unstable fail-closed behavior.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/spectral-estimators` - The runnable periodogram specimen prints coherent gain, equivalent noise bandwidth, peak power, degrees of freedom, and admitted work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/burg-mem-evidence` - The deterministic generated AR(2) recipe prints selected order, residual variance, reflection margin, MEM peak, and admitted work. |
| `route/evaluate-exact-score-consonance` | Evaluate exact score consonance | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row supplies canonical rational score time and identity-bearing Staff conversion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row supplies overlap- and pedal-aware realization before MIDI window evaluation.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row owns the pitch, ratio, contextual sonance, psychoacoustic, and tuning models.<br>`feature/sim-music/exact-score-consonance` - The consonance row composes those owners into multiplicity-preserving sounding windows and separate metric reports.<br>`recipe/sim-music/crates/sim-lib-music-consonance/01-basics/exact-score-consonance` - The Lisp recipe shows an exact progression report with a retained doubling and no implicit aggregate. |
| `route/exchange-score-musicxml` | Import and export a bounded MusicXML score | user, code, framework | `feature/sim-music/midi-notation-workflows` - The notation row owns the bounded MusicXML profile, stable exchange ids, loss reports, and one runtime import callable.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact score row supplies canonical rational Score semantics and the existing SIM expression/read-construct form.<br>`recipe/sim-music/crates/sim-lib-music-notation/01-basics/musicxml-import` - The notation recipe shows the bounded runtime call and profile limits.<br>`spec-test/sim-music/crates/sim-lib-music-notation/src/tests` - The checked fixture proves DTD rejection, bounds, ids, losses, and LilyPond/MusicXML/SIM semantic round trips. |
| `route/execute-stewardship-memo-acceptance` | Turn stewardship memo promises into checked synthetic evidence | user, framework | `feature/sim-sdk/stewardship-memo-acceptance` - Classify every promise before claiming that the composed product satisfies it.<br>`recipe/sim-sdk/stewardship/memo-acceptance` - Inspect the synthetic month, week, season, flock, dialogue, crew, and lifecycle trace plus every explicit unsupported and manual state.<br>`spec-test/sim-sdk/tests/memo_acceptance` - Prove matrix completeness, exact source arithmetic, human authority, prepare-only crew behavior, privacy hygiene, and bounded managed-erasure claims. |
| `route/execute-tensor-at-site` | Execute tensor at a site | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the canonical Tensor value and checked host or resident observation.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns TensorExecutor, TensorSite, and element-wise broadcast request routing.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/tests/execution` - The checked execution tests prove direct CPU execution, TensorSite realization, capability checks, and environment restoration.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-bcast/src/tests` - The checked broadcast tests prove active executor routing, CPU fallback, zero-extent broadcasting, alias safety, and decline errors. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/explain-roadmap-execution` | Inspect and compare a roadmap execution without trusting raw logs or repeating effects | framework | `feature/sim-agent-net/roadmap-execution-status-values` - The projection derives bounded status, explanation, redaction, and audit facts exclusively from verified journal values.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/status` - The fixture proves deterministic effect-free rebuilding, stable diffs, hostile redaction, and bounded terminal output. |
| `route/explore-interference-field` | Explore an interference field | code, framework | `feature/sim-web/heatmap-surface` - The domain-neutral math view validates the already reduced finite scalar grid, mask, detector label, advisory, and SurfaceCaps budget.<br>`feature/sim-web/interference-surface` - The reversible surface selects the detector reduction, preserves Study evidence, and compiles edits to interference/project or interference/solve.<br>`recipe/sim-web/crates/sim-lib-view-interference/01-basics/interference-study` - The checked recipe encodes a Study, realizes projection and model Operations, refreshes the Scene, and proves desktop, phone, and glance budgets. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-foundation/index-vault-projection` - The projection row consumes the canonical inventory and proves every row has exactly one primary claim.<br>`feature/sim-codecs/index-vault-codec` - The codec row owns document dialect composition, v2 bundles, v1 reading, and semantic verification.<br>`feature/sim-index/vault-export` - The vault export row owns canonical loading, artifacts, reports, migration, and managed command behavior.<br>`spec-test/sim-codecs/crates/sim-codec-index-vault/tests/conformance` - The codec specimen proves all-profile round trips, semantic corruption rejection, and bounded v1 migration.<br>`spec-test/sim-tooling/src/index_vault_write_tests` - The checked materialization specimen proves managed writes preserve sibling notes and application configuration. |
| `route/expose-expression-tree-view` | Expose an expression tree view | code, framework | `feature/sim-expr-tree/expression-tree-view` - The expression-tree view row owns the bounded reversible outline over standard Scene, Intent, SurfaceCodec, and SurfaceCaps contracts.<br>`spec-test/sim-expr-tree/crates/sim-lib-view-expr-tree/src/tests` - The focused conformance module proves full rendering, intent reversal, lazy disclosure, continuation, freshness, hostile values, stale revisions, accessibility, large trees, layout projection, and authorization. |
| `route/expose-view-surface` | Expose a view surface | code, framework | `feature/sim-web/view-surface` - The sim-lib-view row owns the default view and reversible edit surfaces.<br>`feature/sim-web/codec-surface-sessions` - The bridge session consumes one SurfaceCodec for encode, decode, commit, projection, and isolated pane state.<br>`feature/sim-web/device-surfaces` - Device profiles show how projection adapts to concrete surface caps.<br>`feature/sim-web/web-shell-host` - The web shell row shows how a browser host loads those surfaces. |
| `route/find-a-grammar` | Find a grammar | code, framework | `feature/sim-codecs/expression-syntax-grammars` - The expression syntax row carries language and grammar subjects.<br>`feature/sim-codecs/domain-syntax-grammars` - The domain syntax row carries data, document, config, and packet syntax contracts.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire row carries packet and byte-level grammar contracts. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-numeric-root` | Find a scalar or vector root with reviewable evidence | code, framework, user | `feature/sim-numbers/root-finding` - The root owner distinguishes proved sign-changing brackets from residual-only estimates and records derivative, rank, work, termination, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-root/src/tests` - The checked tests cover endpoint and multiple roots, invalid and discontinuous brackets, flat derivatives, scaling, singular Jacobians, bounded Broyden resets, and deterministic replay.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/01-basics/scalar-root` - The scalar recipe demonstrates bracket-preserving Brent-Dekker search.<br>`recipe/sim-numbers/crates/sim-lib-numbers-root/02-vector/vector-root` - The vector recipe demonstrates rank-diagnosed damped Newton search. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/fit-finite-transition-model` | Fit an inspectable finite transition model | code, framework | `feature/sim-numbers/statistical-inference` - The statistics framework owns generic finite Markov counts, additive smoothing, held-out likelihood, deterministic serialization, and corpus provenance without domain-specific state types.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/markov_tests` - The checked non-music weather specimen proves smoothing, held-out scoring, fail-closed unknown states, and byte-stable serialization. |
| `route/fit-point-clusters` | Fit deterministic point clusters with convergence and model-selection evidence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns seeded k-means++ restarts, empty-cluster repair, regularized full/diagonal GMM EM, log-domain likelihood, singular-component policy, and AIC/BIC evidence while leaving temporal alignment to sim-discrete.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/clustering_tests` - Generated, degenerate, permuted, bounded, and non-finite fixtures prove deterministic centroids, log-domain likelihood, covariance regularization, convergence, repairs, and fail-closed control.<br>`recipe/sim-numbers/crates/sim-lib-numbers-stats/01-basics/clustering-evidence` - The runnable generated-cluster recipe prints selected restart, centroids, likelihood, repairs, AIC, BIC, and charged work and points alignment callers to the discrete graph owner. |
| `route/fuse-ranked-results` | Fuse bounded ranked result lists deterministically | code, framework | `feature/sim-stream/stream-protocol-helpers` - sim-lib-rank owns provider-neutral reciprocal-rank fusion and its inspectable receipt.<br>`recipe/sim-stream/crates/sim-lib-rank/01-basics/rank-fusion` - The three-list recipe demonstrates stable keys, weighted sources, deterministic output, and receipt ownership. |
| `route/generate-counterpoint` | Generate bounded reversible counterpoint | user, code, framework | `feature/sim-music/exact-score-consonance` - The consonance row owns the content-bound additive patch and exact inverse that preserve the fixed cantus staff.<br>`feature/sim-music/music-counterpoint` - The counterpoint row compiles rule data to finite CSP variables/domains, delegates traversal and receipts to discrete-search, propagates musical constraints, selects diverse legal results, and rechecks each result through the existing analyzer.<br>`recipe/sim-music/crates/sim-lib-music-counterpoint/01-basics/bounded-generation` - The checked Lisp specimen generates two second-species voices under explicit work, result, frontier-default, and seed evidence. |
| `route/grow-bounded-music-catalog` | Grow and inspect a bounded recursive music catalog | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact transform row supplies first-class partial pitch maps and scale-aware music transformations.<br>`feature/sim-music/bounded-rewrite-catalogs` - The rewrite row owns typed musical alphabets, productive context rules, scale-following state data, finite progression-tree topology, and derivation evidence while delegating ordinals and traversal to shared discrete rank/search.<br>`spec-test/sim-music/crates/sim-lib-music-combinators/src/rewrite_conformance` - The checked conformance module proves typed contextual derivation, preflight rejection, rank round trips, bounded filtered search, scale-map materialization, and stable receipts. |
| `route/guard-an-operation` | Guard and replay a capability-scoped operation | code, framework | `feature/sim-runtime/guarded-operations` - The generic gate owns explicit policy, exact approval, atomic use, records, and the performer boundary.<br>`recipe/sim-runtime/crates/sim-lib-operation-gate/01-basics/two-manifests` - Two unrelated fixture manifests prove the gate contains no automotive assumption. |
| `route/handoff-expedition-across-channels` | Move an expedition thought across channels without widening authority or disclosure | framework | `feature/sim-agent-net/diminished-channel-passports` - The passport and channel ceilings enforce diminishing handoff before encode.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/atelier/channel` - Adversarial fixtures reject new grants and claims and prove every named channel ceiling. |
| `route/harmonize-melody` | Harmonize a melody under explicit rules and bounds | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies melody pitch sets, chord templates, scales, registers, voice-leading, and named sonance models.<br>`feature/sim-music/declarative-harmony` - The declarative row supplies the palette plus separate hard legality and weighted score lanes.<br>`feature/sim-music/bounded-harmonization` - The harmonization row adapts the shared musical problem to exhaustive, factored, certified layered-DP, and beam strategies.<br>`recipe/sim-music/crates/sim-lib-music-combinators/01-basics/bounded-harmonization` - The Lisp recipe shows the public layered-DP call with explicit work, result, and seed bounds. |
| `route/host-sim-in-browser` | Host SIM in a browser without moving presentation policy into the platform | code, framework | `feature/sim-platform/browser-capsule` - The browser capsule contributes capability-honest handles and lifecycle events through canonical named-call frames.<br>`feature/sim-web/view-surface` - sim-web remains the sole owner of reversible browser presentation and edit behavior. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/infer-hidden-sequence` | Summarize a stream or infer a finite hidden-state sequence | code, framework, user | `feature/sim-numbers/statistical-inference` - The statistics row owns hard-bounded mergeable quantiles, shared finite transitions, normalized discrete/Gaussian HMM inference, and seeded bounded fitting evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/quantile_tests` - The checked monotone-stream specimen proves exact small data, bounded retained entries, rank accuracy, merge behavior, and transactional refusal.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-stats/src/hmm_tests` - The enumerated tiny fixture proves likelihood, posterior, and Viterbi agreement; long, continuous, seeded-fit, and work-limit fixtures prove numerical and termination behavior. |
| `route/inject-server-wall-clock` | Inject a server wall clock | code, framework | `feature/sim-agent-net/server-wall-clock` - The server row documents consumption and compatibility re-exports without claiming time ownership. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/inspect-jvm-classfile` | Inspect or extend JVM classfile structure | code, framework | `feature/sim-codecs/classfile-inspection` - The classfile codec is the sole byte parser and publishes manifest-derived opcode, constant, and attribute coverage.<br>`recipe/sim-codecs/crates/sim-codec-classfile/01-inspection/retained-classfile` - The recipe demonstrates bounded JVM-free inspection with retained origins.<br>`spec-test/sim-codecs/crates/sim-codec-classfile/tests/index_coverage` - The guard fixture proves duplicate inventories, consumer byte parsers, lossy modified UTF-8, and hand-edited generated coverage fail closed. |
| `route/inspect-jvm-performance-evidence` | Inspect JVM prepared-execution performance evidence | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile publishes prepared-code, verified micro-op, and generated fusion coverage without owning benchmark statistics.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/performance-evidence` - The recipe maps every fidelity and performance claim to generated coverage and canonical benchmark harness raw samples.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/published_performance` - The checked publication fixture keeps raw report identities, summaries, generated coverage, and ownership guards connected. |
| `route/inspect-platform-command` | Inspect and verify the selected platform through the loadable command | user, code, framework | `feature/sim-run/platform-command` - The bootloader dispatches the fixed platform vocabulary through the loaded product library.<br>`spec-test/sim-run/crates/sim-run/src/platform` - The command specimen proves fixed dispatch, machine output, and typed refusal behavior. |
| `route/integrate-femm-index-one-dae` | Integrate a FEMM index-1 DAE | code, framework | `feature/sim-femm/implicit-residual-adapter` - The FEMM adapter owns the residual/Jacobian boundary and preserves FEMM field and mesh ownership.<br>`spec-test/sim-femm/crates/sim-lib-femm-ode/src/tests` - The checked adapter specimen proves that a mixed differential/algebraic FEMM residual becomes the public implicit form. |
| `route/interpolate-periodic-spectrum` | Interpolate or analyze a periodic signal under explicit Fourier conventions | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns continuous DFT-series evaluation and integration, single-bin evaluation, Hilbert construction, unwrapped phase, instantaneous frequency, and envelope following under one explicit convention.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/interpolation_tests` - The generated analytic periodic-function specimen proves off-grid interpolation, exact integration, endpoint refusal, wrapping, and single-bin parity.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/analytic_tests` - The generated tone specimen proves Hilbert quadrature under every normalization, phase unwrapping, interval-centered frequency, and finite attack/release envelopes.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/dft-interpolation` - The Lisp recipe retains unitary scaling, period, origin, endpoint, wrapping, sign, Nyquist, phase, and work evidence. |
| `route/interpret-saved-web-documents` | Interpret saved HTML, feeds, and robots | code, framework | `feature/sim-codecs/saved-web-documents` - The codec organ owns bounded inert parsing and projections for all three saved web formats.<br>`spec-test/sim-codecs/crates/sim-codec-doc/src/html` - The checked document specimen proves bounded inert HTML decoding into the shared document model. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/onset-beat-harmony` - The checked Lisp analysis call selects onsets, beats, MFCC, chroma, key, and chords under explicit varying-tempo, posterior/HMM, work, result, and seed policy; its generated click/chord fixture proves timing and labels.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/link-jvm-lambda` | Link and invoke a JVM lambda or method reference | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The JVM profile owns bootstrap admission, direct-handle resolution, loader-local generated metadata, and Java descriptor adaptation.<br>`feature/sim-runtime/function-organ` - The shared function organ owns the neutral Callable projection and collector-visible captures used at the SIM boundary.<br>`feature/sim-runtime/class-semantics` - The shared class organ owns language-neutral class structure; defining-loader identity remains JVM policy.<br>`feature/sim-runtime/mutation-organ` - The managed graph owns lambda instances, captures, weak identities, and collection rather than a JVM-private graph or global cache.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/verifier_scope` - The verifier consumes the executor-owned admitted bootstrap registry and seals exact whole-class proof before preparation.<br>`recipe/sim-runtime/crates/sim-lib-lang-jvm/01-basics/java-lambda` - The published Java-to-SIM recipe links compiler-produced bytes and invokes the result through ordinary Callable.<br>`recipe/sim-sdk/jvm/lambda-interop` - The SDK recipe reaches both interop directions through public facade exports only. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/load-runtime-library` | Load a runtime library | code, framework | `feature/sim-runtime/library-loading` - The loading row owns standard and language-profile runtime libraries. |
| `route/load-runtime-surface` | Load a runtime surface | code, framework | `feature/sim-run/loaders` - The loader row owns native, source, and re-exported runtime library discovery.<br>`feature/sim-run/terminal-surface` - The terminal surface row shows a loadable view surface provided by a runtime library.<br>`recipe/sim-run/02-scenarios/host-verb` - The checked host-verb recipe exercises the terminal surface path. |
| `route/lower-javascript-regexp` | Lower bounded JavaScript RegExp syntax into the shared pattern organ | code, framework | `feature/sim-runtime/javascript-core-profile` - The bounded JavaScript profile owns ECMAScript admission, exclusions, and UTF-16 syntax policy.<br>`feature/sim-runtime/pattern-cursor-domains` - Admitted RegExp syntax lowers to the shared automaton and executes with exact code-unit offsets and bounded receipts.<br>`spec-test/sim-runtime/crates/sim-lib-lang-javascript/src/regexp` - The JavaScript specimen checks the real RegExp adapter, exact UTF-16 captures, work limits, and fail-closed unsupported clauses.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The organ specimen independently demonstrates exact byte, scalar, and code-unit domains plus bounded adversarial ambiguity. |
| `route/lower-lua-pattern` | Lower Lua pattern syntax into the shared pattern organ | code, framework | `feature/sim-runtime/guest-language-profiles` - Lua retains its surface syntax and character-class policy while composing runtime organs.<br>`feature/sim-runtime/pattern-cursor-domains` - The shared pattern organ validates the lowering and executes it with typed scalar offsets and bounded receipts.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The runnable cross-dialect specimen lowers Lua syntax beside glob and neutral IR, then executes all three through the same organ. |
| `route/lower-shell-glob` | Lower shell glob syntax into the shared pattern organ | code, framework | `feature/sim-runtime/pattern-cursor-domains` - The glob adapter lowers its anchored surface into the shared validated IR and bounded scalar-domain executor.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cross_dialect_safety` - The runnable specimen proves glob syntax reaches the same organ as Lua and neutral IR without copying an executor. |
| `route/match-exact-code-unit-text` | Match exact UTF-16 text without losing code-unit positions | code, framework | `feature/sim-foundation/exact-code-unit-text` - The sim-text value preserves paired and lone surrogate units and owns checked conversion between scalar and code-unit offsets.<br>`feature/sim-runtime/pattern-cursor-domains` - The pattern organ executes directly over exact units and returns domain-typed match and capture offsets.<br>`spec-test/sim-runtime/crates/sim-lib-pattern/tests/cursor_domains` - The checked specimen matches a lone surrogate and stops between a surrogate pair's halves without admitting that position as a scalar offset. |
| `route/model-lumped-boundary-events` | Model a lumped dynamic system with explicit boundaries and events | code, framework, user | `feature/sim-physics/boundary-event-core` - The core owns solve-independent topology and signed-power boundary validation.<br>`spec-test/sim-physics/crates/sim-lib-physics-core/tests/boundary_event` - The specimen proves canonical identity, simultaneous events, and fail-closed topology. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/operate-continuity-phone-surface` | Operate a complete continuity session from a phone surface | user, code, framework | `feature/sim-web/continuity-phone-surface` - The phone codec projects journal truth and compiles only bounded semantic actions.<br>`spec-test/sim-web/crates/sim-lib-view-continuity-phone/src/tests` - The specimen proves silent interaction, stale-safe replay, lifecycle refusal, and an always-reachable stop. |
| `route/operate-model-test-observatory` | Operate the model-test observatory through its loadable command | user, code, framework | `feature/sim-agent-net/model-test-product` - The product owns the complete command vocabulary while delegating lifecycle and provider authority to their existing owners.<br>`spec-test/sim-agent-net/crates/sim-lib-model-test/src/tests` - The product specimen proves fixed dispatch, bounded machine output, and provider-contact boundaries. |
| `route/operate-on-roadmap-intent` | Ground and refine roadmap intent without execution authority | framework | `feature/sim-agent-net/loadable-roadmap-command` - The loadable command owns capability-free roadmap operations.<br>`feature/sim-agent-net/native-roadmap-document-adapter` - The strict adapter imports and renders content-pinned roadmap documents.<br>`feature/sim-agent-net/certified-roadmap-refinement` - Certified refinement preserves acceptance while proving strict descent.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap/src/tests` - The checked fixture covers grounding, import, refinement, rendering, and refusal. |
| `route/operate-provider-organ` | Discover, validate, and fan out across provider seats | user, code, framework | `feature/sim-agent-net/provider-control` - The provider organ owns open family adapters, private inventory, explicit seats, shared conformance, and ordered fan-out.<br>`feature/sim-agent-net/model-runner-framework` - Every opened direct API, CLI broker, local-service, or extension seat executes through ModelRunner.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE owns the checked request and output contract shared by every family.<br>`recipe/sim-agent-net/crates/sim-lib-provider/01-basics/open-seat` - The checked recipe opens a provider seat through the same registry and card contract used by shipped and third-party families.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The unchanged checked GENAI recipe remains the canonical request source. |
| `route/operate-relational-data` | Discover inspect migrate query mutate or mount a database relation schema SQLite site or SQL projection | user, code, framework | `feature/sim-foundation/relation-core` - The provider-neutral logical relation framework owns rows, domains, and identities.<br>`feature/sim-shape/relation-shape` - Shape admits relation values and command artifacts.<br>`feature/sim-codecs/sql-projection` - The SQL codec is a projection of checked plans, never a command escape hatch.<br>`feature/sim-foundation/relation-migrate` - Migration admission and drift checks precede physical action.<br>`feature/sim-storage/bounded-relation-site` - The site contract gates provider effects and bounds.<br>`feature/sim-storage/sqlite-relation-locator` - SQLite is selected through a closed provider identity and opaque locator.<br>`feature/sim-storage/relation-table-dir-projection` - Results render through standard Table/Dir composition.<br>`feature/sim-office/office-site-workflows` - Office workflows reuse relational sites rather than copying storage behavior.<br>`feature/sim-ledger/ledger-libraries` - Ledger composition supplies durable domain reuse.<br>`feature/sim-sdk/relation-composition` - The SDK facade exposes the supported relation composition.<br>`feature/sim-storage/relation-command` - The loadable command parses only named, authorized, bounded operations.<br>`spec-test/sim-storage/crates/sim-lib-relation-cli/src/tests` - Checked specimens cover creation, inspection, adoption, drift refusal, query, mutation, providers, and mounts. |
| `route/parse-javascript-source` | Parse bounded JavaScript source | code, framework | `feature/sim-codecs/javascript-source-frontend` - The JavaScript frontend owns bounded ECMAScript tokenization, structural parsing, source identity, and expression lowering.<br>`spec-test/sim-codecs/crates/sim-codec-javascript/tests/conformance` - The conformance specimen checks accepted syntax, stable lowering, round trips, and fail-closed limits. |
| `route/parse-operator-language` | Parse an operator language | code, framework | `feature/sim-codecs/pratt` - The Pratt codec is the operator-language parser surface. |
| `route/parse-python-source` | Parse bounded Python source | code, framework | `feature/sim-codecs/python-source-frontend` - The Python frontend owns bounded Python tokenization, layout, source identity, and expression lowering.<br>`spec-test/sim-codecs/crates/sim-codec-python/src/tests` - The conformance specimen checks admitted syntax, stable lowering, round trips, and fail-closed limits. |
| `route/parse-typescript-notation` | Parse bounded TypeScript notation | code, framework | `feature/sim-codecs/typescript-syntax` - The TypeScript frontend owns lossless notation and direct erasure of the compiler-independent subset.<br>`spec-test/sim-codecs/crates/sim-codec-typescript/src/tests` - The conformance specimen checks TypeScript and TSX parsing, lossless identity, and fail-closed erasure gaps. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/plan-review-apply-estate-change` | Plan, review, and apply an estate change | code, framework | `feature/sim-estate/exposure-authoring` - Compile a closed operation and shaped values.<br>`feature/sim-estate/guarded-operation-lifecycle` - Intern the exact plan, consume review once inside the gate, lease targets, and dispatch durably.<br>`feature/sim-estate/durable-operation-book` - Browse the linked event truth and rebuild projections. |
| `route/plan-serial-practice` | Freeze and realize a serial plan with immutable provenance and chord-safe partial order | user, code, framework | `feature/sim-music/serial-series-calculus` - The generic serial layer owns stable finite ordered-symbol identity and aggregate validation without assuming pitch or score order.<br>`feature/sim-music/twelve-tone-row-theory` - The pitch-serial layer owns strict row values and operation-bearing RowForm provenance for each row instance.<br>`feature/sim-music/serial-practice` - The music-serial layer freezes row instances, stable event ids, ordinal references, role/origin provenance, explicit parents, simultaneous groups, and precedence into one immutable validated plan, then realizes strict statements with explicit register, duration, dynamics, rests, ties, and canonical music-core rendering.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/immutable-plan` - The checked Rust scenario proves that one row can span multiple voices while equal-onset material stays grouped without a fabricated chord order.<br>`recipe/sim-music/crates/sim-lib-music-serial/01-basics/strict-row-realization` - The checked Rust scenario realizes a strict row statement with a chord, rest, tie, and equal-pitch simultaneous notes while preserving plan, event, voice, and ordinal identity through the piano roll. |
| `route/preserve-physics-finding-history` | Preserve and investigate a surprising physics result without erasing competing explanations | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Produce reviewable sweep evidence and stable study identity.<br>`feature/sim-physics/immutable-finding-history` - Publish the observation, append evidence-bearing successors, retain branches, and rebuild read-only projections from immutable records. Its crate integration suite proves identity, backend parity, interrupted and concurrent append, corruption refusal, branch reconciliation, rebuild, complete history, and redaction.<br>`recipe/sim-physics/06-findings/sweep-to-finding` - Convert one sweep residual into an open finding. |
| `route/process-sampled-signal` | Transform, smooth, solve, or interpolate sampled data | code, framework, user | `feature/sim-numbers/signal-transforms` - The signal row owns wavelet boundaries and inverse shapes, canonical Savitzky-Golay derivative scaling, stable Toeplitz diagnostics, and duplicate/extrapolation interpolation policy.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/wavelet_tests` - The checked wavelet specimen proves multilevel odd-length inverses under periodic, symmetric, and zero extension plus the Le Gall linear-polynomial law.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/smoothing_tests` - The checked smoothing specimen proves polynomial preservation, factorial and sample-spacing derivative scaling, Toeplitz residuals, and singular-pivot diagnostics.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/sample_interpolation_tests` - The checked interpolation specimen proves affine preservation, monotonicity, duplicate resolution, extrapolation policy, and fail-closed input.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/wavelet-smoothing` - The runnable recipe composes all four generic algorithms with their visible reports. |
| `route/project-masked-scalar-heatmap` | Project a masked scalar grid as a heatmap | code, framework | `feature/sim-web/heatmap-surface` - The heatmap row validates caller-prepared finite values, masks, metadata, and SurfaceCaps-derived cell and byte budgets without applying a domain reduction rule.<br>`recipe/sim-web/crates/sim-lib-view-math/01-basics/heatmap-grid` - The checked recipe projects a bounded masked scalar grid into the domain-neutral scene/heatmap contract. |
| `route/project-reversible-expedition` | Project a reversible expedition product | user, code, framework | `feature/sim-web/reversible-worktable` - Replay owner-authored semantic edits into the disposable worktable without moving domain behavior into the surface.<br>`spec-test/sim-web/crates/sim-lib-view-worktable/src/lib` - The dependency-free specimen proves replay, undo, visible arming, manual continuation, and insufficient-evidence behavior. |
| `route/protect-opaque-state` | Protect opaque continuation or handoff state with rotation and optional single-use consumption | code, framework | `feature/sim-foundation/host-primitives` - The shared WallClock contract supplies the explicit expiry observation without ambient host time.<br>`feature/sim-foundation/protected-state` - The protected-state owner supplies the bounded envelope, collision-free binding, injected key and nonce capabilities, rotation behavior, uniform rejection, and canonical Table consumption adapter.<br>`recipe/sim-foundation/crates/sim-lib-protected-state/01-basics/exact-binding` - The checked recipe keeps caller serialization, key custody, clock ownership, and optional replay claims explicit. |
| `route/prove-benchmark-workload-neutrality` | Prove the benchmark harness is workload neutral | user, code, framework | `feature/sim-tooling/benchmark-cli` - The benchmark CLI composes sampling, process isolation, robust comparison, and durable reports without a domain-specific workload contract.<br>`spec-test/sim-tooling/src/bench/neutral_cpu_conformance` - The checked CPU-bound specimen proves deterministic sampling, matched comparison, host refusal, retained failure, and Table/Dir browsing without bytecode.<br>`spec-test/sim-tooling/src/bench/neutral_allocation_conformance` - The checked allocation-bound specimen proves local fingerprinting, high-dispersion refusal, retained failure, and durable report browsing without bytecode. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/qualify-roadmap-shadow` | Qualify a local roadmap runner in shadow mode without creating a second authority | framework | `feature/sim-agent-net/roadmap-shadow-observation` - The fixed semantic record is sanitized, evidence-backed, and incapable of reading an oracle or applying its proposed transition.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/compatibility` - The fixture checks dimension completeness, stable content identity, and rejection of private control-plane values. |
| `route/raise-guest-value-from-sdk` | Raise and match a guest value from the SDK | code, framework | `feature/sim-runtime/organs` - The runtime organ owns Raised, bounded class matching, and ManagedException stable relation edges.<br>`feature/sim-sdk/raised-exception-composition` - The SDK row exposes those canonical contracts together under sim::exceptions. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/realize-host-need` | Realize a host need through the absolute platform membrane | code, framework | `feature/sim-foundation/host-primitives` - Begin with the neutral primitive, open identity, bounded refusal, and lexical binding contract.<br>`feature/sim-storage/host-storage-primitives` - Put storage semantics in the domain-owned port; substitute the corresponding domain port for non-storage needs.<br>`feature/sim-platform/platform-policy` - Use the seeded model as the deterministic, host-free behavior specimen before selecting physical mechanics.<br>`feature/sim-platform/ubuntu-pc-capsule` - Implement the need in one explicitly selected capsule whose Card declares the offered service.<br>`feature/sim-platform/transport-realization` - Run the shared port conformance at the capsule boundary so native results are compared semantically.<br>`feature/sim-platform/platform-owner` - Retain the matching Card, artifact, attestation, and evidence-derived support row in the composed bundle. |
| `route/realize-serial-spine` | Adapt an immutable serial plan onto a modal or caller-defined spine without hiding aggregate loss | user, code, framework | `feature/sim-music/serial-practice` - The serial owner keeps the immutable plan, stable event identities, row-form provenance, explicit strict specs, and strict chromatic baseline intact before any adaptation occurs.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch vocabulary row owns named scales, caller-defined player scales, and contextual sonance models reused by the adaptation report.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact transform row owns the first-class partial pitch-map policy that modal landing reuses instead of inventing another remap surface.<br>`feature/sim-music/open-serial-adaptation` - The open adaptation row owns landed modal realization, aggregate-relaxation receipts, and the independent spine, pitch, ordinal, and sonance report facets.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/third-party-adaptation` - The checked external-style scenario registers a seven-symbol non-pitch series, a custom practice rule, and a custom realizer through public APIs only, then proves the Lisp validation hook and realizer registry both fail closed on unknown components.<br>`recipe/sim-music/crates/sim-lib-music-serial/02-adaptation/modal-spine` - The checked Rust scenario realizes the same immutable plan through C Dorian, C Lydian, and a caller-defined scale, proves plan equality with differing sounding pitches, and inspects the spine report without claiming strict twelve-tone output. |
| `route/reconcile-quarantined-estate-run` | Reconcile or resolve a quarantined estate run | code, framework | `feature/sim-estate/durable-operation-book` - Read the existing run and retained evidence without dispatch.<br>`feature/sim-estate/guarded-operation-lifecycle` - Close, renew, release, or retain quarantine from observed artifacts; a mutation needs a new reviewed plan.<br>`feature/sim-estate/model-provider-conformance` - Prove unknown-after-dispatch and reviewed resolution before using a physical site. |
| `route/reconcile-roadmap-phase` | Replay and reconcile one roadmap phase without coupling execution laws to a host | framework | `feature/sim-agent-net/pure-roadmap-execution` - The pure reducer owns correlation, reconciliation, and success admission while returning all requested effects as data.<br>`spec-test/sim-agent-net/crates/sim-roadmap-exec-core/src/tests` - The transition matrix, hostile correlations, deterministic replay, and forged-success fixture exercise the complete value-machine boundary. |
| `route/recover-bounded-roadmap-execution` | Recover a failed roadmap attempt without widening authority or cycling | framework | `feature/sim-agent-net/bounded-roadmap-recovery` - The pure policy gate makes reconciliation, unchanged identities, named failure class, compatible fallback provenance, and a finite remaining counter prerequisites for another attempt.<br>`spec-test/sim-agent-net/crates/sim-roadmap-exec-core/src/tests` - Randomized sequences consume their declared counters and pinned fallback fixtures reject foreign candidates.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/tests` - Foreign source bytes terminate without effects while only freshly regrounded strict descent reaches the refiner. |
| `route/refuse-energy-influenced-selection` | Prove that a physical-study selection input is independent of energy | code, framework, user | `feature/sim-physics/no-energy-selection` - Apply the fail-closed influence domain over the canonical incremental engine, protected sinks, native type-state, and runtime Shape preparation; its crate integration suite proves direct and laundered influence, branches, joins, loops, opaque code, clean preparation, budget refusal, and incremental edits.<br>`recipe/sim-physics/04-influence/refused-selection` - Run a divide-by-time laundering refusal and inspect its causal path. |
| `route/represent-interference-study-at-runtime` | Represent an interference study as runtime data | code, framework | `feature/sim-interference/tensor-runtime-records` - The runtime feature composes canonical Tensor storage, Citizen read-constructs, and Shape checks without adding a domain parser or grid storage.<br>`spec-test/sim-interference/crates/sim-lib-interference-runtime/src/citizen` - The checked Citizen inventory specimen proves every interference record can be installed and reconstructed. |
| `route/reproducible-federated-research` | Run reproducible federated research | user, code, framework | `feature/sim-agent-net/reproducible-federated-research` - Freeze intent, authority, sites, concurrency, budgets, and revisions before provider dispatch.<br>`feature/sim-agent-net/provider-neutral-http-search` - Collect provider claims and typed partial failures through provider-neutral Retriever cards.<br>`feature/sim-agent-net/reproducible-federated-research` - Cluster only evidenced aliases, fuse via shared RRF, capture selected pages under independent policy, verify selectors, and retain replay receipts.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/fixture-research` - The all-fixture recipe demonstrates intent through claims, capture, selectors, and replay without network access. |
| `route/research-web-through-cli-or-mcp` | Research the web through CLI or MCP | user, code, framework | `feature/sim-agent-net/search-product` - One product owns canonical configuration, execution, records, and stable ids.<br>`feature/sim-agent-net/mcp-server` - The existing Skill projection derives MCP schemas, capability checks, execution, and audit.<br>`recipe/sim-agent-net/crates/sim-lib-search/01-fixture/cli-mcp-parity` - The checked fixture demonstrates identical CLI and MCP behavior without ambient network access. |
| `route/resume-durable-roadmap-execution` | Resume a roadmap execution from durable history without repeating effects | framework | `feature/sim-agent-net/durable-roadmap-execution-journal` - The adapter verifies execution identity, legal record order, byte limits, and referenced-object closure over the generic fenced journal.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/tests` - The focused fixtures cover exact effect-free replay, stale writers, duplicate receipts, identity drift, and secret-shaped payload refusal. |
| `route/review-construction-project-chief-weekly-pack` | Review a construction project-chief weekly pack | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns authoritative append-only facts, accepted baselines, current and historical snapshots, deltas, evidence states, and stable control ids.<br>`feature/sim-construction/office-role-cadence-packs` - The office-pack row filters facts before derivation, applies safety-first cadence ordering, preserves provenance and explained status, produces existing Doc, Sheet, and Deck values, and delegates presentation to the installed office suite surface.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/project-chief-weekly-pack` - The checked recipe shows the exact weekly provenance header, priority order, changed-since controls, non-green mandatory status, existing office values, and suite presentation. |
| `route/run-authorized-jvm` | Run authorized JVM classfile bytes | user, code, framework | `feature/sim-runtime/jvm-loadable-profile` - The runtime row owns bounded JVM semantics while reusing the shared classfile decoder, machine, managed graph, exceptions, and source authority.<br>`feature/sim-runtime/machine-contract` - The shared machine owns slots, frames, iterative drive, and safepoint publication.<br>`feature/sim-runtime/gc-tracing` - The managed and tracing organs own object identity, graph edges, roots, and bounded collection.<br>`feature/sim-runtime/class-semantics` - The class organ owns language-neutral declared-class structure while JVM loader identity remains JVM policy.<br>`feature/sim-runtime/capabilities-read-eval` - The source-authority organ keeps class loading explicit and diminished rather than ambient.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/product_specimen` - The public specimen executes static, managed-object, array, guest-exception, and exact-concat cases.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/ownership_guard` - Policy fixtures reject every prohibited ownership fork and scan the production JVM sources.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/baseline` - Generated opcode and intrinsic coverage totals differ from their manifests by zero. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/provider-control` - Provider families and seats are one open organ with shared conformance and fan-out.<br>`feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-generic-study` | Run and decide a deterministic evidence-backed study | user, code, framework | `feature/sim-runtime/domain-free-study-lifecycle` - The study product owns the one sealed lifecycle, staged design, report graph, selection, and effect boundary.<br>`spec-test/sim-runtime/crates/sim-lib-study/src/design` - The checked deterministic fixture covers smoke, screening, fixed confirmation, budget refusal, and selection evidence. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-local-roadmap` | Run, inspect, recover, or replay one local roadmap execution without delivery authority | framework | `feature/sim-agent-net/loadable-local-roadmap-runner` - The loaded service checks the explicit Shapes and content identities before dispatch and retains the selected generation through acknowledgement.<br>`spec-test/sim-agent-net/crates/sim-lib-roadmap-runner/src/local_command` - The focused tests prove the verb allowlist, mutation gate, identity-complete receipts, replay, and generation retention. |
| `route/run-music-algorithm-foundry` | Assemble a replaceable end-to-end music algorithm application from plan data | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns bounded SMF decoding, exact realization policy, and canonical export.<br>`feature/sim-music/audio-lift-and-render` - The audio row owns the optional audio-input analysis adapter and finite offline rendering inputs.<br>`feature/sim-music/algorithm-foundry` - The foundry row owns data-plan validation, open stage registration, Shape ranking, result checks, trace, Table/Dir boundaries, and realize-target preview delegation.<br>`feature/sim-music/bounded-harmonization` - The harmony row supplies the selected layered-DP and alternate exhaustive implementations under the same rules and bounds.<br>`feature/sim-music/music-counterpoint` - The counterpoint row supplies inspectable species rules, legal transforms, and bounded generation/analysis.<br>`recipe/sim-music/music-algorithm-foundry` - The runnable fixture checks every intermediate summary, exact SMF/WAV frame digests, data-only alternate selection, and the missing optional-stage diagnostic. |
| `route/run-numeric-pipeline` | Run a numeric pipeline | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and the numeric registry execution path.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-numeric/src/pipeline_run` - The checked pipeline-run specimen proves composed run input parsing and tensor-state admission. |
| `route/run-placement-transparent-physics-study` | Run a reviewable physics parameter sweep without coupling it to execution placement | code, framework, user | `feature/sim-physics/immutable-placement-studies` - Bind the immutable plan, generate canonical coverage evidence, preserve statistical partitions, and submit independent points through EvalFabric; Tensor-backed models retain their ordinary TensorSite placement beneath that fabric boundary. The crate integration suite proves boundaries, partitions, sampler replay, placement parity, partial failure, interruption, deterministic replay, provider envelopes, and explicit gaps.<br>`feature/sim-physics/no-energy-selection` - Produce the clean influence proof required by the selection API while leaving energy outside the choice.<br>`recipe/sim-physics/05-study/placement-sweep` - Inspect a small inclusive sweep with an exact injected case and an explicit untested region. |
| `route/run-serial-music-workbench` | Run a serial music workbench | user, code, framework | `feature/sim-music/twelve-tone-row-theory` - The pitch-serial row owns the strict source row, family/matrix inspection, and partition evidence that the workbench freezes before realization.<br>`feature/sim-music/serial-practice` - The serial practice row owns the immutable plan, strict realization context, simultaneous grouping, referential evidence, counter-voice techniques, arrays/time points, and integral parameter surfaces that the workbench assembles into one inspectable source.<br>`feature/sim-music/open-serial-adaptation` - The adaptation row owns the registry-selected modal realizer and the spine report proving aggregate relaxation explicitly rather than silently mutating the source plan.<br>`feature/sim-music/serial-consonance-completion` - The completion row owns the reversible additive patch, structural-versus-all-sounding ledger audit, and exact inverse proof after completion.<br>`feature/sim-music/midi-notation-workflows` - The MIDI and notation row owns SMF export, LilyPond projection, and the exact score lowering surface used for audition.<br>`feature/sim-music/serial-music-workbench` - The workbench row composes those replaceable owners into one executable recipe with fixture-manifest coverage and a hard no-copyrighted-score rule.<br>`recipe/sim-music/serial-workbench/01-end-to-end/row-to-audition` - The checked runnable recipe validates the fixture manifest, runs strict plus modal realization, completes and undoes one additive patch, audits the ledgers, exports MIDI plus LilyPond notation, and lowers the audition score end to end. |
| `route/run-tensor-ode` | Run a tensor ODE | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and numeric registry execution.<br>`feature/sim-numbers/tensor-ode` - The tensor ODE row owns tensor-state admission, CPU fallback, and hardware RHS restrictions.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns Env-bound executor routing and CPU fallback for tensor operations.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-rk/src/tests` - The RK tests prove tensor state across fixed/adaptive methods, hardware refusals, and CPU agreement. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/sdk-conversational-instrument-adoption` | Rebuild and atomically adopt a conversationally proposed instrument | code, framework | `feature/sim-agent-net/reproducible-instrument-adoption` - Use the canonical BRIDGE, ChangeCapsule, pack, rebuild, adoption, and rollback transaction.<br>`recipe/sim-sdk/hotload/conversational-instrument-adoption` - Follow the checked conversation-free SDK recipe. |
| `route/sdk-public-hotload-generation-loop` | Build and safely replace a native library generation through the public SDK | code, framework | `feature/sim-sdk/hotload-facade` - Use the focused facade records while keeping concrete host providers outside application code.<br>`spec-test/sim-sdk/tests/hotload_generation` - Run the deterministic build, admission, activation, refusal, reclamation, and replay proof.<br>`recipe/sim-sdk/hotload/public-generation-loop` - Reuse the two tiny sealed source generations from the cookbook surface. |
| `route/sdk-search-research-audit` | Run sim search research through deterministic conformance to the audit view | user, code, framework | `feature/sim-run/search-command` - The bootloader selects the canonical search product.<br>`feature/sim-sdk/web-search-facade` - The SDK composes the stable public contracts and checked fake-world fixture.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/web_search_offline` - The fixture proves network-free query-to-citation replay and attack boundaries.<br>`feature/sim-web/search-audit-surface` - The inert view exposes ranking, capture, selector, office, and policy evidence. |
| `route/seal-table-dir-backend` | Seal a Table/Dir backend | framework, code | `feature/sim-foundation/table-dir-core` - Keep the canonical Table and Dir contracts.<br>`feature/sim-storage/table-dir-backends` - Select the persistent backend that continues to own storage semantics.<br>`feature/sim-storage/sealed-table-decorator` - Inject host-owned key and nonce services, then wrap the backend with lane-bound authenticated encryption.<br>`spec-test/sim-storage/crates/sim-table-sealed/src/tests` - The checked fixture proves the host/Lisp authority boundary, persistent Dir composition, and fail-closed authenticated bindings. |
| `route/serve-authoritative-expression-tree` | Serve an authoritative expression tree | code, framework | `feature/sim-expr-tree/expression-tree-server` - The server row owns bounded authoritative sessions, EvalSite and EvalFabric routing, caller authority, logical-time lifecycle policy, optimistic revisions, and backpressured watches.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-server/src/tests` - The checked server specimen proves multi-client isolation, reconnect, cancellation, backpressure, authority diminution, stale commits, wall observations, idle expiry, and restart behavior. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/sim-numbers/bounded-numerical-method` | Plan and inspect a bounded numerical method | framework, user | `feature/sim-numbers/method-evidence` - Reuse criterion-specific plans and common evidence while retaining domain certificates.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-method/src/tests` - The checked tests prove bounded plans, termination evidence, and domain-adapter separation.<br>`recipe/sim-numbers/crates/sim-lib-numbers-method/01-basics/method-evidence` - The runnable recipe exposes common method evidence beside its domain result. |
| `route/sim-numbers/coefficient-polynomial` | Compute with coefficient polynomials and evidence | framework, user | `feature/sim-numbers/coefficient-polynomial` - Choose the honest exponent domain, normalize coefficients, and retain exact or numerical evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-poly/src/tests` - The checked tests prove canonical forms, exact identities, guarded approximation, and root evidence.<br>`recipe/sim-numbers/crates/sim-lib-numbers-poly/01-basics/certified-evaluation` - The runnable recipe demonstrates certified coefficient-polynomial evaluation. |
| `route/sim-numbers/extended-precision-convergence` | Rerun a numerical kernel at extended precision | framework, user | `feature/sim-numbers/extended-precision-convergence` - Keep the algorithm fixed and select DoubleDouble at the RealScalar boundary.<br>`feature/sim-numbers/method-evidence` - Retain common convergence and bounded-work evidence beside the domain result.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-extended/src/tests` - The checked tests compare extended precision against exact rational oracles and binary64 behavior.<br>`recipe/sim-numbers/crates/sim-lib-numbers-extended/01-basics/convergence` - The runnable convergence recipe demonstrates the precision-domain substitution. |
| `route/sim-numbers/sealed-interval-certification` | Certify an enclosure and make a definite decision | framework, user | `feature/sim-numbers/sealed-interval-certification` - Start with exact inputs or an existing certificate and pass only reviewed certified intervals to definite classification.<br>`feature/sim-numbers/method-evidence` - Inspect the reviewed kernel and bounded method evidence without promoting an estimate into a certificate.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-interval/src/tests` - The checked tests prove sealed construction paths and refusal of forged or insufficient evidence.<br>`recipe/sim-numbers/crates/sim-lib-numbers-interval/01-basics/sealed-threshold` - The runnable threshold recipe demonstrates a definite decision from certified bounds. |
| `route/sim-numbers/semantic-quantity` | Represent and combine a physical quantity | framework, user | `feature/sim-numbers/semantic-quantities` - The quantity owner composes generic scalars, exact dimensions, open semantic kinds, affine units, roles, Shape, and read construction.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-quantity/src/tests` - Checked laws prove kind mismatch refusal, affine behavior, exact conversion, rational powers, bounded construction, and semantic data closure.<br>`recipe/sim-numbers/crates/sim-lib-numbers-quantity/01-basics/energy-and-torque` - The runtime recipe demonstrates the required equal-dimension, different-kind distinction. |
| `route/sim-numbers/special-functions-and-ranked-grid` | Compute special functions, distribution tails, or a ranked grid | framework, user | `feature/sim-numbers/special-functions-and-ranked-grid` - Select the single numerical owner and declare the tail or tensor policy before inspecting algorithm and bounded-work evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-special/src/tests` - The checked special-function tests prove numerical-region selection and stable probability complements.<br>`recipe/sim-numbers/crates/sim-lib-numbers-special/01-basics/gamma-tail` - The runnable gamma-tail recipe exposes the selected algorithm and retained evidence. |
| `route/sim-numbers/tensor-decomposition` | Factor a dense tensor with reviewable evidence | framework, user | `feature/sim-numbers/tensor-decomposition` - Choose QR or symmetric eigen and set explicit admission and accuracy policy before inspecting the independent certificate.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-decomp/src/tests` - The checked tests prove decomposition identities, residual evidence, rank policy, and fail-closed admission.<br>`recipe/sim-numbers/crates/sim-lib-numbers-tensor-decomp/01-basics/qr-eigen` - The runnable recipe demonstrates QR and eigen evidence on a dense tensor. |
| `route/sim-run/hotload-generation-lifecycle` | Operate and explain one hot generation | user, code, framework | `feature/sim-run/loadable-hot-generation` - Use the five loadable operations, separate capabilities, Shapes, Cards, and typed records.<br>`recipe/sim-run/02-scenarios/hotload-generation-lifecycle` - Verify the public lifecycle contract offline. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/solve-stiff-ode-or-index-one-dae` | Solve a stiff ODE or declared index-1 DAE | code, framework, user | `feature/sim-numbers/numeric-pipelines` - The common numeric problem, plan, trajectory, event, and registry contracts define the composition boundary.<br>`feature/sim-numbers/implicit-radau-dae` - The implicit owner supplies Radau collocation, bounded Newton, Jacobian policies, dense output, and DAE admission.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-implicit/src/tests` - The checked specimen proves order, stiff decay, scale separation, constraint retention, singular refusal, events, and work separation. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/supply-guest-host-services` | Supply explicit host services to a guest runtime | code, framework | `feature/sim-runtime/guest-host-service-boundary` - The boundary keeps source, import, process, class-loading, and runtime configuration explicit and fail-closed.<br>`spec-test/sim-runtime/crates/sim-lib-exec/src/tests` - The execution specimen proves that process effects cross the structured caller-supplied ProcessPort boundary.<br>`spec-test/sim-runtime/crates/sim-lib-lang-lua/src/conformance` - The Lua conformance specimen exercises supplied runtime configuration without ambient host inspection.<br>`spec-test/sim-runtime/crates/sim-lib-lang-python/src/lib` - The Python specimen exercises source and module access through explicit SourceAuthority and roots.<br>`spec-test/sim-runtime/crates/sim-lib-lang-jvm/tests/product_specimen` - The JVM product specimen loads authorized class bytes through the checked loader surface. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/trace-parameter-boundary` | Trace a parameter boundary through folds with bounded restartable evidence | code, framework, user | `feature/sim-numbers/pseudo-arclength-continuation` - The continuation owner validates seeds, predicts and corrects pseudo-arclength points, adapts bounded steps, and records distinct fold, rank, loop, and domain events.<br>`feature/sim-numbers/root-finding` - The bordered Newton solve exposes shared residual, rank, damping, work, and replay evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-continuation/src/tests` - Checked fixtures prove circle, fold/cusp, singular correction, reversed seeds, domain exit, resumption, and bounded work.<br>`recipe/sim-numbers/crates/sim-lib-numbers-continuation/01-basics/limiting-boundary` - The runtime recipe discovers the limiting-boundary continuation schema. |
| `route/transform-and-normalize-audio` | Stretch, pitch, measure, normalize, and lower offline PCM | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio-lift row supplies bounded phase-preserving STFT frames and COLA evidence while delegating FFT conventions and phase unwrapping to numbers-signal.<br>`feature/sim-music/offline-audio-transform-and-loudness` - The offline transform row owns phase-vocoder policy, standards-named loudness/true-peak reports, visible normalization gain, and bounded PCM16 conversion.<br>`spec-test/sim-music/crates/sim-lib-sound-render/src/tests` - The renderer specimen proves duration/pitch invariants, transient resets, EBU reference loudness, intersample true peak, and visible normalization gain.<br>`spec-test/sim-music/crates/sim-lib-stream-file/src/tests` - The interchange specimen proves bounded channel mapping, seeded dither determinism, clipping reports, and routing through canonical WAV encoding. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns loss-audited score conversion, identity-bearing progression algebra, certified voice-leading paths, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies deterministic chord voicing palettes plus the pitch, scale, and tuning vocabulary used by exact transforms.<br>`spec-test/sim-music/crates/sim-lib-music-transform/src/tests/exact` - The checked exact-transform fixture compares greedy movement with a certified optimum over exact score identities. |
| `route/transform-musical-series` | Validate a finite musical series before applying total symbolic transforms | user, code, framework | `feature/sim-music/serial-series-calculus` - The serial row owns stable finite alphabet identity, symbol-bearing order, aggregate-rule data, typed failures, validation ledgers, validated ordinal and symbol bijections, total transform composition, algebra certificates, the Shape adapter, and the Lisp validation call while delegating generic permutation rank to sim-discrete.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/custom-alphabet` - The checked Lisp recipe validates and ranks an exhaustive five-symbol gesture alphabet, proving that the reusable calculus has no pitch dependency.<br>`recipe/sim-music/crates/sim-lib-serial-core/01-basics/certified-transforms` - The checked Rust recipe composes retrograde and rotation, applies the returned inverse, and proves exact source recovery with aggregate-preservation evidence. |
| `route/transform-signal` | Transform a real or complex signal | code, framework, user | `feature/sim-numbers/tensors` - The tensor row owns canonical complex and f64 storage shared by signal inputs and outputs.<br>`feature/sim-numbers/signal-transforms` - The signal row owns explicit transform conventions, direct references, fast Fourier plans, typed convolution/correlation geometry, guarded spectral inverse, strided tensor axes, bounded blocks, and execution reports.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/conformance` - The checked one-dimensional specimen proves impulse and tone bins, orthonormal Parseval and round trips, prime-length coverage, and fail-closed inputs.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/multidimensional_tests` - The checked multidimensional specimen proves strided direct-definition agreement, scratch admission, stable plan evidence, and blocked/in-memory equality.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-signal/src/convolution_tests` - The checked convolution specimen proves direct/FFT agreement, overlap-add/save equality and reports, correlation symmetry, identity/commutativity, and finite guarded deconvolution diagnostics.<br>`recipe/sim-numbers/crates/sim-lib-numbers-signal/01-basics/convolution-evidence` - The checked Lisp specimen exposes automatic convolution cost selection beside the retained result. |
| `route/use-blocking-http` | Issue a bounded blocking HTTP request from a capsule-backed adapter | code, framework | `feature/sim-foundation/host-primitives` - The capsule supplies DNS, socket, clock, cancellation, and trust realization without ambient host access.<br>`feature/sim-foundation/blocking-http-client` - The shared HTTP boundary owns framing, resource caps, sensitive headers, deadlines, cancellation, and authority policy. |
| `route/use-exact-utf16-text` | Preserve, inspect, encode, or round-trip exact UTF-16 code units | code, framework | `feature/sim-foundation/exact-code-unit-text` - The neutral sim-text owner preserves paired and lone surrogate code units without depending on a guest-language string or pattern type. |
| `route/use-javascript-core-profile` | Use the bounded JavaScript core profile | code, framework | `feature/sim-runtime/javascript-core-profile` - The JavaScript profile composes the shared organs under bounded ECMAScript policy without adding an engine or host loop.<br>`spec-test/sim-runtime/crates/sim-lib-lang-javascript/src/lib` - The crate conformance suite checks the bounded profile, objects, jobs, modules, collections, JSON, text, and RegExp behavior. |
| `route/use-python-core-profile` | Use the capability-scoped Python core profile | code, framework | `feature/sim-runtime/python-authorized-library-core` - The authorized core owns source admission, supplied-root modules, diminished eval and exec, and explicit fidelity gaps.<br>`feature/sim-runtime/python-object-control-policy` - The object-control row composes checked classes, descriptors, resumable control, and managed identity over shared organs.<br>`spec-test/sim-runtime/crates/sim-lib-lang-python/src/lib` - The crate conformance suite checks the authorized Python profile and its shared-organ composition. |
| `route/use-tensor-operations` | Use tensor operations | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the storage-polymorphic runtime value, checked observation, typed tensor descriptors, broadcasting, and matrix-operation libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The checked tensor citizen specimen proves runtime projection and semantic reconstruction for the canonical Tensor value. |
| `route/use-typescript-notation` | Use TypeScript notation; does not type-check | code, framework | `feature/sim-runtime/typescript-notation-profile` - TypeScript notation; does not type-check. The profile erases admitted notation directly to the unchanged JavaScript evaluator and retains only faithful browse metadata.<br>`recipe/sim-runtime/crates/sim-lib-lang-typescript/01-basics/browsable-metadata` - The notation-plus-metadata recipe is the terminal public specimen. |
| `route/verify-ledger-reconciliation` | Verify an exact ledger reconciliation certificate | code, framework | `feature/sim-ledger/statement-admission` - Start from a cutoff-frozen canonical statement snapshot.<br>`feature/sim-ledger/exact-reconciliation` - Reload the frozen inputs and immutable human decisions, then recompute coverage and bridge arithmetic independently.<br>`spec-test/sim-ledger/crates/sim-ledger/src/reconciliation` - Mutation and adversarial fixtures prove that altered evidence, duplicate coverage, overflow, and unresolved differences fail closed. |
| `route/web-capture-to-office-evidence` | Trace an office citation to its raw web capture and policy receipt | user, code, framework | `feature/sim-office/web-evidence` - The evidence adapter binds the citation to checked capture and representation ids while retaining the policy receipt.<br>`recipe/sim-office/crates/sim-lib-doc-web/01-basics/capture-to-citation` - The offline recipe shows the capture, representation, anchor, reload, and citation path without network access.<br>`spec-test/sim-office/crates/sim-lib-doc-web/src/tests` - The checked tests prove restart stability and fail-closed behavior for tampered capture, representation, selector, index, and codec metadata. |

## Unclassified routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `route/adapt-domain-model-to-physics-audit` | Feed a domain model into a physics audit without coupling physics to the domain |  | `feature/sim-physics/model-adapter-conformance` - Validate identities, boundary completeness, quantities, evidence, and influences.<br>`spec-test/sim-physics/crates/sim-lib-physics-adapter/tests/conformance` - Prove missing-port refusal and evidence/origin separation. |
| `route/audit-femm-result` | Feed a certified FEMM result into a shared physics audit |  | `feature/sim-femm/physics-audit-adapter` - Preserve FEMM quantities, certificates, provenance, and influences at the audit boundary. |
| `route/audit-interference-observation` | Feed a certified wave observation into a shared physics audit |  | `feature/sim-interference/physics-audit-adapter` - Preserve wave semantics, evidence, provenance, and influences while refusing implicit ports. |
| `route/edit-expedition-book` | Edit and replay an expedition decision book |  | `feature/sim-expr-tree/expression-tree` - The existing framework supplies the durable expression-tree organ rather than a new graph engine.<br>`feature/sim-expr-tree/expedition-book` - The book adds optimistic semantic edits, canonical export, disposable projections, and exact external evidence references. |
| `route/load-continuity-plan` | Load a continuity plan through the bootloader |  | `feature/sim-run/continuity-command` - The generic command composes registered libraries and plan data without boot-frame policy.<br>`feature/sim-agent-net/pure-continuity` - The continuity organ owns replay and policy reduction.<br>`feature/sim-platform/android-capsule` - The Android capsule owns lifecycle and platform services.<br>`spec-test/sim-run/crates/sim-run/tests/continuity` - Command tests cover networkless composition, deterministic artifacts, refusals, lifecycle events, and data-driven plan changes.<br>`recipe/sim-sdk/continuity/expedition-thought` - The first synthetic product exercises the shared continuity mechanism.<br>`recipe/sim-sdk/continuity/quiet-stewardship-cards` - The second synthetic product proves product policy stays outside composition. |
| `route/load-physics-stack` | Load independently selectable physics layers through an ordinary SIM host library |  | `feature/sim-physics/loadable-runtime-composition` - Select the narrow layer closure and load its Cards through the kernel Lib contract; the crate's runtime integration test checks manifest/export identity. |
| `route/sdk-switched-physics-audit` | Compose a switched-boundary physics solve, audit, verdict, and finding projection |  | `feature/sim-sdk/layered-physics-facade` - Select the audit and findings closures without pulling adapter or provider stacks.<br>`recipe/sim-sdk/physics/switched-audit` - Inspect the complete composition and unresolved/certified verdict boundary. |
| `route/sim-numbers/bounded-optimization` | Choose and run a bounded optimizer |  | `feature/sim-numbers/bounded-optimization` - Start from explicit bounds and select scalar Brent, projected BFGS, reflective least squares, or active-set linear least squares.<br>`feature/sim-numbers/method-evidence` - Declare termination and evidence requirements before running the selected optimizer.<br>`feature/sim-numbers/tensor-decomposition` - Reuse the canonical decomposition owner for rank and covariance evidence.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-optimize/src/tests` - The checked tests prove bounded steps, convergence evidence, covariance availability, and failure policy.<br>`recipe/sim-numbers/crates/sim-lib-numbers-optimize/01-basics/bounded-optimization` - The runnable recipe demonstrates bounded optimization with visible evidence. |
