# SIM Index Routes

<!-- GENERATED BY xtask index render; DO NOT EDIT. -->

## User routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/build-audio-workbench` | Build an audio workbench | user, code | `feature/sim-audio-daw/audio-graph-workbench` - The graph row collects the DSP and plugin recipes for local construction.<br>`feature/sim-audio-daw/audio-device-providers` - The provider row selects a modeled or host-backed audio target.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-core/01-basics/copy-node` - The copy-node recipe is the smallest graph example. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/project-charter` - The project-charter recipe shows a synthetic accepted charter and governance record with reference-only evidence. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/exchange-construction-schedule` | Exchange a construction schedule | user, code | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the XML codec and fidelity tests.<br>`recipe/sim-construction/crates/sim-codec-mspdi/01-basics/mspdi-round-trip` - The round-trip recipe shows a modeled Gantt schedule crossing the MSPDI boundary. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-tooling/generated-docs` - The generated-doc row owns the generated documentation evidence that appears in the public Index.<br>`feature/sim-index/vault-export` - The vault export row owns the profile selection, managed namespace, plan, write, and check behavior.<br>`spec-test/sim-tooling/src/index_vault_tests` - The checked end-to-end specimen proves all vault profiles export the fixture graph, preserve sibling notes, and verify managed namespaces. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-expr-tree/tests/boot` - The product smoke test proves standard help and a configured dry-run boot through the real executable.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/pcm-features` - The PCM feature recipe is the current audio-lift specimen.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/open-web-workspace` | Open a web workspace | user | `feature/sim-web/web-shell-host` - The web shell row owns the serve, browse, and atelier commands.<br>`feature/sim-web/view-surface` - The view row supplies the reversible surfaces shown by the browser host.<br>`recipe/sim-web/crates/sim-web-shell/01-basics/open-workspace` - The open-workspace recipe is the shortest browser-host example. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/prepare-balanced-ledger` | Prepare a balanced ledger | user, code | `feature/sim-ledger/ledger-command` - The command row owns the operational entry point.<br>`recipe/sim-ledger/crates/sim-ledger/01-basics/balanced-year` - The balanced-year recipe shows the expected yearly voucher shape. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/repair-output-contract` | Repair an output contract | user, code | `feature/sim-agent-net/output-contract-repair-cli` - The repair command row owns the operational correction path.<br>`recipe/sim-agent-net/crates/sim-lib-agent-runner-core/01-basics/output-contract-repair` - The repair recipe provides the worked contract fixture. |
| `route/replay-content-addressed-fleet` | Replay a content-addressed fleet | user, code | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns the stream-fabric placement library.<br>`feature/sim-agent-net/fabric-fixture-cli` - The fixture command row exposes replay from the CLI surface.<br>`recipe/sim-agent-net/crates/sim-lib-stream-fabric/01-basics/content-addressed-fleet` - The content-addressed fleet recipe is the replay example. |
| `route/review-office-document` | Review an office document | user, code | `feature/sim-office/document-codecs` - The codec row chooses the file format and round-trip path.<br>`feature/sim-office/document-surfaces` - The surface row renders the document into a reviewable view.<br>`feature/sim-office/office-site-workflows` - The site row connects the document to modeled workplace sources.<br>`recipe/sim-office/crates/sim-lib-doc-surface/01-basics/suite-surface-descriptor` - The suite-surface recipe is the shortest surface example. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/serve-over-mcp` | Serve over MCP | user, code | `feature/sim-agent-net/mcp-server` - The MCP row owns the command surface and loaded server libraries.<br>`recipe/sim-agent-net/crates/sim-mcp-server/01-basics/stdio-bootloader` - The stdio bootloader recipe shows the server entry point. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/start-a-compute-command` | Start a compute command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/compute` - The compute row owns dispatch into the loadable compute CLI and headless provider libraries.<br>`spec-test/sim-run/crates/sim-run/tests/compute` - The checked command test exercises modeled and automatic compute boot. |
| `route/start-a-glasses-plan` | Start a glasses plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/glasses` - The glasses row owns the glasses command surface and loaded glasses libraries.<br>`spec-test/sim-run/crates/sim-run/tests/glasses` - The checked command test exercises the glasses entry point. |
| `route/start-a-sim-session` | Start an interactive SIM session | user | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/repl` - The REPL is the interactive entry point.<br>`recipe/sim-run/01-basics/version` - The first recipe confirms the command path. |
| `route/start-a-watch-plan` | Start a watch plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/watch` - The watch row owns the watch command surface and loaded watch libraries.<br>`spec-test/sim-run/crates/sim-run/tests/watch` - The checked command test exercises the watch entry point. |
| `route/start-an-expression-tree-command` | Start an expression tree command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the standard command runtime.<br>`feature/sim-run/expression-tree-command` - The command row selects expr-tree and delegates the complete host composition to sim-lib-expr-tree-serve.<br>`spec-test/sim-run/crates/sim-run/tests/expr_tree` - The checked command specimen exercises the standard-distribution entry point. |
| `route/start-from-sdk` | Start from the SDK | user, code | `feature/sim-sdk/facade-runtime` - The facade row owns the public command and reversible view surface.<br>`feature/sim-sdk/device-recipes` - The device row gives wearable workflows through facade-level recipes.<br>`recipe/sim-sdk/01-basics/boot-runtime` - The boot-runtime recipe checks the smallest facade path. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the object model, sequence transforms, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies the pitch, scale, chord, and tuning vocabulary used by exact transforms.<br>`recipe/sim-music/crates/sim-lib-music-transform/01-basics/sequence-transform` - The sequence-transform recipe is the current transform specimen. |

## Code routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `local/sim-run/route/add-table-dir-backend` | Add a Table/Dir backend | code, framework | `feature/sim-run/index-table-dir` - The IndexDir implementation shows a runtime table exposed as an immutable directory.<br>`recipe/sim-run/02-scenarios/index-examples` - The examples recipe exercises table-backed feature-to-specimen lookup. |
| `local/sim-storage/route/add-table-dir-backend` | Add a Table or Dir backend | code, framework | `feature/sim-storage/table-dir-backends` - The backend row owns reusable Table and Dir implementations.<br>`feature/sim-storage/mounted-table-dir-namespace` - The mounted namespace row composes independently owned backends without copying their storage behavior.<br>`feature/sim-storage/contract-emitter` - The emitter row keeps generated storage contract evidence current.<br>`recipe/sim-storage/crates/sim-table-mount/01-basics/backend-composition` - The backend-composition recipe exercises the mounted Table/Dir surface. |
| `route/add-a-codec` | Add a codec | code, framework | `feature/sim-codecs/codec` - Start with the shared codec contract and position rules.<br>`feature/sim-codecs/contract-emitter` - The emitter row keeps generated codec contract evidence current. |
| `route/add-a-language` | Add a language | code, framework | `feature/sim-codecs/codec` - The codec row owns shared positions, limits, and loadable codec contracts.<br>`feature/sim-codecs/expression-syntax-grammars` - Expression syntax rows expose language and grammar subjects with round-trip contracts.<br>`feature/sim-codecs/pratt` - Operator-shaped languages reuse the Pratt syntax surface. |
| `route/add-a-packet-codec` | Add a packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire grammar row groups packet and byte-level protocol contracts. |
| `route/add-bounded-search-engine` | Add a bounded search engine | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns reusable algorithmic helpers.<br>`feature/sim-discrete/bounded-search` - The bounded-search row owns deterministic state-space search policy, bounds, receipts, and specimens. |
| `route/add-bridge-packet-codec` | Add a Bridge packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar. |
| `route/add-capability-gated-read-eval` | Add capability-gated read-eval | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core row owns read policy, capability checks, and read-construct evidence.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/read_construct` - The read-construct test checks object round trips through standard core. |
| `route/add-citizen-read-construct` | Add a citizen read-construct | code, framework | `feature/sim-citizen/citizen-read-constructs` - The citizen row owns runtime members and read-construct projection.<br>`spec-test/sim-citizen/crates/sim-citizen/src/conformance` - The conformance test checks citizen round-trip behavior. |
| `route/add-cookbook-recipe` | Add a cookbook recipe | code, framework | `feature/sim-foundation/cookbook` - The cookbook row owns reusable recipe metadata. |
| `route/add-discrete-domain-helper` | Add a discrete domain helper | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete row owns algebra, graph, ranking, and spectral helpers. |
| `route/add-eval-fabric-site` | Add an EvalFabric site | code, framework | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns runner and fleet placement libraries. |
| `route/add-femm-domain-helper` | Add a FEMM domain helper | code, framework | `feature/sim-femm/femm-domain-stack` - The FEMM row owns mesh, field, material, solve, and ODE helpers. |
| `route/add-femm-linear-solver` | Add a FEMM linear solver | code, framework | `feature/sim-femm/linear-solvers` - The linear solver row owns solver contracts, factor handles, iterative kernels, and certificate evidence.<br>`spec-test/sim-femm/crates/sim-lib-femm-solve/src/implementation/tests` - The checked implementation specimen proves solver reuse metadata and residual evidence for certificates. |
| `route/add-finite-enumeration-adapter` | Add a finite enumeration adapter | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns combinatorics and rank integration.<br>`feature/sim-discrete/finite-enumeration` - The finite enumeration row owns lazy words, digit/radix adapters, cyclic canonicalization, and longest-only selection. |
| `route/add-generated-doc` | Add a generated doc | code, framework | `feature/sim-tooling/generated-docs` - The xtask documentation row owns generated doc lanes and index fragments. |
| `route/add-hardware-host-surface` | Add a hardware host surface | code, framework | `feature/sim-stream-host/hardware-host-surfaces` - The host row owns placement, device adapters, and hardware-facing stream surfaces. |
| `route/add-host-exec-primitive` | Add a host exec primitive | code, framework | `feature/sim-runtime/host-exec` - The exec row keeps host process behavior in a loadable library. |
| `route/add-host-primitive` | Add a host primitive | code, framework | `feature/sim-foundation/host-primitives` - Host primitives start from shared network and surface-card helpers. |
| `route/add-host-storage-primitive` | Add a host storage primitive | code, framework | `feature/sim-storage/host-storage-primitives` - The host storage row points to file, database, and HTTP backends. |
| `route/add-incremental-query-organ` | Add an incremental query organ | code, framework | `feature/sim-runtime/incremental-query-core` - The core row owns the dependency-light memo graph and cutoff algorithm.<br>`feature/sim-runtime/incremental-query-organ` - The organ row owns SIM conversion, capability gates, shapes, reports, and loadable exports.<br>`spec-test/sim-runtime/crates/sim-lib-incremental/src/tests` - The checked tests exercise registration, invalidation, verification, cutoff, reports, and Card projection. |
| `route/add-index-graph-record` | Add an index graph record | code | `feature/sim-foundation/index-graph-core` - The index core row owns graph ids, records, edges, and validation. |
| `route/add-kernel-contract` | Add a kernel contract or reuse a plain test context | code | `feature/sim-kernel/kernel-contracts` - The kernel contract row owns the reusable protocol boundary plus bare Cx, eager Cx, and plain test context helpers.<br>`feature/sim-kernel/contract-emitter` - The emitter keeps generated contract evidence current. |
| `route/add-library-macro` | Add a library macro | code | `feature/sim-foundation/library-macros` - The macro row owns checked Rust declarations for SIM libraries.<br>`feature/sim-foundation/contract-emitter` - The emitter row keeps generated contract evidence current. |
| `route/add-model-facing-packet-workflow` | Add a model-facing packet workflow | code, framework | `feature/sim-agent-net/bridge-runtime` - The workflow row ties agent model exchange to BRIDGE packet records. |
| `route/add-number-domain` | Add a number domain | code, framework | `feature/sim-numbers/numbers` - The number row owns reusable number domains and their runtime libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The tensor citizen test checks runtime projection for a number domain. |
| `route/add-resident-femm-csr-solver` | Add a resident FEMM CSR solver | code, framework | `feature/sim-compute/femm-resident-solvers` - The compute FEMM row owns resident CSR upload, Krylov execution, f64 refinement, and loadable solver export.<br>`recipe/sim-compute/crates/sim-lib-compute-femm/01-basics/resident-csr-solve` - The recipe shows upload reuse, resident work vectors, scalar synchronization, and f64 certificate acceptance. |
| `route/add-runtime-organ` | Add a runtime organ | code, framework | `feature/sim-runtime/organs` - The organ row groups reusable binding, control, logic, pattern, incremental, mutation, namespace, and sequence behavior.<br>`feature/sim-runtime/contract-emitter` - The emitter row keeps generated runtime contract evidence current. |
| `route/add-server-backed-web-session` | Add a server-backed web session | code, framework | `feature/sim-web/codec-surface-sessions` - The bridge session owns reversible surface encode, decode, commit, and pane isolation.<br>`feature/sim-web/server-backed-web-sessions` - RemoteTransport composes that session with sim-lib-server transport negotiation and eval request frames.<br>`spec-test/sim-web/crates/sim-lib-web-bridge/src/remote_tests` - The remote transport test performs real in-process server round trips, reconnect, close, and stale-revision failure. |
| `route/add-shape-contract` | Add a Shape contract | code, framework | `feature/sim-shape/shape` - The Shape row owns matching, binding, and citizen projection.<br>`feature/sim-shape/contract-emitter` - The emitter row keeps Shape contract evidence current.<br>`spec-test/sim-shape/src/citizen/inventory` - The inventory test checks the exported shape citizen surface. |
| `route/add-shape-recipe` | Add a Shape recipe | code | `feature/sim-shape/shape-recipes` - The recipe row owns standalone Shape example crates.<br>`feature/sim-shape/shape` - The Shape row owns the contract the recipe demonstrates. |
| `route/add-stream-protocol-helper` | Add a stream protocol helper | code, framework | `feature/sim-stream/stream-protocol-helpers` - The stream row owns reusable stream, rank, topology, and device helpers.<br>`spec-test/sim-stream/crates/sim-lib-stream-core/src/read_construct` - The read-construct test checks stream metadata projection. |
| `route/add-table-dir-core` | Add a Table or Dir core contract | code, framework | `feature/sim-foundation/table-dir-core` - The table core row owns shared Table and Dir contracts.<br>`feature/sim-foundation/table-path-references` - The path reference row owns escaped absolute and relative Table/Dir path parsing, normalization, and resolution. |
| `route/add-value-helper` | Add or reuse a value helper, expression field reader, or map field access helper | code, framework | `feature/sim-foundation/value-helpers` - The value row owns shared runtime value conversion, expression builder, expression field reader, map field reader, and field access helpers. |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/build-audio-workbench` | Build an audio workbench | user, code | `feature/sim-audio-daw/audio-graph-workbench` - The graph row collects the DSP and plugin recipes for local construction.<br>`feature/sim-audio-daw/audio-device-providers` - The provider row selects a modeled or host-backed audio target.<br>`recipe/sim-audio-daw/crates/sim-lib-audio-graph-core/01-basics/copy-node` - The copy-node recipe is the smallest graph example. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/check-sim-run-index` | Check the sim-run index fragment | code | `feature/sim-run/index` - The index row owns the generated fragment.<br>`feature/sim-run/runtime-index` - The runtime command reads the merged index.<br>`recipe/sim-run/publish-readiness/package-list` - The recipe exercises package metadata. |
| `route/compile-agent-intent` | Compile agent intent | code, framework | `feature/sim-agent-net/forge-intent` - The Forge row owns checked intent compilation.<br>`feature/sim-agent-net/agent-server-tools` - Server tool rows expose the host-facing model gateway. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/project-charter` - The project-charter recipe shows a synthetic accepted charter and governance record with reference-only evidence. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/exchange-construction-schedule` | Exchange a construction schedule | user, code | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the XML codec and fidelity tests.<br>`recipe/sim-construction/crates/sim-codec-mspdi/01-basics/mspdi-round-trip` - The round-trip recipe shows a modeled Gantt schedule crossing the MSPDI boundary. |
| `route/execute-tensor-at-site` | Execute tensor at a site | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the canonical Tensor value and checked host or resident observation.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns TensorExecutor, TensorSite, and element-wise broadcast request routing.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/tests/execution` - The checked execution tests prove direct CPU execution, TensorSite realization, capability checks, and environment restoration.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-bcast/src/tests` - The checked broadcast tests prove active executor routing, CPU fallback, zero-extent broadcasting, alias safety, and decline errors. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/explore-interference-field` | Explore an interference field | code, framework | `feature/sim-web/heatmap-surface` - The domain-neutral math view validates the already reduced finite scalar grid, mask, detector label, advisory, and SurfaceCaps budget.<br>`feature/sim-web/interference-surface` - The reversible surface selects the detector reduction, preserves Study evidence, and compiles edits to interference/project or interference/solve.<br>`recipe/sim-web/crates/sim-lib-view-interference/01-basics/interference-study` - The checked recipe encodes a Study, realizes projection and model Operations, refreshes the Scene, and proves desktop, phone, and glance budgets. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-tooling/generated-docs` - The generated-doc row owns the generated documentation evidence that appears in the public Index.<br>`feature/sim-index/vault-export` - The vault export row owns the profile selection, managed namespace, plan, write, and check behavior.<br>`spec-test/sim-tooling/src/index_vault_tests` - The checked end-to-end specimen proves all vault profiles export the fixture graph, preserve sibling notes, and verify managed namespaces. |
| `route/expose-expression-tree-view` | Expose an expression tree view | code, framework | `feature/sim-expr-tree/expression-tree-view` - The expression-tree view row owns the bounded reversible outline over standard Scene, Intent, SurfaceCodec, and SurfaceCaps contracts.<br>`spec-test/sim-expr-tree/crates/sim-lib-view-expr-tree/src/tests` - The focused conformance module proves full rendering, intent reversal, lazy disclosure, continuation, freshness, hostile values, stale revisions, accessibility, large trees, layout projection, and authorization. |
| `route/expose-view-surface` | Expose a view surface | code, framework | `feature/sim-web/view-surface` - The sim-lib-view row owns the default view and reversible edit surfaces.<br>`feature/sim-web/codec-surface-sessions` - The bridge session consumes one SurfaceCodec for encode, decode, commit, projection, and isolated pane state.<br>`feature/sim-web/device-surfaces` - Device profiles show how projection adapts to concrete surface caps.<br>`feature/sim-web/web-shell-host` - The web shell row shows how a browser host loads those surfaces. |
| `route/find-a-grammar` | Find a grammar | code, framework | `feature/sim-codecs/expression-syntax-grammars` - The expression syntax row carries language and grammar subjects.<br>`feature/sim-codecs/domain-syntax-grammars` - The domain syntax row carries data, document, config, and packet syntax contracts.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire row carries packet and byte-level grammar contracts. |
| `route/find-code-behind-command` | Find code behind a command | code | `feature/sim-run/bootloader` - The bootloader owns command dispatch into loaded libraries.<br>`feature/sim-run/runtime-index` - The index trace command resolves command surfaces to owning crates and anchors.<br>`recipe/sim-run/02-scenarios/index-trace-cli` - The checked trace recipe follows a CLI surface back to code facts. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-expr-tree/tests/boot` - The product smoke test proves standard help and a configured dry-run boot through the real executable.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/inject-server-wall-clock` | Inject a server wall clock | code, framework | `feature/sim-agent-net/server-wall-clock` - The wall-clock row owns the validated timestamp contract and injectable system and deterministic implementations.<br>`spec-test/sim-agent-net/crates/sim-lib-server/src/clock/tests` - The checked clock specimen proves pre-epoch and overflow rejection, deterministic reads, and rollback-safe scheduling. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/pcm-features` - The PCM feature recipe is the current audio-lift specimen.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/load-runtime-library` | Load a runtime library | code, framework | `feature/sim-runtime/library-loading` - The loading row owns standard and language-profile runtime libraries. |
| `route/load-runtime-surface` | Load a runtime surface | code, framework | `feature/sim-run/loaders` - The loader row owns native, source, and re-exported runtime library discovery.<br>`feature/sim-run/terminal-surface` - The terminal surface row shows a loadable view surface provided by a runtime library.<br>`recipe/sim-run/02-scenarios/host-verb` - The checked host-verb recipe exercises the terminal surface path. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/parse-operator-language` | Parse an operator language | code, framework | `feature/sim-codecs/pratt` - The Pratt codec is the operator-language parser surface. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/prepare-balanced-ledger` | Prepare a balanced ledger | user, code | `feature/sim-ledger/ledger-command` - The command row owns the operational entry point.<br>`recipe/sim-ledger/crates/sim-ledger/01-basics/balanced-year` - The balanced-year recipe shows the expected yearly voucher shape. |
| `route/project-masked-scalar-heatmap` | Project a masked scalar grid as a heatmap | code, framework | `feature/sim-web/heatmap-surface` - The heatmap row validates caller-prepared finite values, masks, metadata, and SurfaceCaps-derived cell and byte budgets without applying a domain reduction rule.<br>`recipe/sim-web/crates/sim-lib-view-math/01-basics/heatmap-grid` - The checked recipe projects a bounded masked scalar grid into the domain-neutral scene/heatmap contract. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/repair-output-contract` | Repair an output contract | user, code | `feature/sim-agent-net/output-contract-repair-cli` - The repair command row owns the operational correction path.<br>`recipe/sim-agent-net/crates/sim-lib-agent-runner-core/01-basics/output-contract-repair` - The repair recipe provides the worked contract fixture. |
| `route/replay-content-addressed-fleet` | Replay a content-addressed fleet | user, code | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns the stream-fabric placement library.<br>`feature/sim-agent-net/fabric-fixture-cli` - The fixture command row exposes replay from the CLI surface.<br>`recipe/sim-agent-net/crates/sim-lib-stream-fabric/01-basics/content-addressed-fleet` - The content-addressed fleet recipe is the replay example. |
| `route/represent-interference-study-at-runtime` | Represent an interference study as runtime data | code, framework | `feature/sim-interference/tensor-runtime-records` - The runtime feature composes canonical Tensor storage, Citizen read-constructs, and Shape checks without adding a domain parser or grid storage.<br>`spec-test/sim-interference/crates/sim-lib-interference-runtime/src/citizen` - The checked Citizen inventory specimen proves every interference record can be installed and reconstructed. |
| `route/review-office-document` | Review an office document | user, code | `feature/sim-office/document-codecs` - The codec row chooses the file format and round-trip path.<br>`feature/sim-office/document-surfaces` - The surface row renders the document into a reviewable view.<br>`feature/sim-office/office-site-workflows` - The site row connects the document to modeled workplace sources.<br>`recipe/sim-office/crates/sim-lib-doc-surface/01-basics/suite-surface-descriptor` - The suite-surface recipe is the shortest surface example. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-numeric-pipeline` | Run a numeric pipeline | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and the numeric registry execution path.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-numeric/src/pipeline_run` - The checked pipeline-run specimen proves composed run input parsing and tensor-state admission. |
| `route/run-tensor-ode` | Run a tensor ODE | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and numeric registry execution.<br>`feature/sim-numbers/tensor-ode` - The tensor ODE row owns tensor-state admission, CPU fallback, and hardware RHS restrictions.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns Env-bound executor routing and CPU fallback for tensor operations.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-rk/src/tests` - The RK tests prove tensor state across fixed/adaptive methods, hardware refusals, and CPU agreement. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/serve-authoritative-expression-tree` | Serve an authoritative expression tree | code, framework | `feature/sim-expr-tree/expression-tree-server` - The server row owns bounded authoritative sessions, EvalSite and EvalFabric routing, caller authority, logical-time lifecycle policy, optimistic revisions, and backpressured watches.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-server/src/tests` - The checked server specimen proves multi-client isolation, reconnect, cancellation, backpressure, authority diminution, stale commits, wall observations, idle expiry, and restart behavior. |
| `route/serve-over-mcp` | Serve over MCP | user, code | `feature/sim-agent-net/mcp-server` - The MCP row owns the command surface and loaded server libraries.<br>`recipe/sim-agent-net/crates/sim-mcp-server/01-basics/stdio-bootloader` - The stdio bootloader recipe shows the server entry point. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/start-a-compute-command` | Start a compute command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/compute` - The compute row owns dispatch into the loadable compute CLI and headless provider libraries.<br>`spec-test/sim-run/crates/sim-run/tests/compute` - The checked command test exercises modeled and automatic compute boot. |
| `route/start-a-glasses-plan` | Start a glasses plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/glasses` - The glasses row owns the glasses command surface and loaded glasses libraries.<br>`spec-test/sim-run/crates/sim-run/tests/glasses` - The checked command test exercises the glasses entry point. |
| `route/start-a-watch-plan` | Start a watch plan | user, code | `feature/sim-run/bootloader` - The bootloader prepares the command runtime.<br>`feature/sim-run/watch` - The watch row owns the watch command surface and loaded watch libraries.<br>`spec-test/sim-run/crates/sim-run/tests/watch` - The checked command test exercises the watch entry point. |
| `route/start-an-expression-tree-command` | Start an expression tree command | user, code | `feature/sim-run/bootloader` - The bootloader prepares the standard command runtime.<br>`feature/sim-run/expression-tree-command` - The command row selects expr-tree and delegates the complete host composition to sim-lib-expr-tree-serve.<br>`spec-test/sim-run/crates/sim-run/tests/expr_tree` - The checked command specimen exercises the standard-distribution entry point. |
| `route/start-from-sdk` | Start from the SDK | user, code | `feature/sim-sdk/facade-runtime` - The facade row owns the public command and reversible view surface.<br>`feature/sim-sdk/device-recipes` - The device row gives wearable workflows through facade-level recipes.<br>`recipe/sim-sdk/01-basics/boot-runtime` - The boot-runtime recipe checks the smallest facade path. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the object model, sequence transforms, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies the pitch, scale, chord, and tuning vocabulary used by exact transforms.<br>`recipe/sim-music/crates/sim-lib-music-transform/01-basics/sequence-transform` - The sequence-transform recipe is the current transform specimen. |
| `route/use-tensor-operations` | Use tensor operations | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the storage-polymorphic runtime value, checked observation, typed tensor descriptors, broadcasting, and matrix-operation libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The checked tensor citizen specimen proves runtime projection and semantic reconstruction for the canonical Tensor value. |

## Framework routes

| Route | Title | Audiences | Steps |
| --- | --- | --- | --- |
| `local/sim-run/route/add-table-dir-backend` | Add a Table/Dir backend | code, framework | `feature/sim-run/index-table-dir` - The IndexDir implementation shows a runtime table exposed as an immutable directory.<br>`recipe/sim-run/02-scenarios/index-examples` - The examples recipe exercises table-backed feature-to-specimen lookup. |
| `local/sim-storage/route/add-table-dir-backend` | Add a Table or Dir backend | code, framework | `feature/sim-storage/table-dir-backends` - The backend row owns reusable Table and Dir implementations.<br>`feature/sim-storage/mounted-table-dir-namespace` - The mounted namespace row composes independently owned backends without copying their storage behavior.<br>`feature/sim-storage/contract-emitter` - The emitter row keeps generated storage contract evidence current.<br>`recipe/sim-storage/crates/sim-table-mount/01-basics/backend-composition` - The backend-composition recipe exercises the mounted Table/Dir surface. |
| `route/add-a-codec` | Add a codec | code, framework | `feature/sim-codecs/codec` - Start with the shared codec contract and position rules.<br>`feature/sim-codecs/contract-emitter` - The emitter row keeps generated codec contract evidence current. |
| `route/add-a-language` | Add a language | code, framework | `feature/sim-codecs/codec` - The codec row owns shared positions, limits, and loadable codec contracts.<br>`feature/sim-codecs/expression-syntax-grammars` - Expression syntax rows expose language and grammar subjects with round-trip contracts.<br>`feature/sim-codecs/pratt` - Operator-shaped languages reuse the Pratt syntax surface. |
| `route/add-a-packet-codec` | Add a packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire grammar row groups packet and byte-level protocol contracts. |
| `route/add-bounded-search-engine` | Add a bounded search engine | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns reusable algorithmic helpers.<br>`feature/sim-discrete/bounded-search` - The bounded-search row owns deterministic state-space search policy, bounds, receipts, and specimens. |
| `route/add-bridge-packet-codec` | Add a Bridge packet codec | code, framework | `feature/sim-codecs/bridge-packet-codec` - The Bridge packet codec owns the Bridge wire grammar. |
| `route/add-capability-gated-read-eval` | Add capability-gated read-eval | code, framework | `feature/sim-runtime/capabilities-read-eval` - The core row owns read policy, capability checks, and read-construct evidence.<br>`spec-test/sim-runtime/crates/sim-lib-standard-core/src/read_construct` - The read-construct test checks object round trips through standard core. |
| `route/add-citizen-read-construct` | Add a citizen read-construct | code, framework | `feature/sim-citizen/citizen-read-constructs` - The citizen row owns runtime members and read-construct projection.<br>`spec-test/sim-citizen/crates/sim-citizen/src/conformance` - The conformance test checks citizen round-trip behavior. |
| `route/add-cookbook-recipe` | Add a cookbook recipe | code, framework | `feature/sim-foundation/cookbook` - The cookbook row owns reusable recipe metadata. |
| `route/add-discrete-domain-helper` | Add a discrete domain helper | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete row owns algebra, graph, ranking, and spectral helpers. |
| `route/add-eval-fabric-site` | Add an EvalFabric site | code, framework | `feature/sim-agent-net/eval-fabric-sites` - The EvalFabric row owns runner and fleet placement libraries. |
| `route/add-femm-domain-helper` | Add a FEMM domain helper | code, framework | `feature/sim-femm/femm-domain-stack` - The FEMM row owns mesh, field, material, solve, and ODE helpers. |
| `route/add-femm-linear-solver` | Add a FEMM linear solver | code, framework | `feature/sim-femm/linear-solvers` - The linear solver row owns solver contracts, factor handles, iterative kernels, and certificate evidence.<br>`spec-test/sim-femm/crates/sim-lib-femm-solve/src/implementation/tests` - The checked implementation specimen proves solver reuse metadata and residual evidence for certificates. |
| `route/add-finite-enumeration-adapter` | Add a finite enumeration adapter | code, framework | `feature/sim-discrete/discrete-algebra` - The discrete stack owns combinatorics and rank integration.<br>`feature/sim-discrete/finite-enumeration` - The finite enumeration row owns lazy words, digit/radix adapters, cyclic canonicalization, and longest-only selection. |
| `route/add-generated-doc` | Add a generated doc | code, framework | `feature/sim-tooling/generated-docs` - The xtask documentation row owns generated doc lanes and index fragments. |
| `route/add-hardware-host-surface` | Add a hardware host surface | code, framework | `feature/sim-stream-host/hardware-host-surfaces` - The host row owns placement, device adapters, and hardware-facing stream surfaces. |
| `route/add-host-exec-primitive` | Add a host exec primitive | code, framework | `feature/sim-runtime/host-exec` - The exec row keeps host process behavior in a loadable library. |
| `route/add-host-primitive` | Add a host primitive | code, framework | `feature/sim-foundation/host-primitives` - Host primitives start from shared network and surface-card helpers. |
| `route/add-host-storage-primitive` | Add a host storage primitive | code, framework | `feature/sim-storage/host-storage-primitives` - The host storage row points to file, database, and HTTP backends. |
| `route/add-incremental-query-organ` | Add an incremental query organ | code, framework | `feature/sim-runtime/incremental-query-core` - The core row owns the dependency-light memo graph and cutoff algorithm.<br>`feature/sim-runtime/incremental-query-organ` - The organ row owns SIM conversion, capability gates, shapes, reports, and loadable exports.<br>`spec-test/sim-runtime/crates/sim-lib-incremental/src/tests` - The checked tests exercise registration, invalidation, verification, cutoff, reports, and Card projection. |
| `route/add-model-facing-packet-workflow` | Add a model-facing packet workflow | code, framework | `feature/sim-agent-net/bridge-runtime` - The workflow row ties agent model exchange to BRIDGE packet records. |
| `route/add-number-domain` | Add a number domain | code, framework | `feature/sim-numbers/numbers` - The number row owns reusable number domains and their runtime libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The tensor citizen test checks runtime projection for a number domain. |
| `route/add-resident-femm-csr-solver` | Add a resident FEMM CSR solver | code, framework | `feature/sim-compute/femm-resident-solvers` - The compute FEMM row owns resident CSR upload, Krylov execution, f64 refinement, and loadable solver export.<br>`recipe/sim-compute/crates/sim-lib-compute-femm/01-basics/resident-csr-solve` - The recipe shows upload reuse, resident work vectors, scalar synchronization, and f64 certificate acceptance. |
| `route/add-runtime-organ` | Add a runtime organ | code, framework | `feature/sim-runtime/organs` - The organ row groups reusable binding, control, logic, pattern, incremental, mutation, namespace, and sequence behavior.<br>`feature/sim-runtime/contract-emitter` - The emitter row keeps generated runtime contract evidence current. |
| `route/add-server-backed-web-session` | Add a server-backed web session | code, framework | `feature/sim-web/codec-surface-sessions` - The bridge session owns reversible surface encode, decode, commit, and pane isolation.<br>`feature/sim-web/server-backed-web-sessions` - RemoteTransport composes that session with sim-lib-server transport negotiation and eval request frames.<br>`spec-test/sim-web/crates/sim-lib-web-bridge/src/remote_tests` - The remote transport test performs real in-process server round trips, reconnect, close, and stale-revision failure. |
| `route/add-shape-contract` | Add a Shape contract | code, framework | `feature/sim-shape/shape` - The Shape row owns matching, binding, and citizen projection.<br>`feature/sim-shape/contract-emitter` - The emitter row keeps Shape contract evidence current.<br>`spec-test/sim-shape/src/citizen/inventory` - The inventory test checks the exported shape citizen surface. |
| `route/add-stream-protocol-helper` | Add a stream protocol helper | code, framework | `feature/sim-stream/stream-protocol-helpers` - The stream row owns reusable stream, rank, topology, and device helpers.<br>`spec-test/sim-stream/crates/sim-lib-stream-core/src/read_construct` - The read-construct test checks stream metadata projection. |
| `route/add-table-dir-core` | Add a Table or Dir core contract | code, framework | `feature/sim-foundation/table-dir-core` - The table core row owns shared Table and Dir contracts.<br>`feature/sim-foundation/table-path-references` - The path reference row owns escaped absolute and relative Table/Dir path parsing, normalization, and resolution. |
| `route/add-value-helper` | Add or reuse a value helper, expression field reader, or map field access helper | code, framework | `feature/sim-foundation/value-helpers` - The value row owns shared runtime value conversion, expression builder, expression field reader, map field reader, and field access helpers. |
| `route/admit-evidence-backed-construction-reference` | Admit an evidence-backed construction reference | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only accepted facts, corrections, conflicts, role authority, visibility, and reference-only evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns exact open exposure and settlement evidence without duplicating the ledger.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row proves distinct completion obligations before final closeout.<br>`feature/sim-construction/sustainability-reference-outcomes` - The outcome row proves chartered people, property, and city-district targets current and achieved.<br>`feature/sim-construction/final-economy-closeout-reference-admission` - The final row reconciles economy, closes typed obligations, enforces disclosure and authority, and creates only an immutable manifest.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/closeout-to-reference-admission` - The modeled recipe follows exact Amount and evidence facts through accountable closeout to an admitted synthetic reference manifest. |
| `route/attach-office-evidence-to-construction-control` | Attach office evidence to construction control | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the immutable fact sequence, precise construction relation, visibility, original ExternalRef, and EvidenceState.<br>`feature/sim-construction/office-evidence-bridge` - The bridge composes feature/sim-office/document-codecs and feature/sim-office/document-surfaces through the existing Doc, ExternalRef, Evidence, LinkRole, attach, and evidence_for contracts; it maps the precise construction relation without copying payloads or acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-office/01-basics/construction-office-evidence` - The checked recipe shows a project-scoped design-source fact attached and resolved through an office design register. |
| `route/build-expression-tree` | Build an expression tree | user, code, framework | `feature/sim-expr-tree/expression-tree` - The loadable framework exposes the stable capability-gated operation family, Shapes, Cards, durable Citizens, and ordinary Expr/Value behavior.<br>`feature/sim-expr-tree/mixed-backend-storage` - The storage feature composes authored source, operational control, rebuildable derived state, and explicit Table/Dir mounts.<br>`feature/sim-expr-tree/incremental-calculation` - The calculation feature supplies bounded automatic and directed dependency-aware work, receipts, refresh, and standard streams.<br>`feature/sim-expr-tree/codec-policy` - The codec policy feature applies installed general-purpose codecs with inherited read and output bounds instead of creating a tree-specific codec.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/01-basics/finite-tree` - The checked Lisp recipe creates generated names, mixes backend kinds, and resolves bare, relative, and absolute paths.<br>`recipe/sim-expr-tree/crates/sim-lib-expr-tree/02-calculation/automatic-and-directed` - The checked Lisp recipe combines automatic and manual work, inspects receipt evidence, repairs a cycle, and reopens named storage. |
| `route/build-music-performance` | Build a music performance | user, code, framework | `feature/sim-music/pitch-and-sound-vocabulary` - The vocabulary row supplies pitch and sound descriptors.<br>`feature/sim-music/midi-notation-workflows` - The MIDI row moves material into and out of performance formats.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns reusable music objects, analysis descriptors, and sequence transforms.<br>`feature/sim-music/daw-session-runtime` - The DAW session row supplies the reusable session graph around the performance.<br>`feature/sim-music/synth-performance-workbench` - The synth row turns that material into local or browser-backed sound.<br>`recipe/sim-music/crates/sim-lib-music-synth/01-basics/subtractive-preset` - The preset recipe is the smallest synth example. |
| `route/calculate-sheet` | Calculate a sheet | user, code, framework | `feature/sim-office/sheet-calculation` - The sheet row binds sparse cells and exact formula syntax to the incremental core.<br>`recipe/sim-office/crates/sim-lib-sheet/01-basics/exact-sheet-formula` - The recipe shows exact rational formula input and output.<br>`spec-test/sim-office/crates/sim-lib-sheet/src/tests` - The checked tests exercise invalidation, cycle paths, cutoff, and repeated-edit parity. |
| `route/check-construction-phase-gate` | Check a construction phase gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the accepted fact book and baseline vocabulary the gate reads.<br>`feature/sim-construction/phase-gates` - The lifecycle-gates row owns reproducible reports and accountable approvals.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/mobilization-gate` - The mobilization-gate recipe shows a derived report and matching approval at the same sequence. |
| `route/compile-agent-intent` | Compile agent intent | code, framework | `feature/sim-agent-net/forge-intent` - The Forge row owns checked intent compilation.<br>`feature/sim-agent-net/agent-server-tools` - Server tool rows expose the host-facing model gateway. |
| `route/compose-expression-tree-from-sdk` | Compose an expression tree from the SDK | user, code, framework | `feature/sim-sdk/expression-tree-composition` - The SDK row owns the single opt-in feature and direct canonical re-exports without another domain abstraction.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/expr_tree` - The public-facade specimen runs the real recipes, mixed storage, automatic and directed calculation, server-backed view, restart, and authority diminution. |
| `route/compose-genai` | Compose GenAI from reusable components | user, code, framework | `feature/sim-agent-net/bridge-runtime` - BRIDGE builds and checks the ASK packet, output contract, and repair loop.<br>`feature/sim-agent-net/model-runner-framework` - Model placement supplies the late-bound EvalFabric target.<br>`feature/sim-agent-net/genai-assembly` - The assembly feature ties the reusable pieces into one recipe.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The checked recipe is the runnable composition surface. |
| `route/compose-gpu-math-from-sdk` | Compose GPU math from the SDK | user, code, framework | `feature/sim-sdk/gpu-math-composition` - The SDK row owns the facade feature aliases and direct re-exports.<br>`recipe/sim-sdk/gpu-math/modeled-matrix-ode-femm` - The SDK recipe shows the modeled default composition and provider-neutral placement boundary. |
| `route/compose-interference-from-sdk` | Compose an interference study from the SDK | user, code, framework | `feature/sim-sdk/interference-composition` - The SDK row owns granular headless features, direct canonical exports, standard installation, compute, and reversible views.<br>`recipe/sim-sdk/interference/modeled-study` - The facade recipe shows solve, view, projection edit, model edit, re-solve, and provider-neutral site swapping.<br>`spec-test/sim-sdk/crates/sim-conformance/tests/spec/interference` - The public-facade conformance specimen executes the complete modeled workflow and unchanged-expression site swap. |
| `route/control-construction-field-item-to-accepted-evidence` | Control a construction field item to accepted evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only reported facts, stable control ids, accountable roles, evidence states, and reference-only evidence.<br>`feature/sim-construction/dalux-project-items` - The Dalux placement owns modeled and gated live reads, note-only writes, URL-free item correlations, and effect-ledger receipts.<br>`feature/sim-construction/production-field-control` - The field-control row owns observation, incident, inspection, defect, corrective-action, import, and safety-first rollup policy.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/field-item-to-corrective-evidence` - The modeled recipe follows a source-minimized Dalux item through reported fact import, safety-first blocking, rejected inspection, and accountable corrective evidence. |
| `route/control-construction-project` | Control a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the identity, charter, fact book, snapshot, governance, capability, visibility, and evidence readiness vocabulary.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/project-charter` - The project-charter recipe shows a synthetic accepted charter and governance record with reference-only evidence. |
| `route/customer-intent-to-collaboration-gate` | Take customer intent to a construction collaboration gate | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns explicit customer intent, bid/no-bid control, and collaboration readiness without a CRM or estimating workflow substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared requirement graph proves that unstated customer assumptions remain missing until accepted evidence satisfies the requirement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/customer-intent-to-collaboration` - The recipe traces modeled customer intent through conditional bid control to missing main-contract evidence for a collaboration gate. |
| `route/decide-construction-work-package-award` | Decide a construction work-package award | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable project ids, evidence references, governance roles, design inputs, and the accepted charter currency.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row owns stable package scope, supplier candidates, tender comparison facts, checked Amount comparisons, award authority, date consequences, and exposed interfaces.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/work-package-award` - The recipe shows one package through inquiry basis, corrected tender evidence, accountable award, and need-date consequence without ledger cloning. |
| `route/escalate-construction-project-risk-consequence` | Escalate a construction project risk consequence | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns current accepted facts, stable ids, evidence references, and accountable roles.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted Gantt baselines, task joins, dependencies, and critical-path consequence evidence.<br>`feature/sim-construction/risk-opportunity-forecast-escalation` - The uncertainty row owns typed risk/opportunity records, method-bearing forecast consequences, comparable exposure, and attention recommendations without making decisions.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/risk-to-project-chief-decision` - The recipe follows one current switchgear risk through critical-path and exact-amount consequence to project-chief attention. |
| `route/execute-tensor-at-site` | Execute tensor at a site | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the canonical Tensor value and checked host or resident observation.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns TensorExecutor, TensorSite, and element-wise broadcast request routing.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/tests/execution` - The checked execution tests prove direct CPU execution, TensorSite realization, capability checks, and environment restoration.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor-bcast/src/tests` - The checked broadcast tests prove active executor routing, CPU fallback, zero-extent broadcasting, alias safety, and decline errors. |
| `route/explain-blocked-construction-gate` | Explain why a construction gate is blocked | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns the stable ids, fact book, source references, capability vocabulary, and evidence state substrate.<br>`feature/sim-construction/obligation-evidence-exceptions` - The obligation row owns mandatory and optional policy, dependencies, validity windows, scoped exceptions, and deterministic requirement explanations over the canonical graph.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe shows transitive blockers, stable paths, and evidence states through one graph-backed gate report. |
| `route/explain-construction-dependency-blockers` | Explain construction dependency blockers | user, code, framework | `feature/sim-construction/obligation-evidence-exceptions` - The construction obligation row lowers requirements to the canonical graph and attaches fact evidence, exceptions, and stable ids.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/blocked-gate-graph` - The blocked-gate graph recipe demonstrates deterministic blocker explanation paths. |
| `route/explain-construction-schedule-impact` | Explain construction schedule impact | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable control ids, accepted baselines, and control graph blockers.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule-impact row owns construction task joins, baseline revision checks, as-of sequence checks, and Gantt-derived schedule explanations.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns optional modeled or gated external schedule placement.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/control-graph-meets-gantt` - The recipe shows a local baseline plan joined to construction controls with optional modeled Powerproject placement. |
| `route/explore-interference-field` | Explore an interference field | code, framework | `feature/sim-web/heatmap-surface` - The domain-neutral math view validates the already reduced finite scalar grid, mask, detector label, advisory, and SurfaceCaps budget.<br>`feature/sim-web/interference-surface` - The reversible surface selects the detector reduction, preserves Study evidence, and compiles edits to interference/project or interference/solve.<br>`recipe/sim-web/crates/sim-lib-view-interference/01-basics/interference-study` - The checked recipe encodes a Study, realizes projection and model Operations, refreshes the Scene, and proves desktop, phone, and glance budgets. |
| `route/export-index-to-vault` | Export the SIM Index to a Markdown vault | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns the public graph that the vault exporter consumes.<br>`feature/sim-tooling/generated-docs` - The generated-doc row owns the generated documentation evidence that appears in the public Index.<br>`feature/sim-index/vault-export` - The vault export row owns the profile selection, managed namespace, plan, write, and check behavior.<br>`spec-test/sim-tooling/src/index_vault_tests` - The checked end-to-end specimen proves all vault profiles export the fixture graph, preserve sibling notes, and verify managed namespaces. |
| `route/expose-expression-tree-view` | Expose an expression tree view | code, framework | `feature/sim-expr-tree/expression-tree-view` - The expression-tree view row owns the bounded reversible outline over standard Scene, Intent, SurfaceCodec, and SurfaceCaps contracts.<br>`spec-test/sim-expr-tree/crates/sim-lib-view-expr-tree/src/tests` - The focused conformance module proves full rendering, intent reversal, lazy disclosure, continuation, freshness, hostile values, stale revisions, accessibility, large trees, layout projection, and authorization. |
| `route/expose-view-surface` | Expose a view surface | code, framework | `feature/sim-web/view-surface` - The sim-lib-view row owns the default view and reversible edit surfaces.<br>`feature/sim-web/codec-surface-sessions` - The bridge session consumes one SurfaceCodec for encode, decode, commit, projection, and isolated pane state.<br>`feature/sim-web/device-surfaces` - Device profiles show how projection adapts to concrete surface caps.<br>`feature/sim-web/web-shell-host` - The web shell row shows how a browser host loads those surfaces. |
| `route/find-a-grammar` | Find a grammar | code, framework | `feature/sim-codecs/expression-syntax-grammars` - The expression syntax row carries language and grammar subjects.<br>`feature/sim-codecs/domain-syntax-grammars` - The domain syntax row carries data, document, config, and packet syntax contracts.<br>`feature/sim-codecs/wire-protocol-grammars` - The wire row carries packet and byte-level grammar contracts. |
| `route/find-expression-tree-command` | Find code behind the expression tree command | user, code, framework | `feature/sim-expr-tree/expression-tree-product` - The product row identifies the thin bootloader binary, loadable serve entrypoint, standard runtime configuration, and component recipe behind the command.<br>`spec-test/sim-expr-tree/crates/sim-expr-tree/tests/boot` - The product smoke test proves standard help and a configured dry-run boot through the real executable.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-serve/src/tests` - The serve specimen proves capabilities, in-process and external EvalFabric placement, configured storage, web-surface injection, and graceful shutdown. |
| `route/find-whether-feature-exists` | Find whether a feature exists or reuse existing code | user, code, framework | `feature/sim-index/core` - The SIM Index core row owns generated graph queries, strict fixpoint checks, overlap checks, and duplicate implementation prevention.<br>`spec-test/sim-tooling/src/index_fixpoint_tests` - The checked fixpoint test proves the index can index and validate itself. |
| `route/import-export-smf` | Import and export SMF | user, code, framework | `feature/sim-music/midi-notation-workflows` - The MIDI row owns MIDI event, SMF, stream, and notation interchange.<br>`feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the lift and lower adapters between MIDI tracks and music descriptors.<br>`recipe/sim-music/crates/sim-lib-midi-smf/01-basics/file-metadata` - The SMF metadata recipe proves the file owner.<br>`recipe/sim-music/crates/sim-lib-music-lift/01-basics/from-midi-track` - The lift recipe imports MIDI track material into music descriptors.<br>`recipe/sim-music/crates/sim-lib-music-lower/01-basics/to-midi-track` - The lower recipe exports music descriptors back to MIDI track material. |
| `route/inject-server-wall-clock` | Inject a server wall clock | code, framework | `feature/sim-agent-net/server-wall-clock` - The wall-clock row owns the validated timestamp contract and injectable system and deterministic implementations.<br>`spec-test/sim-agent-net/crates/sim-lib-server/src/clock/tests` - The checked clock specimen proves pre-epoch and overflow rejection, deterministic reads, and rollback-safe scheduling. |
| `route/inspect-compute-device` | Inspect a compute device | user, code, framework | `feature/sim-compute/compute-cli` - The compute CLI row owns devices, probe, profile, explain, and recipe command behavior.<br>`feature/sim-compute/tensor-providers` - Provider rows supply the modeled, automatic, retained-device wgpu, and optional hardware site symbols the command inspects.<br>`recipe/sim-compute/crates/sim-lib-compute-cli/01-basics/inspect-compute-device` - The recipe shows the loadable command inspecting installed sites and Table-backed profile storage. |
| `route/lift-audio-features` | Analyze current audio lift into music descriptors | user, code, framework | `feature/sim-music/audio-lift-and-render` - The audio row owns PCM feature lift, spectral summaries, and offline render evidence for audio analysis.<br>`feature/sim-music/exact-music-analysis-and-transform` - The music analysis row owns pitch histograms and exact object descriptors that consume lifted musical material.<br>`recipe/sim-music/crates/sim-lib-sound-audio-lift/01-basics/pcm-features` - The PCM feature recipe is the current audio-lift specimen.<br>`recipe/sim-music/crates/sim-lib-music-analysis/01-basics/pitch-histogram` - The pitch-histogram recipe is the current exact music analysis specimen. |
| `route/load-construction-project-control` | Load construction project control | user, code, framework | `feature/sim-construction/project-control` - The construction row composes Citizen read-constructs, Shape contracts, and standard runtime loading into one thin capability-minimal library over the complete project-control domain.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/loadable-project-control` - The checked Lisp specimen loads the library through the standard bootloader, constructs and appends a fact, queries a historical snapshot, and explains a blocker. |
| `route/load-runtime-library` | Load a runtime library | code, framework | `feature/sim-runtime/library-loading` - The loading row owns standard and language-profile runtime libraries. |
| `route/load-runtime-surface` | Load a runtime surface | code, framework | `feature/sim-run/loaders` - The loader row owns native, source, and re-exported runtime library discovery.<br>`feature/sim-run/terminal-surface` - The terminal surface row shows a loadable view surface provided by a runtime library.<br>`recipe/sim-run/02-scenarios/host-verb` - The checked host-verb recipe exercises the terminal surface path. |
| `route/open-expression-tree` | Open an authoritative expression tree in a desktop or phone browser | user, code, framework | `feature/sim-expr-tree/server-backed-web-ui` - The product composition binds one expression-tree SurfaceCodec and server resource to fresh authority-preserving RemoteTransport browser sessions.<br>`recipe/sim-expr-tree/03-server/web-session` - The checked recipe exercises create, edit, calculation, explanation, lazy collapse, progress, stale failure, reconnect, authority isolation, phone projection, and deterministic browser fixtures. |
| `route/parse-operator-language` | Parse an operator language | code, framework | `feature/sim-codecs/pratt` - The Pratt codec is the operator-language parser surface. |
| `route/place-construction-schedule-in-powerproject` | Place a construction schedule in Powerproject | user, code, framework | `feature/sim-construction/mspdi-schedule-exchange` - The MSPDI exchange row owns the portable schedule document crossing.<br>`feature/sim-construction/powerproject-schedule-placement` - The Powerproject row owns modeled receipts, gated desktop export, and Dataverse operation planning.<br>`recipe/sim-construction/crates/sim-site-powerproject/01-basics/powerproject-placement` - The placement recipe shows the modeled OLE receipt and Project for the web operation set. |
| `route/project-masked-scalar-heatmap` | Project a masked scalar grid as a heatmap | code, framework | `feature/sim-web/heatmap-surface` - The heatmap row validates caller-prepared finite values, masks, metadata, and SurfaceCaps-derived cell and byte budgets without applying a domain reduction rule.<br>`recipe/sim-web/crates/sim-lib-view-math/01-basics/heatmap-grid` - The checked recipe projects a bounded masked scalar grid into the domain-neutral scene/heatmap contract. |
| `route/prove-construction-design-release` | Prove design is released for construction production | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable construction ids, evidence states, role accountability, and the shared graph substrate.<br>`feature/sim-construction/design-release-authority-control` - The design-control row owns current revision, RFI, review, purpose-specific release, permit, inspection, authority obligation, stale release, and blocker derivation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/design-release-to-production` - The recipe proves a production package through synthetic references without storing design payloads. |
| `route/prove-construction-supplier-production-handoff` | Prove a construction supplier is ready to hand off | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable project ids, evidence states, role accountability, and reference-only evidence boundaries.<br>`feature/sim-construction/work-package-procurement-awards` - The procurement row proves the package has an accountable award without treating award as production readiness.<br>`feature/sim-construction/design-release-authority-control` - The design-control row proves the released design is current for production.<br>`feature/sim-construction/supplier-production-handoff` - The supplier handoff row owns qualification obligations, restricted supplier evidence, subcontract depth, expiry/conflict/rejection states, material lead time, and responsibility acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/supplier-to-production-handoff` - The recipe joins award, qualified supplier, production release, material timing, and accepted responsibility into one handoff-ready report. |
| `route/prove-construction-system-ready-for-handover` | Prove a construction system ready for handover | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns stable ids, append-only facts, accountable roles, and reference-only evidence.<br>`feature/sim-construction/obligation-evidence-exceptions` - The shared obligation row owns accepted evidence, validity, supersession, dependency blockers, and bounded exceptions.<br>`feature/sim-construction/system-handover-commissioning-acceptance` - The handover row owns typed hierarchy membership, leaf commissioning rollup, exact burn-down counts, six distinct completion meanings, and sequence-bound acceptance.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/system-to-accepted-handover` - The recipe follows a cross-area ventilation system through retest, explicit evidence, accepted remaining work, contractual acceptance, and still-blocked final completion. |
| `route/prove-three-to-six-week-construction-readiness` | Prove three-to-six-week construction readiness | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns accepted facts, shared requirements, exceptions, and retained snapshot sequence evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted baseline task joins and Gantt validation without duplicating schedule logic.<br>`feature/sim-construction/production-lookahead-readiness` - The lookahead row derives six-week demand, three-week commitments, constraint owner, need date, consequence, escalation, ready/not-ready/unknown, and separate human commitments.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/six-week-production-readiness` - The recipe shows a project-chief readiness snapshot over shared construction facts and accepted schedule joins. |
| `route/read-construction-items-from-dalux` | Read construction field items from Dalux | user, code, framework | `feature/sim-construction/dalux-project-items` - The Dalux row owns modeled item reads, API-identity live gating, response checks, and narrow note patches.<br>`recipe/sim-construction/crates/sim-site-dalux/01-basics/dalux-modeled-items` - The modeled recipe shows synthetic Dalux items crossing into a local office document without live service access. |
| `route/represent-interference-study-at-runtime` | Represent an interference study as runtime data | code, framework | `feature/sim-interference/tensor-runtime-records` - The runtime feature composes canonical Tensor storage, Citizen read-constructs, and Shape checks without adding a domain parser or grid storage.<br>`spec-test/sim-interference/crates/sim-lib-interference-runtime/src/citizen` - The checked Citizen inventory specimen proves every interference record can be installed and reconstructed. |
| `route/run-genai-locally` | Run GenAI locally | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework supplies provider-neutral requests, process adapters, loopback HTTP profiles, and loadable model sites for local GenAI execution.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE carries the checked model-facing packet workflow.<br>`feature/sim-agent-net/eval-fabric-sites` - EvalFabric placement resolves the selected local model site.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe is the reusable checked exchange run after placing the local model site.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs the same recipe through fake, subprocess, loopback Ollama, native provider, and OpenAI-compatible placements. |
| `route/run-genai-with-provider` | Run GenAI through a provider | user, code, framework | `feature/sim-agent-net/model-runner-framework` - The runner framework owns HTTP provider profiles and the provider-neutral request and response contract.<br>`feature/sim-agent-net/bridge-runtime` - BRIDGE keeps provider calls inside checked ASK packets and output validation.<br>`spec-test/sim-agent-net/crates/sim-lib-bridge/src/tests/ask` - The checked ASK specimen validates the exchange contract provider runners answer.<br>`recipe/sim-agent-net/crates/sim-lib-agent/01-basics/genai-assembly` - The assembly recipe stays backend-neutral while the host places the provider target.<br>`spec-test/sim-agent-net/crates/sim-lib-agent/src/tests/genai_assembly` - The checked specimen runs that recipe through OpenAI, Anthropic, and OpenAI-compatible provider placements with replacement and capability checks. |
| `route/run-interference-on-compute-site` | Run interference on a compute site | user, code, framework | `feature/sim-interference/scalar-wave-model` - The provider-independent problem fixes the propagation convention, medium, sources, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Sampling and work evidence admit the complete physical request before provider selection.<br>`feature/sim-interference/normalized-tensor-lowering` - The domain lowering exposes only bounded tile-local f32 arithmetic through canonical open Tensor operations.<br>`feature/sim-interference/resident-compute-studies` - InterferenceComputeLib composes the runtime StudySolver seam with TensorSite's active executor and preserves fallback, residency, transfer, tolerance, and failure evidence.<br>`recipe/sim-interference/crates/sim-lib-interference-compute/01-basics/modeled-resident-study` - The checked modeled recipe ends with one resident tile, three immutable uploads, zero intermediate materializations, two cached final component reads, and no fallback. |
| `route/run-numeric-pipeline` | Run a numeric pipeline | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and the numeric registry execution path.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-numeric/src/pipeline_run` - The checked pipeline-run specimen proves composed run input parsing and tensor-state admission. |
| `route/run-tensor-ode` | Run a tensor ODE | code, framework | `feature/sim-numbers/numeric-pipelines` - The numeric pipeline row owns composed pipeline values and numeric registry execution.<br>`feature/sim-numbers/tensor-ode` - The tensor ODE row owns tensor-state admission, CPU fallback, and hardware RHS restrictions.<br>`feature/sim-numbers/tensor-execution` - The tensor execution row owns Env-bound executor routing and CPU fallback for tensor operations.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-rk/src/tests` - The RK tests prove tensor state across fixed/adaptive methods, hardware refusals, and CPU agreement. |
| `route/run-tensor-on-compute-site` | Run tensor on a compute site | user, code, framework | `feature/sim-compute/tensor-providers` - The compute-provider row owns modeled resident, automatic, probe-backed wgpu, optional CUDA, and optional ROCm site placement.<br>`recipe/sim-compute/crates/sim-lib-compute-model/01-basics/modeled-resident-matrix` - The modeled-resident recipe shows chained resident tensors with one materialization.<br>`recipe/sim-compute/crates/sim-lib-compute-auto/01-basics/measured-profile-routing` - The auto recipe shows measured Table-backed profile routing, conservative CPU fallback, and ledger evidence.<br>`recipe/sim-compute/crates/sim-lib-compute-cuda/01-basics/cuda-discovery` - The CUDA recipe shows runtime ABI validation and optional site export without a toolkit dependency.<br>`recipe/sim-compute/crates/sim-lib-compute-rocm/01-basics/rocm-discovery` - The ROCm recipe shows HIP/rocBLAS ABI validation, observed gfx target evidence, and optional site export without a toolkit dependency. |
| `route/serve-authoritative-expression-tree` | Serve an authoritative expression tree | code, framework | `feature/sim-expr-tree/expression-tree-server` - The server row owns bounded authoritative sessions, EvalSite and EvalFabric routing, caller authority, logical-time lifecycle policy, optimistic revisions, and backpressured watches.<br>`spec-test/sim-expr-tree/crates/sim-lib-expr-tree-server/src/tests` - The checked server specimen proves multi-client isolation, reconnect, cancellation, backpressure, authority diminution, stale commits, wall observations, idle expiry, and restart behavior. |
| `route/show-construction-project-changes` | Show what changed in a construction project | user, code, framework | `feature/sim-construction/project-control` - The project-control row owns append-only facts, deterministic as-of snapshots, deltas, conflicts, corrections, and reference-only evidence.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/what-changed` - The what-changed recipe shows a modeled fact correction and a stable change report between project sequences. |
| `route/solve-coherent-interference` | Solve coherent interference | user, code, framework | `feature/sim-interference/scalar-wave-model` - The exact scalar-wave model fixes the propagation convention, medium, source identity, and singularity boundary.<br>`feature/sim-interference/certified-sampling` - Physical sampling and work certificates admit the complete request before field allocation.<br>`feature/sim-interference/deterministic-reference-solver` - The deterministic CPU implementation is the verified reference provider.<br>`feature/sim-interference/tensor-runtime-records` - Citizen records and Shapes carry the admitted problem, plane, field, evidence, study, and projection over the canonical Tensor.<br>`feature/sim-interference/certified-interference` - InterferenceLib exposes the complete Shape-checked workflow and resolves StudySolver from the child environment before the registry default.<br>`recipe/sim-interference/crates/sim-lib-interference-runtime/01-basics/two-source-cancellation` - The exact checked Lisp recipe realizes a strict 1024-by-1024 two-source cancellation study through core/local-fabric. |
| `route/store-construction-project-book-in-table-dir` | Store a construction project book in Table Dir | user, code, framework | `feature/sim-construction/table-backed-project-books` - The construction row composes feature/sim-foundation/table-dir-core and feature/sim-storage/table-dir-backends: it binds one project and authoritative writer to injected storage, validates every TablePath and fact before append, rebuilds from facts, and verifies disposable projections.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/table-backed-project-book` - The checked recipe documents backend injection, the canonical semantic layout, historical rebuild, and harmless projection loss. |
| `route/trace-construction-change-to-final-settlement` | Trace a construction change to final settlement | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies stable construction identity, accepted facts, accountable roles, corrections, and reference-only evidence.<br>`feature/sim-construction/gantt-schedule-impact` - The schedule row owns accepted-baseline task joins and critical-path consequence evidence.<br>`feature/sim-construction/change-commercial-exposure` - The change row owns one stable chain, exact supplier/customer components, authority state, derived exposure, and settlement reconciliation.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/instruction-to-final-settlement` - The modeled recipe follows one instruction through every change stage to matching final settlement without inferring journals, payment, or approval. |
| `route/trace-construction-sustainability-target-to-reference-evidence` | Trace a construction sustainability target to reference evidence | user, code, framework | `feature/sim-construction/project-control` - The project-control row supplies charter identity, shared obligations, evidence states, and reference-only evidence.<br>`feature/sim-construction/sustainability-reference-outcomes` - The sustainability row owns method and boundary provenance, source-retained quantities, coverage, blockers, supersession, disclosure, and reference-claim admissibility.<br>`recipe/sim-construction/crates/sim-lib-construction-project/01-basics/sustainability-to-reference-evidence` - The recipe shows climate and place targets traced from charter references to accepted or rejected reference evidence. |
| `route/transform-exact-music` | Transform exact music | user, code, framework | `feature/sim-music/exact-music-analysis-and-transform` - The exact music row owns the object model, sequence transforms, partial pitch-map policy composition, lift/lower adapters, and notation descriptors.<br>`feature/sim-music/pitch-and-sound-vocabulary` - The pitch row supplies the pitch, scale, chord, and tuning vocabulary used by exact transforms.<br>`recipe/sim-music/crates/sim-lib-music-transform/01-basics/sequence-transform` - The sequence-transform recipe is the current transform specimen. |
| `route/use-tensor-operations` | Use tensor operations | code, framework | `feature/sim-numbers/tensors` - The tensor row owns the storage-polymorphic runtime value, checked observation, typed tensor descriptors, broadcasting, and matrix-operation libraries.<br>`spec-test/sim-numbers/crates/sim-lib-numbers-tensor/src/implementation/citizen` - The checked tensor citizen specimen proves runtime projection and semantic reconstruction for the canonical Tensor value. |
